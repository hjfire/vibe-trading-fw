# Vibe-Trading 项目档案（给 AI 助手看的交接说明）

> 本文件由助手于 2026-08-14 生成，用于新对话快速了解本项目状态。2026-08-15 起经用户确认保留在仓库中（随 main 同步维护）。

## 一、项目概况

- 项目来源：HKUDS/Vibe-Trading（官方仓库）的本地副本，版本基线为官方最新 main 分支
- 用户 GitHub 仓库：https://github.com/hjfire/vibe-trading-fw
- 用户是 Git 新手，操作前需要先讲解方案并征得同意

## 二、环境与启动

- Python 3.11.9（后端），Node.js v22（前端）
- 后端依赖已用清华镜像安装：`pip install -e ".[deepseek]"`
- LLM 配置：`agent/.env`（DeepSeek，密钥不入库，已 gitignore）
- 启动：双击 `start.bat`（后端 8000 + 前端 5899）
- 同步官方更新：双击 `sync.bat`（等价于 git pull）

## 三、Git 架构（重要）

```
main 分支结构：
  官方 HKUDS/Vibe-Trading 完整历史（地基）
    ├── 本地自定义①：start.bat 一键启动脚本
    ├── 本地自定义②：vite.config.ts 代理端口 8899→8000
    ├── 本地自定义③：api_server.py sys.modules 注册修复（2行）
    └── 本地自定义④：sync.bat 同步脚本 + 每日同步工作流
      └── 本地自定义⑤：项目档案.md 交接文档（仅本地，未入库的 wiki 产物已用 .git/info/exclude 忽略）
        └── 本地自定义⑥：sync-upstream.yml 改用 SYNC_TOKEN 个人访问令牌推送（修复同步推送被拒）
          └── 本地自定义⑦：akshare 技能补充 references/ 参考文档（4 个文件，纯新增零冲突）
            └── 本地自定义⑧：TradingView 风格图表页（/tv-chart 试水 + /pro-chart 落地，后端 /market/kline）
                  └── 本地自定义⑨：自选列表 watchlist（/market/quote + 侧边栏导航入口 + A股新浪日线回退）
                    └── 本地自定义⑩：指标公式系统（自写公式 + 12 内置模板 + 主图/副图，内置解释器不走 eval）
                          └── 本地自定义⑪：TradingView Pine Script 兼容层（公式+策略）/ 脚本库 / 导入导出与分享链接 / 条件筛选器 / 标的周期持久化
                              └── 本地自定义⑫：主图布林带显示修复（figure 必带 type + 标准差总体口径）
                                └── 本地自定义⑬：K 线拖动弹回修复（DataLoader 方向语义，新增 klinePaging.ts）
                                  └── 本地自定义⑭：画线可用性修复（面板高度预算 paneLayout.ts + 画线持久化 chartDrawings.ts）
                                    └── 本地自定义⑮：画线编辑一致性收尾（onRemoved/onPressedMoveEnd 回写 + starved 告警 + 尊重手拖高度）
                                      └── 本地自定义⑯：画线样式可配（颜色/线宽/虚实写进 overlay.styles + 选中改这条 + 样式随线持久化）
                                        └── 本地自定义⑰：画线清单管理（逐条选中/锁定/隐藏/删除 + lock/visible 随线持久化）
                                          └── 本地自定义⑱：画线导出/导入/分享链接（.json 容器 + ?d= 深链，复用 ⑪ 的分享码 codec）
                                            └── 本地自定义⑲：副图也能画线（面板稳定地址 `sub:<指标名>` + 关指标时画线暂存/回流，不再被库悄悄改写回主图）
                                              └── 本地自定义⑳：画线落点不得锚在最新 K 线右侧的空白「未来区」（吸回最后一根，吸成一点则放弃）
                                                └── 本地自定义㉑：报警内核 + IM 推送（/alerts 页 + 规则状态机 + TradingView/Pine webhook 入站桥）
                                                  └── 本地自定义㉒：数据接口按账号实际权限优先走本机 FutuOpenD（网关熔断/节流/批量快照 + 三条股票链前置 + 盘中实时价）
                                                    └── 本地自定义㉓：代码输入联想 type-ahead（本机 32k 行名册 + GET /market/symbols + 四个标的输入框）
                                                      └── 本地自定义㉔：应用内使用说明（/help 页渲染 public/usage-guide.md + 侧栏「使用说明」入口）
                                                        └── 本地自定义㉕：分钟 K 线全市场优先本机 FutuOpenD（.SH/.SZ/.HK/.US + 60m→1H 映射 + qfq 口径守卫 + 修分钟轴 8 小时偏移）
```

- 远程：`origin` = 用户仓库（SSH），`upstream` = 官方仓库（SSH）
- 自动同步：`.github/workflows/sync-upstream.yml` 每天北京时间 20:00 自动合并 upstream/main 到用户仓库
- 备份分支：本地 `backup-20260813`（最初无关历史的初始提交，确认无误后可删）
- 新功能开发流程：feature 分支开发 → 合并回 main → push
- 已禁用继承的官方工作流（CI 等），只保留 Sync upstream
- 本地自定义⑦说明：官方 `agent/src/skills/akshare/SKILL.md` 文末引用了 `references/` 子目录但目录不存在，本地补建了 4 个实测验证过的参考文档（分钟线/基本面资金面/指数成分/宏观期权链），接口实测日期 2026-09-04（akshare 1.18.81）；东财 push2 系列接口在用户本机被代理拦截，文档已标注新浪替代路径
- 本地自定义⑧说明：新增 TradingView 风格图表能力，均为纯新增文件 + 极小接线（冲突面可控）。
  - 前端新增两页（各自独立路由，不改官方页面）：`frontend/src/pages/TVChart.tsx`（/tv-chart，嵌 TradingView 官方免费 widget，仅试水验证交互需求，数据走 TradingView 公开服务器）；`frontend/src/pages/ProChart.tsx`（/pro-chart，开源 KLineChart v10 Apache-2.0，红涨绿跌，MA/VOL/MACD 内置 + 6 种画线工具，数据走本项目自有 /market/kline）。
  - 前端新增 `frontend/src/lib/marketApi.ts`（/market/kline 客户端，独立于上游 api.ts，复用 apiAuth.authHeaders，零改上游）。
  - 后端新增 `agent/src/api/market_routes.py`（GET /market/kline；日线复用 `backtest.correlation._fetch_price_series` loader 回退链，A股分钟线走 akshare 新浪 `stock_zh_a_minute`；时间戳全链路统一为**毫秒**以匹配 KLineChart，`before` 参数支持左滚分页）。
  - 接线上改动（上游活跃区，注意同步后复查）：`api_server.py` 加 2 行注册 market 路由；`vite.config.ts` 的 PROXY_PATHS 加 `"/market"`；`router.tsx` 加 /tv-chart、/pro-chart 两路由；`package.json`+`package-lock.json` 加 `klinecharts`（顺带把 node_modules 对齐到上游已声明的 vite ^8.2.2）。
  - 尚未做（用户要求"逐步长出"）：~~自选列表②~~（已在⑨完成）、~~筛选器③~~（已在⑪完成：条件脚本 × 标的池扫描）。
  - 验证：端到端实测通过（挂载/数据/周期切换/标的切换/画线链路/毫秒时间戳/非A股自动禁用分钟按钮/无红字无404）。
- 本地自定义⑨说明：⑧的"逐步长出"第二步——自选列表 + 导航入口。
  - 新增 `frontend/src/components/charts/WatchList.tsx`：/pro-chart 左侧自选侧栏（localStorage 键 `pro-chart.watchlist.v1`，默认 6  preset 标的；行显最新价+涨跌幅（红涨绿跌）；支持点行切图/搜添/删除/手动刷新，无轮询以保护上游数据源）。
  - `ProChart.tsx` 布局改为左列表右图表，工具栏加"☆加自选/★已自选"按钮；顺手修复⑧遗留的无效 Tailwind 类（项目配置无 accent/panel/glass 色系，选中高亮改用真实存在的 bg-muted）。
  - 后端 `market_routes.py` 新增 GET /market/quote（批量报价，每标取 2 根日线算涨跌幅；**串行**拉取——loader 链非线程安全，并发时仅首个标的能拿到数据）。
  - **A股日线改新浪优先**（`stock_zh_a_daily`，qfq）：主链 tencent 接口高频调用后会限流返回 HTTP 501，eastmoney/akshare-东财被本机代理拦截，新浪不受影响；主链仍作备胎，source 字段如实上报路径。
  - **重大稳定性修复**：akshare 新浪接口依赖的 py_mini_racer（内置 Chromium）在 Windows 非主线程首次使用时会直接把后端进程 V8 FATAL 崩掉（partition_address_space 断言失败，表现为接口忽好忽坏、进程整个退出）；已在 market_routes.py 用 `_run_sina` 把所有新浪调用收敛到单一专用线程串行执行，连发 8 轮压测不再崩。
  - **侧边栏导航入口**（用户反馈"找不到新功能"的根因）：`Layout.tsx` NAV 末尾加 /pro-chart 条目（TrendingUp 图标），8 个语言包各加 `layout.proChart` 一行；/tv-chart（试水页）仍仅 URL 直达不进导航。
  - 验证：/market/quote 6 标的全绿（~14s）；日线 init+backward 分页 200 不重叠；dev(5899)/prod(8000) 双模式 UI 实测：导航可见可点、报价出数、source 显示 sina 路径、无 console 错误。
- 本地自定义⑩说明：/pro-chart 指标公式系统（用户要求：自己编写公式 + 内置常用公式 + 可主图/副图显示）。
  - 新增 `frontend/src/lib/indicatorLang.ts`：**自带分词器 + 递归下降解析器 + 树遍历解释器**的迷你公式语言（Pine 风格：`名字 = 表达式;` 逐行赋值、`return { 线名: 序列 }`、算术逐根广播、支持中文标识符、`//` `#` `/* */` 注释）；无循环语法因此不可能卡死，错误带行号。编译后注册为 KLineChart 自定义指标（overlay→`series:"price"` 挂 candle_pane，pane→独立副图）。
  - **关键约束（不可回退为 eval）**：生产模式 CSP 为 `script-src 'self'`（`agent/src/api/security.py`，有单测锁定），`new Function`/`eval` 在 :8000 下直接 EvalError。首版用 eval 写时 dev 能跑、prod 一条指标都挂不上，已实测确认后改写为解释器；**不要为这个功能去放宽 CSP**。
  - 新增 `frontend/src/lib/indicatorTemplates.ts`：12 个内置公式模板（主图 5：EMA双线/布林带/唐奇安/VWAP/ATR通道；副图 7：RSI/金叉信号/CCI/威廉%R/ROC+信号/AO/MFI），均已用独立 Wilder 参考实现做数值交叉校验。
  - 新增 `frontend/src/components/charts/IndicatorEditor.tsx`（右侧抽屉工作台：模板库/编辑器/参数/主副图选择/启停/删除/语法速查）+ `indicatorStore.ts`（localStorage 键 `pro-chart.userIndicators.v2`）。
  - `ProChart.tsx`：工具栏"ƒ 指标公式 · N"入口 + 数据就绪后按**图表实例**（WeakSet）重放已保存指标。两个必输坑：① 用单个 boolean 做守卫会被 React StrictMode 双挂载的**首个（已 dispose）chart**消费掉，导致真图永不恢复；② 恢复失败的提示不能复用 `status.error`——紧随的自动 backward 分页会把它重置掉（实测存活 <1s），必须独立 state 常驻。
  - 测试：`frontend/src/lib/__tests__/indicatorLang.test.ts` 56 条（逐函数数学值/广播/下标/中文键/解析期与求值期错误分类/挂载参数断言）；dev+prod 双模式端到端 15 项全过（含模板应用、手写公式、刷新自动恢复、坏公式持久告警、切标的/切周期重算、无 console 错误）。
  - ~~未做（待用户需要时再加）：公式共享/导入导出、标的与周期选择不持久化~~ → 均已在⑪完成。
- 本地自定义⑪说明：**兼容 TradingView 的公式与策略**（用户原话），顺带完成⑧计划的③条件筛选器与标的/周期持久化。
  - **Pine 引擎（纯前端解释执行，与⑩同一 CSP 前提）**：`pineLang.ts`（词法+递归下降解析+AST：换行/缩进决定语句块、`ta.xxx` 点命名空间、`key=value` 命名参数、`[a,b]=ta.macd(...)` 多返回、用户自定义函数 `f(x) =>`（单行/块体/元组返回，局部量按调用点保留历史））→ `pineRuntime.ts`（**逐 bar 标量执行**，历史缓冲实现 `x[k]`，`input.*` 声明顺序即参数索引）+ 内置库 `pineTa.ts`/`pineMath.ts`/`pineOrders.ts` + 类型层 `pineTypes.ts`；对外 API 在 `pineScript.ts`（`compilePine`/`validatePine`/`pineDefaults`/`toArtifact`/`isPineStrategy`）。`strategy()` 走 `pineOrders.ts` 的逐 bar 交易模拟，产出净收益/回撤/胜率/盈亏比/权益曲线报告。
  - **两套语言共存**：`indicatorLang.detectDialect(code)` 嗅探后在 `applyUserIndicator` 内部分发，⑩ 的向量公式一字未改；持久化仍用同一个键 `pro-chart.userIndicators.v2`。
  - 新增 `scriptExchange.ts`（`.pine` 文本容器 `//> k: v` 头 / JSON bundle / `?s=<g|j><base64url>` 分享链接，导出名 `SHARE_QUERY_KEY` 读写共用）、`scriptLibrary.ts`（27 条对标 cn.tradingview.com/scripts 分类：趋势/振荡/量能/波动率/策略，逐条实测跑通）、`screener.ts`、`klineLocale.ts`。
  - 工作台从 1 个文件长成 5 个标签：`components/charts/workbench/{types,EditorTab,LibraryTab,ExchangeTab,ReportTab,ScreenerTab}`，`IndicatorEditor.tsx` 只做外壳（⑩ 的 props 契约保持不变，ProChart 无需改动即可用）。
  - **必须知道的坑**：
    1. KLineChart 在 `calc` 内部计算，策略报告/告警只能通过 `indicatorLang` 的侧信道（`lastArtifacts` + `subscribePineArtifact`）回到 UI；发布发生在挂载时同步进行，所以点「应用」后报告立即可读。
    2. 编辑器实时预览要用小预算 `PROBE_BUDGET=6e6`（运行时默认 `OP_LIMIT=2.5e7`），否则逐字符输入时卡死；`getChart` 必须存进 ref 不入 useEffect 依赖，不然 setProbe→重渲染→再编译无限循环。
    3. `Number("") === 0` 造成两个真实缺陷：空的参数框会把脚本自己的 `input.*` 默认值全压成 0（`workbench/types.ts: parseParams`）；`.pine` 头 `//> params:` 为空时掺进一个 0（`scriptExchange.ts: numberList`）。
    4. 提示条必须挂在外壳（`{lines, bad}`），因为「导入成功→自动跳编辑器」会把原标签卸载，提示跟着消失。
    5. 策略报告的候选 chip 不能只看「是不是 Pine」：`indicator()` 也满足，选中它却仍显示上一个策略的数字。有 artifact 用 `result.scriptKind`，没有 artifact 才用 `isPineStrategy(code)` 源码嗅探（先剥注释，避免 `// strategy.entry(...)` 误判为策略）。
    6. **KLineChart 的 i18n 查表无守卫**（`locales[locale][key]`）：⑧ 一直传的 `"zh_CN"`（下划线）不是它支持的 tag，导致十字光标 tooltip 每次重绘抛 `Cannot read properties of undefined (reading 'day'/'minute')`（进页即刷 2~13 条）。现由 `klineLocale.ts` 用库自带 `getSupportedLocales()` 解析，单测锁定「答案必在支持列表内」。
    7. **兼容性只能用别人写的代码验收**：自写的 27 条库脚本会不自觉地避开引擎短板，喂进 4 段从网上抄回来的真实 Pine（TradingView 官方内置 Supertrend、awesome-pinescript 的 MA 交叉策略、社区 Enhanced RSI、他平台文档里的 SuperTrend 策略）当场暴露 6 个缺陷：用户自定义函数被词法直接拒绝（`=>` 视为不支持）、`if (cond)` 被 `parseIf` 吞掉左括号、`ta.supertrend` 的 direction 符号与 TV 相反、`strategy.entry` 反手不平反向仓、`color.new()` 被拍平成文本丢色、`display=display.none` 仍画出一条多余的线。新增 `__tests__/pineRealWorld.test.ts` 把这 4 段源码原文钉成回归。
    8. **TV `ta.supertrend` 的权威约定**（已按官方 Pine 正文重写为逐句移植）：`direction = 1` 是下跌趋势（线在价格上方作阻力）、`-1` 是上涨（线在下方作支撑），所以官方写法是 `plot(direction < 0 ? supertrend : na, "Up Trend")`；轨道只“棘轮”（上轨不升、下轨不降，除非收盘价越轨），趋势态用 `prevSuperTrend == prevUpperBand` 判定并且用**当根**收盘价比较，不是上一根。验收方式很值钱：同一段脚本里同时跑手抄的 Pine 函数与我们的 `ta.supertrend`，逐 bar 要求一致（300 根里 291 根可比，一致率 >95%、平均轨距 < 均价 1%）——旧实现一致率只有 64.8%。
    9. **Pine 用户函数的作用域语义**：参数在**调用方**作用域求值；函数体内变量按**调用点**分桶（`f<cid>`）各自保留历史，所以体内 `x[1]` 是上一根同一调用点的值；块体最后一句的值即返回值；`[a, b]` 后面没有 `=` 时是数组字面量返回而不是解构声明（靠 `looksLikeTupleDecl()` 前瞻区分）；顶层 `fn` 在构造期提升注册，脚本可以先调用再定义。
    10. **策略是净头寸模型**：`strategy.entry` 方向与持仓相反时必须先平掉**所有**反向仓位（与订单 id 无关）——真实脚本普遍用 `"Long"`/`"Short"` 两个 id 反手，旧实现按 id 匹配反转，结果整份回测 0 笔平仓交易；`pyramiding<=1` 时同向不同 id 不再叠加。
    11. **`?s=` 深链接在 dev 下静默失效**（prod 看不出问题）：React StrictMode 双挂载——首挂载读 query 并立刻 `replaceState` 抹掉 → cleanup 把 `alive` 置 false → 次挂载已无码可读，而异步解码结果又被 `alive` 守卫丢弃，抽屉不弹、无报错。现把分享码与 in-flight Promise 提到模块作用域（`ProChart.tsx` 的 `takeShareTask`/`stripShareQuery`），哪个挂载先跑谁负责，且只交货一次。教训：**dev 与 prod 表现不一致时两边都要实测**。
  - **条件筛选器不新造语言**：条件就是工作台脚本（已保存的或编辑器草稿，不必先挂载），`scriptSeries`/`applyRule`/`screenOne` 是纯函数，`screenPool` 只做 4 路并发 + `AbortController` 协作取消 + 逐行回调。扫描前用当前图上的K线做「预检」：判定需要 2 条输出线而脚本只有 1 条时直接禁用「开始扫描」，避免整池 6 只全部返回同一句原因的废扫描（prod 实测踩过）。
  - 持久化：`pro-chart.session.v1`（symbol+interval，过期自愈——非 A股不保留分钟线）、`pro-chart.screener.v1`（判定/周期/标的池/自定义列表）。换标的时**先修周期再 setSymbol**，否则切到美股的第一发请求必 400。
  - 验证（⑩ 那轮 prod 冒烟之后）：全量 `vitest run` **73 文件 801 用例**全绿（新增 `pineRealWorld.test.ts` 10 条真实源码验收）；`tsc --noEmit` 零错；`vite build` 通过。dev(:5173) 端到端用**未改写的真实源码**跑了两条链路：（a）`?s=` 分享链接自动弹开工作台并灌入代码（修复后复验）、（b）导入导出粘贴→自动跳编辑器。TV 内置 Supertrend：主图 `candle_pane` 挂上 Up Trend(#26a69a, 415/1000 根) 与 Down Trend(#ef5350, 566/1000 根)，`display=display.none` 的填充锚点没多画线，参数表单自动生成 ATR Length=10 / Factor=3，改 ATR=20 后线值确实变化（末根 1267.52→1263.70），`fill()` 降级如实上报。MA 交叉策略：编译行标出「含策略回测」，主图 6 个 figure（两条 MA + 买入开仓/卖出平仓/卖出开仓/买入平仓 33/33/33/32），策略报告 65 笔 / 胜率 27.69% / 盈亏比 0.22 / 最大回撤 85.50% / 净收益 -8,199.76（初始 1 万），多空严格交替，刷新后数字一致，全程 console 0 error。
  - **已知边界**（诚实告知，不假装等价）：Pine v5 的 `label/table/bgcolor/fill` 等装饰类降级为提示不影响主线（`fill()` 只提不渲染，`display=display.none` 的 plot 不产出线但仍可当句柄）；不支持 `while`、`+=`、`request.*`（多周期/跨标的）；已支持用户自定义函数（`f(x) =>` 单行与块体、`def`/`function` 前缀、默认值参数、`[a, b]` 元组返回）、`if (cond)` 带括号写法、`var` + `:=` 自引用；`strategy.*` 实现 entry/close/exit 与常用设置（初始资金/手续费/滑点/qty 类型/止损止盈），报告是**本地逐 bar 模拟**，不与 TradingView 服务器数值对齐；**不做保证金/风控干预**（上面那个 -82% 就是脚本自己 `initial_capital=10000` 买 100 股 1300 元茅台的 13 倍杠杆，TV 不填 `margin_required` 时同样照回测）；`plot` 全区间无数据的线会被丢弃并告警（因此 markers=0 不等于 `plotshape` 没执行）。
- 本地自定义⑫说明：**主图布林带显示错误修复**（用户报「我在主图显示布林带指标显示错误」，顺带统一两个引擎的标准差口径）。
  - **坑 A：figure 缺 `type` 会被 KLineChart 整条静默丢掉**。`prepareIndicatorFigures`（dist 3081-3133）只在 `isValid(figure.type)` 时把 figure 放进 `preparedFigures`，而画线（8093）、副图图例（7272）、右侧末值标记（9874）三处都只遍历 `preparedFigures` → ⑩ 向量语言注册的是 `figures: keys.map(k => ({key:k, title:k}))`，**没有 type**，所以「布林带 BOLL（主图）」模板算得出 1000 行正确数值、图上却一条线都不画，而且不报任何错。Pine 那条线（`toKcfFigure`）一直传了 type，所以只有向量模板受害。修法：`indicatorLang.ts` 向量注册补 `type:"line"`，标题一并改成 `"MID:"` 形式与内置指标一致。
  - **坑 B：标准差口径必须是总体（÷n）**。Pine `ta.stdev(source, length, biased)` 的 `biased` 默认 **true** = 总体估计（官方文档原文 `math.sqrt(sumOfSquareDeviations / length)`），TA-Lib BBANDS ddof=0、KLineChart 内置 BOLL 亦 `sqrt(sum/dataSize)`；我们三条实现（`pineTa.stdStep` / 向量 `indicatorLang.stdev`）原来都用 ÷(n-1)，布林带系统性偏宽 √(n/(n-1)) ≈ 2.6%（n=20）。现 `stdStep(src,n,st,biased=true)` 支持第三参数、`ta.stdev`/`ta.bb`/`ta.variance` 与向量 `stdev` 全部走总体。`math.stdev(array)` 未动（没有权威依据说明它与 `ta.stdev` 同口径，不做无证据改动）。
  - 验收方法（视口只有 456×407 且 `document.hidden=true`，截图与像素比对不可用）：React fiber 遍历拿到 live Chart 实例存 `window.__chart`，再 `import('/src/lib/indicatorLang.ts')` 复用**同一模块实例**直接调 `applyUserIndicator`，读 `chart.getIndicators()` 的 `paneId/series/figures[].type/result`。两个自查坑：注册名前缀是 `UCI_` 不是 `usr-`（按错名会误判「指标没挂上」）；`calc` 是异步的，同步立刻读 `result` 得到 0 行会误判「没数据」。
  - 新增回归：`pineIndicatorWire.test.ts`「每个输出序列都必须可画」3 条（向量 figure 的 type/标题、12 个公式模板逐个挂载、脚本库全条目）+「两套引擎口径一致」1 条（同一个布林带用向量模板与 Pine `ta.bb` 逐根重合，并锁定手算总体 σ，防两个引擎一起漂）；`pineScript.test.ts` 2 条钉 `ta.stdev` 默认总体 / `biased=false` 样本、`ta.bb` 三返回值 = 中轨 ± mult·σ。变异验证：把向量 `stdev` 临时改回 ÷(n-1)，两条断言当场报红。
  - 实测证据（dev :5899，1000 根真实 K 线）：修复后 `UCI_v-boll` 与 `UCI_p-boll` 的 figures 全为 `=line`、paneId `candle_pane`；末根三条线 向量 = Pine = KLineChart 内置 BOLL = MID 1310.2385 / UP 1355.5177 / LOW 1264.9593，981 根可比 K 线上**最大偏差 0**（修复前最大相对偏差 0.5693%）。
  - 全量：`vitest run` **73 文件 807 用例**全绿；`tsc --noEmit` 零错；`vite build` 通过（chunk >500kB 为既有告警）。
- 本地自定义⑬说明：**主图拖动后弹回原位、且停不下来修复**（用户原话「鼠标按住拖动K线移动，移动后会自动跳回移动前界面，无法停止需要停止的界面」）。这不是鼠标处理 bug，是 `DataLoader` 把 KLineChart v10 的方向语义读反了。
  - **库源码实证**（`klinecharts/dist/index.esm.js`）：`_processDataLoad("forward")`（13635）传给业务的 `timestamp` 是 `dataList[0].timestamp` = **最旧**一根，`_addData` 收到后 `data.concat(dataList)` = **前插**且**不动视图**（13473）；`_processDataLoad("backward")`（13631）传的 `timestamp` 是 `dataList.at(-1).timestamp` = **最新**一根，`_addData` 收到后 `dataList.concat(data)` = **追加**，并且 `_startLastBarRightSideDiffBarCount -= data.length` = **主动挪视图**（13464-13468）。触发条件（13598-13603）是 `from === 0 && more.forward` / `to === totalBarCount && more.backward`，而刚打开的图**正好停在右边缘**。
  - **因此旧代码的死循环**：`const before = type === "backward" && timestamp ? timestamp : null` 把「最新一根的时间戳」当 `before` 发给后端（后端语义是「严格早于」）⇒ 拿回的就是**已经握着的那一整页**；又因为 `callback(bars, { backward: bars.length >= PAGE, forward: false })` 无条件把 backward 报成 true ⇒ 库把这 500 根重复 bar 追加到右侧、按长度平移视图（弹回）、`more.backward` 仍为 true ⇒ 再问一次。实测一轮 500 根重复、时间戳乱序，永不自愈。
  - 修法：抽出纯函数模块 `frontend/src/lib/klinePaging.ts`（`boundsOf` / `pagingBefore` / `shapeResponse`，文件头即上面那张方向对照表 + 源码行号），`ProChart.tsx` 的 loader 只用这三个函数。关键一条：`shapeResponse` **只回图表还没有的 bar**（forward 过滤 `ts < oldest`、backward 过滤 `ts > newest`），more 标志按**真正交付的条数**推导 ⇒ 重叠页变成「空答案 + 停止标志」，这一类错再也循环不起来。loading 态条件同步改为 `type !== "backward"`（右边缘静默拉新，不闪 loading）。
  - 验收方法：视口 456×407 / 主图 74×100px 下 `scrollByDistance` 与 pointer 合成事件都不可靠（rAF 不跑、事件面不通），所以**翻页契约在 jsdom 里复刻库规则来验**：`ProChartPaging.test.tsx` mock 掉 klinecharts（fake chart 逐条照抄 `_addData` 的前插/追加/视图位移/more 单标志更新）与 4000 根假后端，用 `settle()` 复刻 13598-13603 的边缘触发，断言「自动停止 + 无重复无乱序 + `viewShift === 0` + `before` 严格递减不重复 + backward 不追加任何东西」；纯函数层另配 `klinePaging.test.ts` 9 条（含一条**旧写法回归钉**：30 轮 ⇒ 重复 >10000 根、`more.backward` 仍 true、`viewShift = -15000`）。
  - 变异验证：把 loader 临时改回旧写法，`ProChartPaging.test.tsx` 3 条全部抛 `分页没有停止：已发出 42 次请求`；还原后 3/3 绿。
  - 实测证据（**prod :8000 真实构建 + 真实后端**，600519.SH / 5m）：初始只 **1 次** kline 请求（`count=500` 无 `before`）、500 根 **0 重复**；向右边缘停着（`to === totalBarCount`，正是 backward 触发条件）静置 2.5s + 3 次继续外推 ⇒ 请求数**恒为 1**、offRight 稳定不回弹；向左拖到左边缘后才出现第 2 次请求 `before=1787318700000`，前插后 `from: 0 → 497`（**视图仍锚在同一根**，不是弹回）；一路拖到底共 4 次请求，`before` 严格递减（1787318700000 → 1786097700000 → 1784815500000），页数 500→1500→1970（末页 470 < 500 ⇒ 自动停止），全程 `duplicates: 0`、`outOfOrder: 0`，console 0 error 0 warn。
  - 全量：`vitest run` **75 文件 819 用例**全绿（+2 文件 +12 用例）；`tsc --noEmit` 零错；`vite build` 通过。
- 本地自定义⑭说明：**画线功能无法使用（真因是面板布局）+ 画线/指标两条链路系统核查**（用户原话「画线功能无法使用，请系统性核查画线功能和指标功能，确保功能正常，显示正常」）。画线的接线本来是对的，坏在**主图被副图挤到 29px**，用户点不到主图。
  - **库源码实证**：`ChartImp._layout`（`klinecharts/dist/index.esm.js` 14787-14830）先 `remaining = chartHeight - xAxisHeight - separatorCount * styles.separator.size`，除主图外每个 pane 取 `max(minHeight, height)`（**默认 height=100**，minHeight=30，见默认样式 13251-13257），`candle_pane` 是 flexible pane **只拿余数**（14822）⇒ 每加一个副图指标主图固定被吃 100px。另一半：overlay 落点的 `value` 由**接收鼠标事件的那个 pane 的 y 轴**换算（dist 8798-8811），所以挤扁主图后「价格线」存的是成交量量级。6 个工具名（segment/rayLine/horizontalStraightLine/fibonacciLine/priceLine/brush）**都是 v10 内置**（dist 11938-12708），不是名字问题；v10 事件绑的是 `mousedown/mousemove/mouseup`（1511-1873），不是 pointer。
  - 修法 1（可用性地基）：新增纯函数模块 `frontend/src/lib/paneLayout.ts` 复刻上面那套算术（`subPaneIdsOf` / `planPaneHeights`，常量 `DEFAULT_X_AXIS_HEIGHT=26`、`DEFAULT_SEPARATOR_SIZE=1`，默认 `mainMinFraction=.55` / `mainMinPx=180` / `subMinPx=44` / `subMaxPx=120`，返回 `assignments` + `starved`）。`ProChart.tsx` 的 `applyPaneLayout()` 读 `getPaneOptions()` → 对副图 `setPaneOptions({id, height})`（v10 `createIndicator(value, isStack?)` **没有** v9 的第三个 paneOptions 参数，只能事后设）。四个触发点：mount、loader 恢复用户指标后、`[indCount, layoutTick]` 效应（工作台任意 commit 都 bump tick，避免「一换一」时数量不变而面板变了）、以及 **ResizeObserver 观察宿主元素**（宿主是 `flex-1`，窗口尺寸不变也会因侧栏/抽屉改高）。两个守卫：`|pane.height - want| < 2` 跳过（防 setPaneOptions↔relayout 自激）、`state !== "normal"`（被用户最大化的面板）不参与重排。
  - 修法 2（画线语义 + 持久化）：新增 `frontend/src/lib/chartDrawings.ts` —— arm 时**显式 `paneId: "candle_pane"`**；`isInProgress()` 优先读运行时 `isDrawing()`（d.ts 未声明，结构守卫后回退 `currentStep >= 0`），库**没有**公开的 forceStopOverlay，取消 = `removeOverlay({ id })`；`serializeDrawings()` 丢弃半成品与**没有 timestamp 的点**（裸 `dataIndex` 会在前插历史后指向别的 bar）；按 `pro-chart.drawings.v1` 以 `symbol|interval` 分桶（200 条/桶、60 桶、空列表删桶、全程 try/catch）。交互补齐：按钮激活态 ring + `title`/提示行 + 二次点同一按钮退出 + **Esc 取消** + **撤销** + 清除，画线随标的走（loader 里 key 变了先存旧桶再 `removeOverlay()` + 恢复新桶），卸载时落盘。
  - **指标侧核查结论**：5 个指标数据层全部健康（MA/UCI 挂 `candle_pane` 且 `series=price`、figures 带 type，VOL/MACD/UCI 各占一副图，均 500 行），删指标后面板正确折叠、无孤儿面板；显示层唯一真问题就是上面的高度分配。
  - **库层 footgun（务必传对象）**：`removeIndicator(filter)` 传**字符串**会被当成空过滤对象 ⇒ **一次删光图上全部指标**（实测 VOL/MACD/两个 UCI 同时消失）。本项目调用点都是 `{name}`（`indicatorLang.ts` 1029/1107/1118），已由 `indicatorLang.test.ts:389` 与 `ProChartDrawings.test.tsx` 双向钉住。
  - 验收方法（视口 456×407 且 `document.hidden=true`）：截图/像素差分不可用，改用 `getDom(paneId,'main').getBoundingClientRect()` + `getPaneOptions()` + `getOverlays()` 取证；**合成鼠标事件必须全部打在同一个 canvas 元素上**（库的 mousemove/mouseup 绑在 rootElement，派发到 document 不会向下传播）；隐藏页 `setTimeout` 被限流到 1 次/秒，脚本一律零 sleep、一次一个动作，React 的 state 回读要放到下一次调用里。
  - 实测证据（**prod :8000 真实构建 + 真实后端**，600519.SH / 5m，宿主 358px）：修复前主图 **29px**、`价格线` 落点 `value=52874.25`（成交量量级）；修复后主图 **182px**、三个副图各 **49px**（与 `planPaneHeights` 逐像素吻合：usable=358-26-3=329 → 副图 49 → 主图 182），点「价格线」两下 → `value=1328.73`（价轴，末根 K 线 1330），画笔一拖 → 7 个点全在 1328~1331 且 `mouseup` 即完成；`pro-chart.drawings.v1["600519.SH|5m"]` 落盘，**刷新后自动复原**（提示「已画 1 条」）；撤销 → 桶变 `{}`、清除 → overlay 归 0；Esc 取消半成品（overlay 归 0、ring 消失）；工作台关掉那个副图指标 → 面板消失、其余两副图 49→74，再勾回 → 三副图重新 49；拖拽平移 `offsetRight 32 → 20` 且**不回弹**（kline 请求恒为 1，⑬ 未回归）；全程 console **0 error 0 warn**。
  - 变异验证（三条都命中）：① `applyPaneLayout` 里把 `chartHeight` 写死 0 ⇒ 2 条断言报红（`expected 100 to be 75`、`expected 100 to be 49`）；② 去掉 `paneId: MAIN_PANE_ID` ⇒ 「按钮点亮工具，并钉住主图 pane」报红；③ loader 里禁掉画线换绑 ⇒ 「画线跟着标的走」报红（茅台的线留在了苹果图上）。
  - 全量：`vitest run` **78 文件 854 用例**全绿（+3 文件 +35 用例：`paneLayout.test.ts` 11 / `chartDrawings.test.ts` 15 / `ProChartDrawings.test.tsx` 9）；`tsc --noEmit` 零错；`vite build` EXIT=0。附带把 `ProChartPaging.test.tsx` 的假 chart 补上 `getOverlays`/`getPaneOptions`/`setPaneOptions`（⑭ 的 loader 现在会读它们，缺一个就整文件报错）。
- 本地自定义⑮说明：**画线「改完不生效 / 删了又复活」收尾 + starved 告警 + 尊重手拖高度**（⑭ 审计时发现但当时没修的两个真实缺口 + 一处死代码）。根因一句话：KLineChart v10 对画线的**事后编辑一律不回传**，除非建 overlay 时就把回调传进去。
  - **库源码实证**（`klinecharts/dist/index.esm.js`）：
    - 按住拖动是**原地改写** `overlay.points`：`pressedMouseMoveEvent`（8632-8654）在 `!overlay.lock` 时按 figureType 走 `eventPressedPointMove`（8386-8400，写 `this.points[pointIndex]`）或 `eventPressedOtherMove`；松手才触发 `onPressedMoveEnd`（8603-8608，`checkOverlayFigureEvent` 会看 `figure.ignoreEvent`）。
    - **右键点线 = 库内置删除手势**：`_figureMouseRightClickEvent`（8774-8790）未 preventDefault 就 `store.removeOverlay(overlay)`；`getOverlaysByFilter` 在有 id 时**只按 id 匹配** ⇒ 只删一条，不会误删同名线。DOM 侧绑的是 `contextmenu`（1517-1521）。
    - **`removeOverlay` 先回调后拼接**：`StoreImp.removeOverlay`（14459-14492）在 **14467 调 `onRemoved`**、**14475-14478 才 splice** ⇒ 回调里 `getOverlays()` 仍含被删者。所以 `makeDrawingEvents` 把**被删 id 回传**给调用方（`serializeDrawings(list, excludeId)` / `refreshDrawCount(chart, excludeId)`）；不这么做就是「删掉的线刷新后复活」+「已画 N 条说谎」两个 bug 同时在线。
    - 安全性：`StoreImp.destroy()`（14594）用 `_overlays.clear()`，**不触发 onRemoved** ⇒ 组件卸载不会把存储清空（否则切页面就丢画线）。
    - 库自身怪癖（非本项目 bug）：priceLine 端点的 `figureIndex` 是 **-1**，而 `eventPressedPointMove` 用 `this.points[pointIndex]` ⇒ 拖 priceLine 的**端点**无效（实测 liveValue 不变），拖**线身**有效。
  - 修法：`chartDrawings.ts` 新增 `DrawingEvents` / `makeDrawingEvents()`（onDrawEnd → 解除工具激活态 + 落盘；onRemoved → 半成品被删时也要解除卡死的激活态 + 带 excludeId 落盘；onPressedMoveEnd → 落盘），`serializeDrawings` 加 `excludeId`，`restoreDrawings` 加 `events`（**从存储恢复的线同样必须带回调**）；`ProChart.tsx` 四处 —— ① `syncDrawings`/`refreshDrawCount` 全链路走 excludeId；② 切标的换绑期间用 `drawingsMutedRef` 静默（否则逐条 onRemoved 会把**旧标的**的桶写成空，真丢数据）；③ `layoutSigRef`（`chartHeight|副图id列表` 签名）签名不变就不重排 ⇒ **尊重用户手拖的分隔条**；④ `paneStarved` 从死代码变成琥珀告警「副图过多，主图仅 Npx — 关闭部分指标可恢复」（title 里带副图 px），并把「拖动可改位，右键点线删除」写进提示行与底部说明（这个手势库有、用户不知道）。
  - 实测证据（**prod :8000 真实构建**，600519.SH / 1D）：画 priceLine 1320.67 → 实例带 `onDrawEnd/onRemoved/onPressedMoveEnd` 三个函数 → 用与右键处理器完全相同的路径删除（`removeOverlay(overlayObject)`）⇒ 图上 0 条、提示回 idle、撤销/清除两按钮 disabled、**存储变 `{}`**（**修复前实测：清图后桶里那条线仍然留着**）；重画 1316.12 → 刷新 → **恢复出来的线同样带三个回调** → 复刻库的原地改点 + `onPressedMoveEnd` ⇒ 线上值 1290.62、**存储同步 1290.62**；切 AAPL ⇒ 茅台桶完整保留、AAPL 无残留，切回 ⇒ 线回来且仍带回调；kline 请求 1→2→3 无翻页风暴（⑬ 未回归）；点「撤销」⇒ overlay 归 0 + **存储 key 被删** + 提示回 idle；starved：宿主压到 238px ⇒ 两副图被夹到 **44px 地板**、告警文字「主图仅 122px」与**真实 DOM 主图 122px 完全一致**，复原 ⇒ 74/74、告警消失、主图 182；手拖守卫：`setPaneOptions({height:130})` 模拟拖拽后切周期（走 loader commit，会调 `applyPaneLayout`）⇒ 副图 **DOM 实测仍 130px**（主图由库重算成 126），⑮ 之前这里必被抹回 74；两段式工具（趋势线）在真实构建上可完成（`pts:2, step:-1, values:[1375.04,1347.16]`）；全程 console **0 error 0 warn**。
  - 探针教训（白走三步，都不是产品问题）：① 合成绘制的第二落点**必须在视口内且落在真实 bar 上**——`elementFromPoint` 视口外返回 null，而落在最后一根 bar 右侧空白没有 `timestamp` ⇒ `nextStep()` 不推进，看着像「工具坏了」；② 同一个 `evaluate_script` 里连点两次同一工具按钮会读到 stale 的 `drawTool`，两次都走「退出」分支；③ 内嵌浏览器合成的 `contextmenu` 不触发库的删除（改走与右键处理器等价的 `removeOverlay(对象)` 直调取证）；④ 宿主 `min-h-[360px]` 优先于内联 `height`，压高度要同时设 `minHeight='0px'`；⑤ React fiber 找 chart ref 要沿 `f.return` **向上**走，沿 child/sibling 走找不到。
  - 变异验证（5 段探针）：M2（丢 excludeId）/M3（禁 mute）/M4（禁签名守卫）/M6（去半成品解除激活态）/M7（onPressedMoveEnd 空转）同时注入 ⇒ 6 红；发现 **M2 会掩盖 M3**（M2 让 onRemoved 存的是「仍在列表里的旧线」，恰好把旧标的的桶保住了）⇒ 还原 M2 单独跑 M3 ⇒ 精确 1 红（「画线跟着标的走」`expected undefined to deeply equal [{name:'priceLine'…}]`）；单独跑 M5（`setPaneStarved(null)`）⇒ 精确 1 红。全部还原。首轮真实红测还抓到一处**产品级缺陷**：`refreshDrawCount` 漏了 excludeId ⇒ 图上 0 条却显示「已画 1 条」。
  - 全量：`vitest run` **78 文件 865 用例**全绿（+11：`chartDrawings.test.ts` 15→19、`ProChartDrawings.test.tsx` 9→16）；`tsc --noEmit` 零错；`vite build` EXIT=0（2.58s）。测试假 chart 升级为**镜像库契约**（`removeOverlay` 先回调后 splice、`createOverlay` 把事件挂到实例上），否则这类「顺序 bug」在单测里永远绿。
- 本地自定义⑯说明：**画线样式可配**（⑮ 汇报里列的下一增量）。能力：5 色 × 3 线宽 × 实/虚；**先点线再点样式 = 改这条**，没点线 = 只影响下一条；样式**随每条线自己**存进画线桶，刷新复原；默认值不落盘。
  - **库源码实证**（`klinecharts/dist/index.esm.js` + `index.d.ts`）：
    - 每条线的样式是 `overlay.styles`，类型为全局 `styles.overlay` 的 **DeepPartial**（d.ts 1122；`OverlayStyle` 本体 486-495，**带 `[key: string]: unknown` 索引签名**）。图元最终样式 = `{ ...defaultStyles[type], ...overlay.styles?.[type], ...figure.styles }`（dist **8955**）⇒ **图元自带 styles 会盖过 overlay.styles**，逐个工具核对过：我们暴露的 6 个工具里只有 `brush` 带 `smooth/lineCap/lineJoin`（11944-11960），fibonacciLine（12023-12033）/priceLine（12337-12352）/rayLine（12410-12415）/segment（12441-12446）/horizontalRayLine **均不锁 color** ⇒ 颜色一定能生效；`simpleAnnotation`（12465-12470）用 `styles:{line:{style:'dashed'}}` 证明模板层就是这个写法。
    - 默认样式 11741-11780：`line { style:'solid', color:Color.BLUE, size:1, dashedValue:[2,2] }`，`Color.BLUE = '#1677FF'`（11371），**text 是白字蓝底** ⇒ 改色必须 line/point/text 三处一起，否则「红线 + 蓝价签」像 bug。
    - 改已存在的线只有 `overrideOverlay({id, styles})`（d.ts 978 / dist 14430-14444）：内部 `merge(this.styles, styles)`（8288-8291，merge 见 157-174，原始值直接覆盖）；`shouldUpdate()` 用**对象身份**比 `prev.styles !== this.styles`（8314-8318）⇒ 任何 override 必重绘。它走 `getOverlaysByFilter`（**14285-14298**），而 `isValid` 只挡 null/undefined ⇒ **缺 id = 命中图上全部 overlay**（一次改色全图），空串 id = 一条都不命中（静默无效）。
    - 选中态唯一来源是 `onSelected`/`onDeselected`（8687-8702，载荷 `{chart, overlay, figure, ...event}`）；`setClickOverlayInfo`（**14538-14557**）的顺序是**先 deselect 旧、再 select 新**，点同一条不重复触发，点空白 deselect ⇒ 清选中必须按 id 守卫。右键 `onRightClick`（8774-8790）其实支持 `preventDefault()`，但**决定不劫持**该手势（保留库内置的右键删除）。
  - 修法：`chartDrawings.ts` 加样式层 —— `DrawingStyle`/`DEFAULT_DRAWING_STYLE`/`DRAWING_COLORS`/`DRAWING_SIZES`/`withAlpha`/`normalizeDrawingStyle`（颜色非法→null、线宽夹到 1..6）/`OverlayStyleFragment`（**索引签名不是装饰**，缺了会被 `DeepPartial<OverlayStyle>` 拒）/`overlayStylesOf`（线 + 端点 + 价签同色）/`styleOfOverlay`（读回样式，等于默认→undefined）/`applyDrawingStyle`（**id 必填**）/`StoredDrawing.style` + serialize/restore 双向带上/`loadDrawingStyle`+`saveDrawingStyle`（偏好是独立 key `pro-chart.drawStyle.v1`，不进画线桶）。`ProChart.tsx` 加 `drawStyle` + `selectedDraw` 两个状态、`drawingEvents` 扩 `onSelected`/`onDeselected`、`armTool` 传 `styles`、样式工具条（5 圆点 + 1/2/3px + 实/虚，选中时 title 变「同时改掉选中的线」）、提示链 `armedTool → selectedDraw → 已画 N 条 → idle`。
  - prod 实测证据（**真实构建 :8000**，600519.SH / 5m；内嵌浏览器 hidden ⇒ rAF 不跑、**画布不上色 ⇒ 不能像素取色**，改为读实例 `styles` + 存储）：点「红」→ 已 arm 的 overlay 实例即带 `styles.line={color:'#F23645',size:1,style:'solid'}` + `text.backgroundColor:'#F23645'`；画布真实单击 → 线落在**价位** 1327.82（`paneId:candle_pane`）→ 存储出现 `"style":{"color":"#F23645","size":1,"dashed":false}`；选中它点「绿」⇒ 该线变 `#089981`（价签同色）；**未选中**点「橙」⇒ 两条线纹丝不动、只有偏好变橙（`overrideOverlay` 没被调用）；随后新画的第二条线实例为 `{#FF9800, 2, dashed}` ⇒ 「新线用新样式」闭环；**隔离性**：选中第三条线点红 ⇒ 它变 `#F23645`，前两条仍是 `#089981`/`#FF9800`（一次只改一条）；撤销这条被选中的线 ⇒ 图上 2 条、提示「已画 2 条」、选中态被 id 守卫正确清空；第三条线点落在右侧**价格轴**上（点只有 `value` 无 `timestamp`）⇒ **按 ⑭ 规则不写盘**，撤销后存储一字未改；刷新 ⇒ 两条线各带自己样式回来（`#089981/1/solid` 与 `#FF9800/2/dashed`，`dashedValue:[4,2]`），且恢复实例 `onSelected/onDeselected/onRemoved/onPressedMoveEnd` 四个回调齐备；全程 console **0 error 0 warn**。
  - prod 抓到并修掉的真实缺陷：`pickStyle` 原先从**渲染闭包** `drawStyle` 合成下一个值 ⇒ 同一轮里两次样式点击（2px + 虚线）只生效最后一次（实测偏好停在 `{size:2, dashed:false}`）。改为 `drawStyleRef` 镜像「最新请求值」再合成，并补一条用例锁死。
  - 探针教训（都不是产品问题）：① 画布 **x=330 是 bar 区、x=360 已在价格轴上**，落轴上的点无 `timestamp` ⇒ 不会进存储，别误判成「持久化坏了」；② **一次 `evaluate_script` 只做一次动作**，程序化连点两个按钮时 React 不在两次派发之间重渲染（prod 与 jsdom 同现象，jsdom 侧已被 M7 证实）；③ 线画完会被库**自动选中**，「已选中」提示可能来自上一条而非本次点击，判读选中态要跨调用分两次读（本轮第一次「选中改色无效」就是这个误读，不是 bug）；④ 探针里打印 `isDrawing` 时必须从**原始实例**读，不能从自己 map 过的普通对象上读（一度因此误判「v10 实例没有 isDrawing」；枚举原型链确认 `isDrawing/forceComplete/shouldUpdate/override` 均在 `OverlayImp` 上，`isInProgress` 优先走 `isDrawing()`、`currentStep` 只是兼容旧数据的路径）。
  - 变异验证（**8 段单注入**，每段精确红 1-3 条，全部还原）：M1a 价签不跟色 → 2 红；M1b 端点色不派生 → 1 红；M2 `restoreDrawings` 丢 styles（颜色刷新后丢）→ 2 红；M3 去掉 `applyDrawingStyle` 的 id 守卫 → 1 红（为此把假 `overrideOverlay` 改成**镜像 `getOverlaysByFilter` 真实语义**：缺 id 命中全图、空串命中 0 条 —— 之前的假实现太宽松，M3 永远绿）；M4 不落偏好 → 2 红；M5 `armTool` 不带 styles → 3 红；M6 `onDeselected` 不清选中 → 1 红；M7 `pickStyle` 退回读渲染闭包 → 精确 1 红（`expected {color:'#1677FF',size:1,…} to deeply equal {size:2,dashed:true}`）。
  - 全量：`vitest run` **78 文件 885 用例**全绿（+20：`chartDrawings.test.ts` 19→28、`ProChartDrawings.test.tsx` 16→27）；`tsc --noEmit` 零错；`vite build` EXIT=0（2.39s）。
- 本地自定义⑰说明：**画线清单管理**（⑯ 汇报里列的下一增量）。画出来的线过去只有「在图上点中它」这一条访问路径，于是：画在副图里的线、藏起来的线、遮住了别的线的线都管不了。能力：工具栏「清单 · N」开一块可滚动面板，每行 = 工具中文名 + 落点时间/价位 + 当前颜色圆点 + 点数，动作 = **选中**（复用 ⑯ 的「选中改这条」）/**锁定**（拖不动）/**隐藏**（图上不画）/**删除**；lock、hidden 与样式一样**随每条线自己**持久化，且**只在偏离库默认时写盘**。
  - **库源码实证**（`klinecharts/dist/index.esm.js` + `index.d.ts`）：
    - `lock` / `visible` / `zLevel` 是 `Overlay` 顶层字段（d.ts 1070-1081：`lock` 注释即“will not respond to events”），且 `OverlayCreate = Omit<Overlay, "drawingMode"|"currentStep"|"totalStep"|create*Figures|performEvent*>` ⇒ 二者都能直接当 `createOverlay` / `overrideOverlay` 的**顶层键**传；`OverlayImp.override` 把除 `id/name/currentStep/points/styles` 之外的一切 `merge(this, others)` 写进实例（dist 8277-8306）。**但这三个被 `__rest` 排除的键并不是“改不了”：points 与 styles 各有专属分支（points 传入即整体替换并把 `currentStep` 置 FINISHED，8292-8306），“override 改不动 points”是误读 —— ⑳ 的原地修线就建立在这条分支上。**
    - **`lock` 只被一处读取**：`_figureMouseDownEvent` 开头 `if (overlay.lock) return false;`（dist **8731-8734**）。click 选中（8746）与右键删除（8774）**都不看 lock** ⇒ 「锁定」的准确语义是**防误拖**，不是只读；UI 文案按这个写（`锁定后拖不动这条线（右键仍可删）`）。
    - **`visible` 才是重绘开关**：`OverlayView.drawImp` 里 `if (overlay.visible) _drawOverlay(...)`（dist **8895-8899**）；而图元的 `registerEvent` + `addChild` 发生在**绘制阶段**（`_createFigureEvents`，8612-8670）⇒ 跳过后那条线**既不画也点不到**，隐藏顺带防误触，找回只能走清单。
    - **本轮最坑的一处**：`shouldUpdate()`（**8314-8318**）只比较 `zLevel` 变化 / `points` JSON / `visible` / `extendData` / `styles` 对象身份，**不比较 `lock`**；`StoreImp.overrideOverlay`（14430-14458）只在 `sort||draw` 时 `updatePane` 并 **return false** ⇒ 纯锁定改动**已经生效但返回值说没生效**。任何「按返回值决定要不要落盘」的写法都会静默丢状态，所以 `applyDrawingFlags` 不信返回值，改为 `getOverlays({id})` **读回实例校验**（M4 变异证明这层不可省）。
    - `getOverlaysByFilter`（14285-14310，⑯ 已记）还有一条对本次成立：`isValid` 只挡 null/undefined ⇒ 过滤 `{}` 会连**画到一半**的 overlay 一起返回 ⇒ 清单与序列化都必须过 `isInProgress`，否则出现只有 1 个点的“趋势线”行。
  - 修法：`chartDrawings.ts` 加 `DrawingFlags` + `applyDrawingFlags`（id 必填 + 读回校验）、`DrawingRow` + `describeDrawing`/`listDrawings`（行 = 中文名 + `formatBarTime` 整点省时钟、秒/毫秒自适应 + `formatPrice` + 点数 + 两个 flag）、`StoredDrawing.lock?/hidden?` 与 serialize/restore 双向（`...(o.lock === true ? { lock: true } : {})`、`...(o.visible === false ? { hidden: true } : {})`；恢复侧 `...(d.hidden ? { visible: false } : {})`）。`ProChart.tsx` 加 `drawRows`/`drawPanelOpen`，并把 `refreshDrawCount` 改成**一次读图同时喂清单与「已画 N 条」**（同源，结构上消灭 ⑮ 那类「数字说谎」）；`toggleDrawPanel` 顺带 `setLayoutTick(t=>t+1)` 让面板占高参与副图预算；`flagDrawing`（成功才 `syncDrawings`）、`removeDrawing`（走 `chart.removeOverlay({id})`，复用 ⑮ 的 `onRemoved` + excludeId 链路，不另起一套删除）、`focusDrawing`（再点同一行取消选中）。
  - prod 实测证据（**真实构建 :8000**，600519.SH / 1D，500 根 bar）：起手写 3 条线（趋势线两击、价格线、水平线）→ 按钮 `清单 · 3` 可用，面板 3 行文案 `趋势线09-04 08:00 → 09-08 08:00 · 1336.33 → 1295.91` / `价格线09-05 08:00 · 价位 1275.69` / `水平线09-06 08:00 · 价位 1316.12`，行内色圆点与实例颜色一致；**开面板前后主/副图高度 182/74/74 一字不变**（`layoutTick` 重排没把主图挤窄，无 starved 告警）；点「锁定」→ 实例 `lock:true`、按钮文字 锁定→解锁、行首出现「已锁定」、存储**只给这一条**加 `"lock":true`（另两条一字未动）；**锁定 A/B 对照**：同一套合成拖动打在**未锁定**的趋势线上 → `_pressedOverlayInfo='…:other'` 且两点从 1336/1296 移到 1316/1276 并**回写进存储**（⑮ 链路在真实构建里成立），打在**已锁定**的价格线上 → `_pressedOverlayInfo=null`（8731 分支挡在按下之前）、`value` 分毫未动；点「隐藏」→ 实例 `visible:false`、存储 `"hidden":true`、`清单 · 3` 与行都不少（隐藏≠删除）、该行点不出选中（`_clickOverlayInfo` 保持 null）；点「显示」→ 实例回 `visible:true` 且存储里 **`hidden` 键整个消失**（默认值不落盘）；**刷新后**三条线的 `lock/hidden/points` 原样回到实例上，面板仍显示 已锁定/已隐藏；**从清单删除**（未选中项）→ 图 2 条、存储 2 条、`清单 · 2`、选中态不被误清（提示仍是「已选中一条线」），再删（选中项）→ 提示退回「已画…」；删到 0 → 面板空态「当前标的与周期上还没有画线」、按钮 `清单 · 0` 且 `disabled`、存储桶键消失；**⑯×⑰ 集成**：从清单选中第二条 → 点「红」⇒ 只有这条的 `line/point` 变 `#F23645`（价签保持 `#FFFFFF` 白字蓝底契约）、存储只给它加 `style`、行内圆点实时变红，第一条保持 `#1677FF`；全程 console **0 error 0 warn**。
  - **本轮首次突破「隐藏标签页不上色」限制**（三条都是 harness 坑，不是产品坑，但直接决定 prod 取证能验到什么深度）：① **rAF 不跑 ⇒ 画布从未绘制**，`canvas.width/height` 停在默认 300×150、`Canvas._listener()` 一次不执行，而图元事件是**绘制时注册**的 ⇒ 「点线选中 / 按住拖动」在这套 harness 里必然失败；破解=手工复刻 dist 9083-9107 的绘制体（`_requestAnimationId=0` → 按 dpr 算 `_pixelWidth/_pixelHeight` → 写 canvas 尺寸 → `clearRect` → `scale` → `cn._listener()`），16 张画布全部真上色（`getImageData` 非透明像素计数可增减），此后像素级判读才可用。② **库自己的 click 合成器被定时器钳制打死**：v10 不听原生 `click`，点击由 `_mouseUpHandler` 内部合成（dist 1729-1743），且 `if (_clickTimeoutId !== null && ++_clickCount > 1)` 走**双击分支**把这次点击吞掉；`_clickTimeoutId` 来自 `setTimeout(_resetClickTimeout, Delay.ResetClick=500)`（1821-1826），而 hidden 页 Chrome 把定时器钳到 ≥1s ⇒ 500ms 复位永不发生 ⇒ **多点工具第二击永远落不下**（现象：趋势线卡在 `currentStep=2` 只有 1 个点）。破解=patch `window.setTimeout` 捕获那个 500ms 的 bound 回调（生产包已压缩，`cb.name === "bound "`）并在每击前后手动调用，等价于「用户两次点击之间过了 500ms」。③ **React 18 提交要真定时器**：MessageChannel 让出不足以让 `setState` 落地（调度器在 hidden 页用被钳制的 timeout），一度误判「清单里点选中无效」，改 `await setTimeout(300)` 后 `ring-primary` 与提示同时出现 —— 与 ⑯ 的「一次 evaluate 一个动作」是同一条纪律的两个方向。④ 另注：`elementFromPoint` 在**视口外**返回 null，本环境视口仅 456×407 且图高 360，任何合成事件前必须 `scrollIntoView` 并**重读** rect（工具栏提示文案变长会把图整体下移 ~44px）。
  - 变异验证（**11 段单注入**，全部还原，red 数 1-3）：M1 序列化丢 `lock` → 3 红；M2 序列化丢 `hidden` → 3 红；M3a/M3b 恢复丢 `lock`/`hidden` → 各 2 红；**M4 相信 `overrideOverlay` 返回值 → 3 红**（锁死「读回实例校验」）；M5 去掉 `applyDrawingFlags` 的 id 守卫 → 精确 1 红（`expected "vi.fn()" to not be called at all, but actually been called 1 times`）；M6 `hidden` 错映射成 `visible=hidden` → 3 红；M7 清单纳入半成品 → 2 红；M8 清单藏掉隐藏行 → 3 红；M9 行点击无法取消选中 → 1 红；M10 行不过滤 `excludeId` → 2 红。两轮探针自身故障与修复：变异脚本**必须先 `Set-Location frontend`**（否则 vitest 在仓库根跑、jsdom setup 缺失 ⇒ 11 段全显示「38 failed」的**假红**，且 `Set-Content` 因文件占用抛 IOException ⇒ 实际一个变异都没注入）；PowerShell 字符串里 `"\n"` **不是换行**（要 `` `n ``），且**仓库源文件是 CRLF**（needle 用 `` `r`n `` 才命中，M5 因此前两轮 hits=0）；vitest 输出带 ANSI，解析汇总行先 `-replace "\x1b\[[0-9;]*m",""`；`.NET ReadAllText` 的相对路径按**进程 CWD** 解析、不看 PowerShell 位置；`Select-String` 没有 `-cmatch` 参数（用 `-CaseSensitive`）。
  - 全量：`vitest run` **78 文件 903 用例**全绿（+18：`chartDrawings.test.ts` 28→38、`ProChartDrawings.test.tsx` 27→35；两处假 chart 同步升级为**镜像 lock/visible 语义**：`overrideOverlay` 只在 visible/points/styles 变化时返回 true，纯 lock 返回 false）；`tsc --noEmit` 零错；`vite build` EXIT=0（2.46s）。
- 本地自定义⑱说明：**画线带走与接过来**（⑰ 汇报里列的下一增量，与 ⑪ 的脚本导入导出对称）。能力：工具栏「导出」→ 一份 `.json`（含样式与锁定/隐藏）、「链接」→ 一条 `?d=<g|j><base64url>` 深链（gzip 压过，实测 6 条线 439 字符）、「导入」→ 选文件**合并**进当前标的与周期；对方打开链接即自动导入。容器与 codec 全部复用 ⑪，不新增服务端、不新增依赖。
  - **三条硬规则（都是 ⑭/⑮ 踩过的坑，导入端必须重申一遍）**：① 落点必须带数值 `timestamp`，裸 `dataIndex` 的点整条丢弃（前插历史后会指向别的 bar）；② 文件里的 `paneId` **一律改回 `candle_pane`**——副图 paneId 的线价位落在成交量轴上，正是 ⑭ 修掉的「线真实存在但看不见」，别人发来的文件不能把这个 bug 带回来；③ 条目**逐键重建**（`readDrawing` 只认 name/points/style/lock/hidden），因此 `__proto__`、多余键、`note` 之类既污染不了对象原型也进不了存储。工具名过 `DRAW_TOOLS` 白名单（`text`/`note`一类不可复现的一律跳过并说明原因）。
  - 新增 `frontend/src/lib/drawingExchange.ts`：`DRAWING_BUNDLE_KIND/VERSION`、`DRAWING_SHARE_QUERY_KEY="d"`（与脚本的 `"s"` 互不串味，实测脚本链接喂给画线读取器得到「链接里没有画线分享码」）、`importDrawingsJson`（**信封 / 裸数组 / 单对象**三种形状都收）、`DrawingsImport{ok,drawings,skipped,from}`——坏条目不是错误而是 `skipped`（带「第 N 条：原因」编号，UI 放到提示的 `title` 里），`mergeDrawings` 按 `drawingKey`（`name|timestamp:value,…`，**不含样式** ⇒ 改过色的线再导一次不会翻倍）去重并返回 `fresh`（只有新面孔需要立刻上画布），`createDrawingsShareLink`/`readDrawingsShareLink`、`drawingsFileName`（`vt-drawings-<标的>-<周期>-<日期>.json`，非法字符 slug 化）、超 `MAX_DRAWINGS_PER_BUCKET`（200）时**丢最早的并说明**。
  - 顺手把 ⑪ 的分享码层抽成两边共用：`ShareCodeError{stage:"prefix"|"base64"|"inflate"|"json"}` + `shareCodeMessage()`（**分段报错**——本轮重构一度把「分享码不是合法的 base64」退化成「解分享码失败：Invalid character」，被 `scriptExchange.test.ts` 当场抓住，说明这层文案必须共享而不是各写一份）；`extractPayload(input,key)` 补一条残缺裸码兜底：聊天工具截断后的码仍带 codec 首字母 ⇒ 交给解码器报出**坏在哪一步**，而不是谎报「链接里没有分享码」（prod 实测从 `画线分享链接无效：链接里没有画线分享码` 变成 `…：分享码不是合法的 base64`）。
  - `ProChart.tsx` 接线：导出/链接两按钮 `disabled={drawCount===0}`、**导入始终可用**（空图时正是它最有用）；`drawNotice{text,bad?,detail?}` 独立提示槽（优先级高于「已 arm / 已选中」，红字 `text-red-500`、明细挂 `title`），并在 `syncDrawings`/`armTool`/`applySymbol`/`pickInterval` 四个动作处让位清空；`?d=` 与 ⑪ 的 `?s=` 同构：模块级捕获 + `takeDrawLinkTask()`（StrictMode 双挂载只解一次）+ 读完 `stripQueryKey` 清地址栏；`ingestDrawings` 命中当前桶才 `restoreDrawings(chart, merged.fresh, drawingEvents())`（事件齐 ⇒ 导入的线一样可拖可删可改色，⑮），否则只落盘等 loader 换绑时读取。
  - **不重复实现浏览器胶水**：`downloadText`/`copyText` 直接复用 `components/charts/workbench/types.ts` 里 ⑪ 已有的两个函数。它们**失败时返回 false 而不抛**，所以 UI 必须兜底——剪贴板不可用时摊出一个只读链接框让用户手动选中（内嵌浏览器实测就走的是这条：`剪贴板用不了，请手动选中下方链接复制`），下载被拒时提示改用链接。
  - prod 实测证据（**真实构建 :8000**，600519.SH / 1D）：① **导出**——patch `URL.createObjectURL` 抓到 Blob，文本为 `{kind:"vibe-trading.drawings",version:1,from:{symbol,interval,exportedAt,count},drawings:[…]}`，字段与 ⑭ 的桶格式一一对应；② **链接**——`?d=g…` 共 439 字符（gzip 前缀生效），剪贴板失败 → 只读框出现；③ **清空桶后带 `?d=` 重载** ⇒ 地址栏被清洗（`location.search === ""`）、6 条线全部回来且 `清单 · 6`（数字读自 `chart.getOverlays()`，即线真的在图上）、提示「导入 6 条画线到 600519.SH|1D」，无关桶 `KEEP|1D` 一字未动（**分桶隔离**）；④ **导入含脏条目的文件**（信封，`from` 指向另一个标的）⇒ 提示「导入 3 条画线到 600519.SH|1D；重复 1 条未加；跳过 2 条；文件来自 000001.SZ|1D」，`title` 里是「第 4 条：text 不是可复现的画线工具；第 5 条：没有落在某根 K 线上的落点」，其中 `paneId:"pane_VOLUME_MOCK"` 那条落盘后是 `candle_pane`；⑤ **裸数组文件** ⇒ 2 条并入（共 6），`dashed:true` 与 `hidden:true` 原样进桶，清单两行分别带「已锁定」「已隐藏」标记、锁定按钮 title 随状态在「锁定后拖不动这条线（右键仍可删）」/「解锁后可拖动改位」之间切换；⑥ **幂等**——同一条链接第二次打开 ⇒ 提示「6 条画线都已经在 600519.SH|1D 上了」、桶长度不变、`清单 · 6`；⑦ 坏码链接红字、非 JSON 文件红字带文件名（`junk.json：不是合法的 JSON：Unexpected token '<'…`）、`[]` 红字「没有可导入的画线」且**不清空**已有画线；⑧ 收尾把存储复原成测试前的 1 条线，全程 console **0 error 0 warn**。
  - 变异验证（**13 段单注入**，`drawingExchange.test.ts` + `scriptExchange.test.ts` + `ProChartDrawings.test.tsx`，**全部 CAUGHT**）：M1 去工具名白名单 / M2 照抄文件里的副图 paneId / M3 不拦空落点 / M4 去重签名把颜色算进去 / M5 超限时留最早的而不是最新的 / M6 画线链接借用脚本的 `s` 键 / M7 `fresh` 把已有线也算成新加的 / M8 隐藏旗标不导出 / M9 跳过原因不编号 / M10 丢 base64 错误分类 / M11 导入后不刷新屏幕上的图 / M12 存档只写 `fresh`（等于覆盖）/ M13 新动作不清提示。其中 M12 与 M6 是**推演时发现原覆盖不够**才补的断言（「已有画线时导入是合并不是覆盖」、「脚本链接 ≠ 画线链接」要断真实互读），M3/M6/M7/M8 三段同时打红组件层，说明这些规则在 UI 链路上也有守卫。
  - 全量：`vitest run` **79 文件 945 用例**全绿（+1 文件 +42 用例：新建 `drawingExchange.test.ts` 30 条、`ProChartDrawings.test.tsx` 35→47）；`tsc --noEmit` 零错；`vite build` EXIT=0（`ProChart-CoU1BnA3.js` 416.65 kB / gzip 118.98 kB，入页 `index-DmXUev-_.js`；prod 取证是在这个包上重跑的）。`chartDrawings.ts` 只改一处：把 `MAX_PER_BUCKET` 导出为 `MAX_DRAWINGS_PER_BUCKET`，让导入端与存储端共用同一个上限（两边各写一个数字迟早会不一致）。

- 本地自定义⑲说明：**副图也能画线**（⑭「画线功能无法使用」主线的第六个增量）。现象：在 MACD 副图上画线当时是成功的，**刷新之后线跑到主图底部**（看不见、删不掉），而旧版存储里还沉着永远回不来的线。能力：主图与任一副图都是画线目的地，线**记得自己住在哪个面板**（归属随线存进画线桶，⑯⑰⑱ 的样式/旗标/导出链路一并带上），副图关掉时它的线**暂存**在清单尾部、重开指标自动回到**同一个面板**，且清单仍严格等于图上的线数（⑰ 的「数字不说谎」契约）。
  - **库源码实证（`klinecharts/dist/index.esm.js`）——为什么刷新会把线搬走**：
    - `createIndicator` 不传 paneId 时走 `indicator.paneId ?? createId('indicator_pane_')`（dist **15271**，`createId` 是 `Date.now()` 拼计数，dist 450-460）⇒ 副图地址**每次挂载都在变**，昨天存的那个 id 今天必然不存在。
    - `createOverlay` 遇到不存在的面板 id 不会报错，而是**静默改写成 `candle_pane` 并保留原 value**（dist **15364-15367**）⇒ 一个 -1.10 的 MACD 量级落点被画到价格轴外的看不见的地方，**正是 ⑭ 的隐形线 bug 从存储里回流**。
    - 但**副图画线本身不需要任何新功能**：首击时 `overlay.isStart()` 成立⇒`updateProgressOverlayInfo(paneId, true)`（dist **8508-8510** / 14421-14429）会同时改写 `_progressOverlayInfo.paneId` 与实例 `paneId`，即库已经会把线归到接住第一击的面板。于是本轮范围从「做一个面板选择器」收敛为「**给面板一个能复发的地址** + **上图前先问面板在不在**」，UI 零新增控件。
  - 方案 A（地址）`paneLayout.ts`：`SUB_PANE_PREFIX="sub:"` + `subPaneIdOf(name)`（非 `[A-Za-z0-9_-]` 字符拍平成 `_`、去首尾 `_`、空串→`"pane"`，所以 `subPaneIdOf("中文")==="sub:pane"）、`isSubPaneId`（`"sub:"` 单独为 false）、`subPaneNameOf`。三个接入点：`ProChart.tsx` 启动指标 `createIndicator({name:"VOL",paneId:subPaneIdOf("VOL")})`、`indicatorLang.applyUserIndicator` 的**两条引擎路径**（向量 + Pine）副图均改传 `subPaneIdOf(name)` ⇒ 自编公式面板也有稳定地址（`sub:UCI_<id>`，prod 实测 `sub:UCI_imto6hosj5wi`）。不用库的 `overrideOverlay`/`setPaneOptions` 去追赶随机 id：只有创建时传对才能避开重排抖动。
  - 方案 B（不丢线）`chartDrawings.ts`：谓词 `isRestorablePaneId(id) = id===MAIN_PANE_ID || isSubPaneId(id)`（只认自己发过的地址）、`paneIndicator(id)` 反查指标名、`PaneLookup` + `ALL_PANES_PRESENT`（默认全在，保持旧签名兼容）。`restoreDrawings(chart, drawings, events?, paneExists = ALL_PANES_PRESENT)` 现在返回 `RestoreReport{applied,parked}`：**面板不存在（或 `createOverlay` 返回假值如 `""`，dist 15361）一律 parked，绝不交给库去猜**。存在性探针是 `chart.getPaneOptions(id) != null`（dist 15587-15594），探针报错时兜底「只有主图安全」。
  - 方案 C（不丢线的第二半）`ProChart.tsx`：parked 不在图上，所以**每一次写桶都必须带上它**——`bankDrawings = saveDrawings(s,i,[...parkedRef.current, ...serializeDrawings(...)])`，parked 在前是因为 `saveDrawings` 溢限时留尾部（没人看得见的线先牺牲）；`flushParked` 挂在 `[indCount, layoutTick]` effect 里（面板集变化唯一的入口），重跑 `restoreDrawings`而不是直接 `createOverlay`，这样同一 tick 内开了又关的面板会立刻 park 回去；`discardParked` 是唯一能清理永不复活的旧随机 id 的出口（否则那类线只能永远挂在存储里）。清单行新增 chip（`MACD`/`RSI 相对强弱`/…）与 parked 页脚「N 条在等副图 · … — 开启对应指标即回到图上」+ 删除按钮；提示槽同一信息；`describeDrawing` 里副图行的量纲改成「值」（主图仍叫「价位」——把 -0.42 叫价位就是那条隐形线的报告版同一诖）。
  - ⑱ 的导入规则随之升级（不能再一句「一律改写回主图」）：`readDrawing` 只保留 `isRestorablePaneId` 命中的 paneId（其余仍改回 `candle_pane`，**别人的随机 id 依旧不能回流**），且去重签名 `drawingKey` **把 paneId 算进去**（同一组数字在主图与在 MACD 上是两条不同的线）。
  - **prod 现场抓到的第二个缺陷（本轮唯一补的产品 bug）**：工作台「取消勾选」关掉指标时面板消失，但库**不会删掉住在它上面的 overlay**（没有面板可以卸载它们）⇒ 线仍在 `getOverlays()` 里、隐形、仍被计数，`清单 · 3` 但屏幕上只有 2 条——⑰ 的契约当场破了（单测里没发现，因为既有副图用例都走 `reload()`，而 reload 会进 `restoreDrawings` 仍被 park）。修法：新增 `parkOrphans(chart)`，与 `restoreDrawings` 共用 `paneLookup`，在同一 effect 里**先 park 孤儿再 flush**：把 `isSubPaneId(paneId) && !paneExists(paneId)` 的已完成线移出画布并入 parked（半成品只移除、不保存，并且不过 mute 以便库的 `onRemoved` 抹掉 tool 高亮）；移除时 mute 画线写盘、收尾统一 `syncDrawings`，避免中间态重复落盘。M15（不 park）/M16（park 但不移出）各精确红 1 条。
  - prod 实测证据（**真实构建 :8000**，600519.SH / 1D，500 根 bar；隐藏页 harness 同 ⑰）：新包 `ProChart-NFBDCy18.js` 下 `getPaneOptions()` = `candle_pane / sub:VOL / sub:MACD / x_axis_pane`（旧包是 `indicator_pane_1788…`），副图高 74/74与 `planPaneHeights` 一致；**真实合成点击** MACD 面板中央（先手工跑绘制体注册图元事件）→ 新建 overlay `paneId=sub:MACD`、`value=-1.1032`（MACD 量级），落盘同值；刷新后**仍在 sub:MACD**（旧行为会被改写到主图），清单行「价格线 + chip MACD + `值 -1.1`」，主图行仍「价位 1264.07」；脚本库→「RSI 相对强弱（副图）」保存并应用 → 新面板 `sub:UCI_imto6hosj5wi`，在其上再点击→存 `value=52`（RSI 0-100 量级）；**取消勾选 RSI（不刷新）**→ paneGone、`getOverlays()` 只剩 `candle_pane`+`sub:MACD`、`清单 · 2+2`、提示「另 2 条在等 已关闭的副图、**RSI 相对强弱**」（parked 名称从 `loadUserIndicators()` 反查，不是“某个副图”）、**桶里 4 条一条不少**；重新勾选→ `sub:UCI_imto6hosj5wi` 上的线回到原面板且仍是 1 条（`清单 · 3+1`）；**链接**导出的 `?d=` 现场用 `DecompressionStream('gzip')` 解码得 `{v:1,d:[…],f:"600519.SH|1D"}` 共 3 条：parked 的旧随机 id 被合法化为 `candle_pane`（⑱ 规则）**但仍随链接带走**（不丢数据），`sub:MACD` 原样保留；全程 console **0 error 0 warn**，收尾把存储复原为初始的 1 条主图线（顺便用新按钮清掉了那条永远回不来的 `indicator_pane_1788596294379_2` 僵尸）、`pro-chart.userIndicators.v2` 与探针备份键已删。
  - 变异验证（**16 段单注入**，`paneLayout/chartDrawings/drawingExchange/indicatorLang/pineIndicatorWire` + 组件层，**16/16 killed**；基线自检先报 `baseline green: 206 tests across 6 files`，红数以该次为权威，分 3 批跑完）：M1 面板地址掺 `Date.now()` → 7 红；M2 空前缀名不兜底 → 1 红；M3 `isRestorablePaneId` 恒真 → 3 红；M4 `restoreDrawings` 不看 `paneExists` → 4 红；M5 `createOverlay` 返回假值也算上图 → 1 红；M6 副图也叫「价位」 → 2 红；M7 `DrawingRow.paneId` 恒主图 → 2 红；M8 导入仍强制主图 → 1 红；M9 `drawingKey` 去掉 paneId → 1 红；M10 写桶丢 parked → 1 红；M11 启动 VOL 不给地址 → 3 红；M12 不 `flushParked` → 1 红；M13 用户公式丢 paneId（两条引擎一起改）→ 3 红；M14 `discardParked` 变空操作 → 1 红；M15/M16（parkOrphans 两条腿）→ 各 1 红。**探针本身两个坑（都不是产品问题）**：① `spawnSync("npx.cmd", …, {shell:true})` 跑 vitest 会先崩在 jsdom 环境（`Cannot read properties of undefined (reading 'config')`）⇒ 报告里 `numTotalTests:0`、`assertionResults:[]`，而收集失败本身被当作「红」⇒ **14 段探针全打印 `killed failing=0/0`**（一行断言都没跑却全绿，比「全存活」更坏的一种假结果）；② 改成 `spawnSync(process.execPath,["node_modules/vitest/vitest.mjs",…])` 后仍全红，真因是脚本里的项目路径写了**小写盘符 `e:/`**，而 Node/vitest 解析出 `E:/` ⇒ `@vitest/runner` 被加载两份，`describe()` 直接抛错。两条都靠「**基线自检**」兜住：先跑一轮未变异的钉住文件，`total===0` 或有红即 `BASELINE-FAIL` 退出（+ `collection-error` 判定），否则一个断言都没跑却会汇报「全部 killed」。
  - 全量：`vitest run` **79 文件 961 用例**全绿（+16：`paneLayout.test.ts` 11→15、`chartDrawings.test.ts` 38→44、`drawingExchange.test.ts` 30→31、`ProChartDrawings.test.tsx` 47→52（含 parkOrphans 一条）；`indicatorLang.test.ts`/`pineIndicatorWire.test.ts` 各 1 条断言升级、条数不变）；`tsc --noEmit` 零错；`vite build` EXIT=0（`ProChart-NFBDCy18.js` 420.14 kB / gzip 120.29 kB）。组件层的假 chart 同步升级了两处语义（否则⑲的面板集合永远是空，预算类断言在自证）：`createIndicator` 传入未知 paneId 就按该 id 建面、已知则 stacking；`getPaneOptions` 无参列全部、带 id 返回该面板或 null。

- 本地自定义⑳说明：**画线不能锚在「未来」**（⑭「画线功能无法使用」主线的第七个增量）。现象（用户截图指向 600519.SH 日线右上角空白）：线可以停在最后一根 K 线**右侧的空白里**，价签上写着一个数据里根本不存在的日期，下一根 bar 进来还会往左挪。勾选定位：「画线锚在未来（最可能是这个）」。
  - **库源码实证（`klinecharts/dist/index.esm.js`）——为什么点得中**：`StoreImp.timestampToDataIndex` 对越界时间戳是**外推而不是拒绝**（dist **13829-13843**：`if (timestamp > lastTimestamp) { referenceTimestamp = lastTimestamp; baseDataIndex = lastIndex; }`），而绘制 x 正是 `dataIndexToCoordinate(timestampToDataIndex(ts))`（`OverlayView._drawOverlay` dist **8905-8936**）⇒ 右侧空白「可点」且被照单全收。prod 现场双重实证：500 根 bar（末根 2026-09-04）时 `getVisibleRange()={from:492,to:500,realFrom:497,realTo:506}`（未来区宽 6 个 dataIndex），`store.dataIndexToTimestamp(502)=2026-09-07`（一个数据里从未有过的“交易日”）。
  - 修法（纯函数层 `chartDrawings.ts` 新增 4 个导出，UI 只做接线）：`lastBarTimestamp(dataList)` 从尾往前取第一个**有限数** `timestamp`，**没数据返回 undefined ⇒ 调用方一律不动别人的落点**（不拿陈旧列表猜边界）；`clampPointsToLastBar(points, lastTs)` 把 `ts > lastTs` 的落点**只改 x**（`timestamp`），**`value` 一个都不动**（用户点的价格永远是用户自己的）；`clampDrawingsToLastBar(drawings, lastTs)` 是桶/导入列表的批量版（汇总 `moved`/`dropped`）；`reanchorOverlay(chart, overlay, lastTs)` 管图上单条，`isInProgress` 的半成品**跳过**（它还有下一次点击，此刻吸它会打断手势）。退化判定 `next.length > 1 && timed === next.length && stamps.size === 1`：吸回来全部重合成一个点 ⇒ 零宽度、无容身之处 ⇒ **删除并说明**。`timed === next.length` 这个前置条件是本轮踩出来的——混着只带 `dataIndex` 的点时那些点自己有位置，判退化就是误杀；单点工具（价格线/水平线）因此永不误判。
  - **原地移线可行，不必删了重建**（三条 dist 证据，顺带纠正 ⑰ 笔记里「points 被 override 排除」的误读）：`OverlayImp.override` 有 points 专属分支（dist **8277-8307**，传 points 即 `this.points=[...points]` 并把 `currentStep` 置 FINISHED）、`shouldUpdate()` 比较 points JSON（**8314-8318**）⇒ 一定重绘、`StoreImp.overrideOverlay` 在 sort||draw 时 `updatePane` 并刷新 X_AXIS（**14430-14458**）；而 `getOverlaysByFilter` 只按 id/groupId/paneId/name 过滤（**14285-14298**）⇒ `chart.overrideOverlay({ id, points })` 之后 **id 不变**，⑯ 的选中态与 ⑰ 的清单行不会因为修线而蒸发。
  - **四个入口过同一把尺**：① 绘制完成 `onDrawEnd` ② 拖动松手 `onPressedMoveEnd`（拖整条与拖手柄都收在这一个事件，dist 8605-8606 的载荷都带 `{chart, overlay}`）③ 载入：loader 里 `clampDrawingsToLastBar` 后**先回写存储再 `restoreDrawings`**（旧数据自愈，且回写只在这一次发生）④ 导入/分享链接：`ingestDrawings` 里 clamp 必须在 `mergeDrawings` **之前**（别人的脏落点不进本桶）。事件契约为此扩两个钩子：`DrawingEvents.onAnchor`（改图上几何，**必须先于 `onChanged`**，否则落盘的是用户瞄向空白的那次点击）与 `onSettled`（报本次提示，**必须后于 `onChanged`**，因为 `syncDrawings` 无条件 `setDrawNotice(null)` —— ⑱ 的契约）。新顺序：`onDrawEnd → onAnchor → onChanged → onSettled`。
  - prod 实测证据（**真实构建 :8000**，包 `ProChart-DMe4TP96.js`，0700.HK / 1D、500 根、末根 2026-09-04；内嵌 hidden 页经 React fiber 走 hooks 拿到真实 `ChartImp`，模拟**严格按 dist 8502-8520 库自身的完成顺序**：`stepDrawingModeEventMoveForDrawing → nextStep → progressOverlayComplete → onDrawEnd`；拖拽按 8737/8638/8605 顺序 `startPressedMove → eventPressedPointMove|eventPressedOtherMove → onPressedMoveEnd`）：**A** 价格线点在 idx502 ⇒ 几何与存储 `timestamp` 双双变回 2026-09-04、`value 450` 保留、**id 不变**、提示「落点在最新 K 线右侧的空白里，已吸回最后一根 K 线」（非红）；**B** 趋势线两点全在未来（09-07 / 09-10）⇒ 判退化，`geomAfter=null`、**从未进桶**、红字「这条线的落点全在最新 K 线右侧的空白里，吸回来会重合成一个点，已放弃」；**C** 趋势线一点真实（08-10）一点未来（09-10）⇒ 只修越界那个，线存活；**D** 拖手柄把价格线拖到 idx503 ⇒ 松手即吸回，`value 450→455` 照收；**E** 整条线段右移 6 格（库把两个落点都外推到 09-16 / **10-05** 这种周末日期）⇒ 全越界 ⇒ 放弃 + 红字 + 不进桶；**F** 整条右移 10 格（前点 04-28 真实、后点 09-14 越界）⇒ 吸回后点，提示非红；**G** 全部落盘干净后刷新 ⇒ 图上 2 条 = 桶里 2 条 = `清单 · 2`，`everyAnchorReal: true`，且**没有任何修复提示**（不误报、不唠叨）。旧数据自愈也已实证：把 4 条含未来落点的线灌进桶后刷新 ⇒「4 处画线落点在最新 K 线右侧的空白里，已吸回最后一根；1 条吸回后重合成了一个点，已删除」，图上只剩 2 条与存储逐条一致，而**未被加载的另一个桶保持原样**（按桶修，设计如此）。收尾把 localStorage 复原为用户原状（`600519.SH|1D` 一条 priceLine）。
  - 变异验证（**18 段单注入**，`chartDrawings.ts` 13 段 + `ProChart.tsx` 5 段，基线自检先报 `baseline green (0/115 failing)`，**18/18 killed、零 HARNESS-BLIND、零 SURVIVED**）：M1 把「过去」的落点也夹 / M2 边界差一号（`<=`→`<`）/ M3 干脆不夹 / M4 退化阈值写成 `size===2` / M5 去掉「每个点都带 timestamp」前置（误杀只带 dataIndex 的点）/ M6 单点工具也判退化 / M7 拿第一根当最后一根 / M8 接受 NaN 时间戳 / M9 桶里保留退化线 / M10 `moved` 覆盖而非累加 / M11 动到半成品 / M12 图上不删 / M13 要两个落点越界才修 / M14 拖拽不修（去 `onAnchor`）/ M15 载入不回写存储 / M16 导入不夹 / M17 不报本次提示（去 `onSettled`）/ M18 没数据也猜。红数依次 23/2/13/15/1/44/23/1/2/1/1/2/4/4/2/1/3/1 —— 每条规则至少有一段专属探针，不是「顺手红了一片」。
  - 全量：`vitest run` **79 文件 980 用例**全绿（+19：`chartDrawings.test.ts` 44→56、`ProChartDrawings.test.tsx` 52→59）；`tsc --noEmit` 零错；`vite build` EXIT=0（`ProChart-DMe4TP96.js`）。组件假件同步升级一处语义（否则 ⑳ 在测试里自证）：`overrideOverlay` 必须像 dist 8292-8306 那样**接受并落地 points**，且 `draw` 判定要把 points 变化算进去。
  - **本轮两条流程教训（都不是产品问题，但都会造出假结论）**：① **变异探针与 `vite build` 并发** —— 第一次构建读到了正在变异的源码，产物 `ProChart-C87G_PcW.js` 是脏的，探针跑完干净重建才拿到 `DMe4TP96`；探针改源码期间**禁止构建**，构建必须排在探针之后。② **prod 交互模拟不忠实** —— 裸调 `o.onDrawEnd` 而未先 `chart.getChartStore().progressOverlayComplete()`，那条 overlay 就一直挂在 `_progressOverlayInfo` 上，而 `getOverlays()` 会把它**追加**进结果（dist 14308-14311）⇒ 出现「删掉的线仍被报出、还把未来区落点写进了存储」的假缺陷；测的是脚本，不是产品。

- 本地自定义㉑说明：**报警 + IM 推送**（用户需求原话是「盯住某个条件，命中就推到群里」）。能力：4 类规则（`market` 标的自身 K 线/指标、`position` 已连接组合的持仓、`account` 组合层数字、`event` 外部入站事件）× 8 个条件算子（`nonEmpty`/`truthy`/`gt`/`lt`/`crossUp`/`crossDown`/`rising`/`falling`，与 `frontend/src/lib/screener.ts` **同一套名字和语义**，避免筛选器和报警内核对 crossUp 各解一遍）× `inactive→pending→firing→resolved` 状态机（`for_bars` 去抖、`realert`/`exponential_realert` 冷却、severity 抑制、`send_resolved` 恢复通知）× 走现有 channel 运行时推送，另配 TradingView / Pine alert 的 **webhook 入站桥**，入口是独立 `/alerts` 页（与 `/scheduled` 同构）。语义对标 Prometheus Alertmanager（pending/firing/resolved + inhibit + 冷却）与 ElastAlert2 / Lean，**只借语义不拷代码**（三者均 Apache-2.0）。
  - **上游源码实证（本项目已有的三块地基，决定「零新增依赖 / 零新增环境变量」能成立）**：
    - 发送不自己实现：`src/channels/base.BaseChannel.send_with_receipt` 已经是「消息 → 回执」的既有契约，`src/channels/targets.py` 的 opaque ref 注册表也已存在 ⇒ `delivery.resolve_addresses` 只做 ref→地址的解析，`GET /alerts/targets` 直接读它（`{targets, channels}`，channels 取自 manager 的实时 keys），**报警层从不 import 任何渠道 SDK**。
    - 落盘不自创机制：`src/scheduled_research/store.py` 的「临时文件 + fsync + `os.replace` + fsync 父目录」以及「文件存在但解析失败 ⇒ 隔离改名并抛 `CorruptAlertStoreError`，**绝不返回空列表**」被 `src/alerts/store.py` 整块沿用（空列表和「用户没有规则」长得一样，poller 会就地不再盯任何规则）。规则与事件历史分两份文档（`alert_rules.json` / `alert_incidents.json`，每规则留 200 行）⇒ 通知突发碰不到规则定义，删规则也不会抹掉它已经说过的话。
    - 开关不新造：`_alerts_enabled()` 读的就是既有的 `vibe_trading_enable_scheduler`（`VIBE_TRADING_ENABLE_SCHEDULER`，**默认 False**）。再立一个报警专用开关等于给运维两个会互相矛盾的东西。
  - 新增文件（全是新文件，上游只动 3 个接线点）：后端 `agent/src/alerts/{__init__,models,conditions,engine,store,delivery,inbox,sessions,service,poller}.py` + `agent/src/api/alerts_routes.py`（13 个端点：rules 的 CRUD / `enabled` / `reset` / `dry-run` / `test-send`、`incidents`、`targets`、`run`、`webhook/{rule_id}`）+ `agent/tests/test_alerts_*.py` 10 个文件；前端 `frontend/src/lib/alertsApi.ts` + `frontend/src/pages/Alerts.tsx` + 两个测试文件。数据落 `~/.vibe-trading/alerts/`（运行时目录，不在仓库内）。poller 是单 asyncio 任务：tick 60s（只是延迟上限，哪条规则真的到期由规则自己的 `poll_interval_ms`（默认 300s）决定）、欠账通知每 5min 重扫（`MAX_DELIVERY_ATTEMPTS=6` 后放弃）、单趟异常只记日志不结束循环、保存规则会 `wake()` 立刻跑一趟。
  - **接线的全部足迹**：`api_server.py` 净 **+7 行**（顶部 1 行 import、启动 1 行 `_start_alert_poller()`、关停 1 行 `await _stop_alert_poller()` **排在 channel/scheduled 之前**（否则报警循环可以在别人之后才停，等于关停顺序反了还能排队发消息）、底部 3 行注册块 + 1 空行 + 一处 docstring 改写）；`router.tsx` 2 行；`Layout.tsx` 2 行。**不新增环境变量、不新增依赖、不改 8 个语言包**（页面文案一律 `t(key as never, { defaultValue })`）。
  - **webhook 入站的三处安全决定**：① 该路由**故意不挂 `require_auth`**（TradingView 的告警分发器不会带 bearer），共享密钥走 `?key=` / body 的 `key|secret|token` / `x-alert-key` 三处任一，比对用 `secret_matches`（**常数时间**，库里只存 sha256）；② 「规则不存在」与「规则不是 event 类」**合并成同一个 404** ⇒ 这条公开路由不能变成「哪些 id 存在、各是什么类型」的探针；③ 已暂停的规则回 409（「事件投回来了但没人看」和「没有这个地址」是两个事实，不能都回 200）。
  - 本轮**主动修掉的四个「话与事实不一致」**（同一类错，与下面第 1 条同源）：
    1. `test_alerts_delivery.py::test_one_dead_address_does_not_block_a_live_one` 原本是**单地址**规则——一个「首个失败就放弃」的实现与一个「继续往下发」的实现测出来一模一样，测试名承诺的「不阻塞」根本没被钉住。改成一条规则点名**两个**地址、并且故意把死的那个排在前面，让「停在第一个失败」必红。
    2. `Alerts.tsx` 里 20 处插值全写成 i18next 的**单花括号**（`"Saved {rule}"`），会原样渲染成 `Saved {rule}`。修完在页面用例里加一道总闸：`expect(document.body.textContent).not.toContain("{{")`。
    3. `kind` 切到 `event` 时 `sendResolved` 仍是默认的 `true`，后端 `validate` 判 `event_no_resolution` 拒收，而那个 checkbox 对 event 是 `disabled` ⇒ **用户无解的死锁表单**。改为 `update()` 里 event 强制 `sendResolved = false`。
    4. test-send 无论后端回 `sent` / `failed` / `no_target`（三者都是 HTTP 200）一律提示 “Test push sent”。改成按 `report.status` 分支（`runAction<T>` 的 message 允许是 `(result) => string`），没送达就直说「什么都没收到：{{detail}}」。
  - 实测证据：后端 `pytest tests/test_alerts_*.py` **334 passed**（10 个文件 / 237 个 `def test_`，parametrize 展开 334 条）；前端 `vitest run` **81 文件 1035 用例**全绿（+2 文件 +55：`alertsApi.test.ts` 35 / `Alerts.test.tsx` 20）、`tsc --noEmit` 零错、`vite build` EXIT=0（`dist/assets/Alerts-C66lOp05.js` 30.11 kB / gzip 9.08 kB，独立 chunk）；`tools/ci_grep_gates.sh` 五道门绿（唯一命中 worldquant 门的是 `.qoder/repowiki/**.md`，被 `.git/info/exclude` 本地忽略，不进 CI）。**同步上游 48 个提交后在同一棵新树上重跑，四组数字与产物 chunk 哈希（`C66lOp05`）分毫未变。**
  - **真实服务器端到端**（`:8899` 起 `api_server.py`，非 TestClient）：`GET /` 200、`GET /alerts/rules` 200 `[]`、`GET /alerts/targets` `{targets:[],channels:[]}`、创建 201、`POST /alerts/rules/<id>/dry-run` 走**真实 akshare** 返回 `{bars:120, value:1330.0, market_open:true, hit:false, action:"none", next_state:"inactive"}`、`POST /alerts/run?deliver=false` 返回 `evaluated:1 fired:0` 且**规则状态分毫未动**（`deliver` 默认 false 是刻意的：点「立即评估」应该回答「引擎看到了什么」，不是往群里灌一批消息）、`DELETE` 200 后 `GET` 404；足迹压缩后重跑开机取证：三个 GET 全 200 + CTRL_BREAK **优雅关停**走完 lifespan（`Application shutdown complete` / `Finished server process`，日志零 Traceback）。
  - **已知边界（诚实告知）**：只有服务以 `VIBE_TRADING_ENABLE_SCHEDULER=true` 启动时规则才会**自动**测量（默认 False），页面提示直接点名这个变量并给出「Evaluate now」退路；`position`/`account` 类规则依赖已连接的组合账本，没连上时评估结果是带 `last_error` 的「测不到」而不是假 0；webhook 入站要求 TradingView 能访问到本机端口（默认 `127.0.0.1:8000` 需要用户自己加转发）；数据序列是固定词表（`close/open/high/low/volume`、`change_pct`、`sma:N`/`ema:N`/`rsi:N`、`macd_*`、`bb_*`、`pnl_pct`/`position_value`/`quantity`/`cost_price`/`market_price`、`equity_usd`/`equity_cny`/`drawdown_pct`、`event_*`），**不接用户的 Pine/公式脚本输出**——那是 ⑩⑪ 那条线，两套引擎合并会同时失去可预测性。

- 本地自定义㉒说明：**数据接口优先使用本机 FutuOpenD**（用户需求原话「根据我有的权限，帮我将数据接口改为优先使用 futu openD 稳定传输」+ 权限截图）。项目其实**早就有两块富途代码**（`backtest/loaders/futu.py` 的历史K线 loader、`trading/connectors/futu/` 的下单连接器），但富途在链里排第 4 位、且从不用它的推送/快照能力，A股/美股链里根本没有 futu。
  - **权限决定链（截图取证，不是猜的）**：港股 LV2、美股股票 LV3、A股沪深 LV1、日本 LV2 有；**加密货币无权限**、美股指数无权限、各期货无权限、OTC 无权限。⇒ 只把 futu 前置到 `a_share` / `us_equity` / `hk_equity` 三条链；`crypto` / `forex` / `index` / `futures` / `fund` / `macro` **刻意不进 futu**（`test_registry.py` 有守护用例）。北京交易所 `.BJ` 也排除（实测富途回 `ret=-1`）。
  - **「优先」有三层，只改 registry 完全无效**（这是本轮最大的坑，见第四节第 35 条）：① `registry.FALLBACK_CHAINS` 链头；② `src/market_data.py` 的 `detect_source` —— `fetch_market_data` 用 `candidates = [src, *chain]` 把「静态首选」硬顶到第 1 位，富途只会落在第 2；③ `src/api/market_routes.py::_fetch_daily` 对 `.SH/.SZ` 有一段**在进链之前就 return** 的 Sina 硬短路。
  - 新增文件 2 个：`agent/backtest/loaders/futu_gateway.py`（连接生命周期唯一入口：`probe()` TCP 判活 + 90s 熔断 `in_cooldown/note_failure/note_success` + 快照节流 `MIN_SNAPSHOT_INTERVAL_S=0.5` + 批量 `fetch_snapshots`）、`agent/tests/test_futu_gateway.py`（32 条，全 mock，CI 无需 OpenD）。
  - **稳定性的四个实质动作**（「稳定传输」不是一句形容词）：
    1. **熔断只认传输层故障**：`fetch_snapshots` 只在 `OSError` 时 `note_failure`；富途**拒绝某一批**（坏代码/无权限）与**本地处理异常**（解包错、字段拼错）都**不熔断**——否则一个笔误能把健康网关挤出所有降级链 90 秒，比它掩盖的 bug 破坏性更大。
    2. **`is_available()` 从 SDK 握手改为 TCP 探测**：降级链每个标的都会问一次可用性，原先每次是一个 `OpenQuoteContext` 建连+关闭（约 1s + 后台线程）；实测单标的日线原来要付**两次**握手，现在 `fetch()` 整批复用一个 context、可用性判定不建连。富途对快速建断连是有连接数上限的，这属于故障源本身。
    3. **网关不在时把 futu 从候选里整条剔除**（`market_data._fetch_via_chain`）：`max_fallback_attempts` 只有 **5**，而 A股链有 8 个源；留一个答不上话的源占位，等于让 `akshare`/`tushare` 永久不可达——这是对**没有 OpenD 的用户**的回归，用 `test_dead_gateway_is_dropped_from_the_walk` 钉住。
    4. **快照节流按上一个截止点排程**（`due = max(now, prev_deadline)`），官方限 60 次/30 秒；`_quote_batch` 改成**整批一次快照**——逐标的调快照会让 30 行的自选清单变成 30 次建断连 + 约 15 秒。
  - 盘中实时价：`futu.realtime_quotes(codes)`（按 `_SERVED_PREFIXES` 过滤，不给 BTC-USDT/SHELL.L/^SPX 白跑一趟，结果按原始标的回键）→ `/market/quote` 单标的 `_quote_one` 与批量 `_quote_batch` 都先问富途 tape，拿不到再回落日线（原先报警和自选清单拿的是**上一根日线收盘**，不是盘中价）。时间戳与日线路径严格同一套语义，`update_time` 解析失败**返回 None 而不是造一个**（假时间戳会让图表和规则一起排序错）。
  - **修掉的真实缺陷 3 个**：① `_to_futu_symbol` 不认 `.US`（`AAPL.US` 原样送进富途 ⇒ 美股永远取不到数）；② `PRICE_CALIBER_BY_SOURCE` **没有 futu 条目** ⇒ `price_caliber()` 返回 `unknown` ⇒ 被排除在 `_COMPARABLE_CALIBERS` 之外 ⇒ **混口径告警对富途永不响**（已登记 `split_dividend`）；③ `loader.markets` 缺 `us_equity`。
  - **纠正上一轮我自己给出的一个错判**：我曾断言「loader 没传复权参数 ⇒ 富途给的是不复权 raw 价」。实测 `request_history_kline` 的 `autype` **默认就是 `'qfq'`**，数据一直是对的，缺的只是口径登记（见第 36 条）。
  - 实测证据（活体，OpenD 已登录 11111）：**网关在上** `fetch_market_data(["600519.SH","700.HK","AAPL.US"])` 三个的 `_provenance.source` 全为 `futu`（各 25 行）；`_fetch_daily("600519.SH")` source=`futu:opend`；`_quote_batch` 四标的**一次快照**出三行 `realtime:true src=futu`，BTC 正常回落 OKX；`_preferred_source` 对 `600119.BJ` 仍回 `tencent`。**网关在下**（`note_failure` 模拟）：首选自动落回 `tencent/tencent/yahoo`，A股走 `akshare:sina_stock_zh_a_daily` 且**收盘价 1330.0 与富途一致**，`_quote_batch` 返回 `realtime:None` 的两行正常价，`fetch_market_data` 三标的**全部解析成功、`_unresolved` 为空**、耗时 1.48s，熔断全程保持 ⇒ 一次失败覆盖整批遍历，无逐标的罚时。
  - 回归：`pytest tests`（全量）**12652 passed / 54 failed / 9 errors**，与第四节第 30 条记录的 Windows 基线（54 failed + 9 errors）**数量与集合一致**，失败面与本增量改动面**零交集**（已逐条验因：`WinError 1314` 无符号链接特权、gbk 默认编码缺 `encoding=`、`0o600`/fsync、`reports\\daily.json` 路径分隔符）。
  - **上游冲突面**（全部是改上游文件，需要同步时优先看这几处）：`backtest/loaders/registry.py`（三条链 + 口径表）、`src/market_data.py`（`_preferred_source` / `_futu_gateway_live` / 候选剔除）、`backtest/loaders/futu.py`（`is_available` 语义、`.US` 映射、`realtime_quotes`）、`src/api/market_routes.py`（快照优先 + A股链头）、`tests/conftest.py`（新增「默认无网关」环境钉）、`tests/test_market_data.py`/`test_registry.py`/`test_futu_loader.py`/`test_settings_api.py`/`test_source_order_overrides.py`。**不改 `pyproject.toml`、不改 lock 文件、不改 8 个语言包、不动 `api_server.py`**；`FUTU_HOST/FUTU_PORT/VIBE_TRADING_DATA_CACHE` 只写进 `agent/.env`（已 gitignore），仓库内零配置足迹。
  - **必须告知用户的两个副作用**：① `futu-api` **不在** `pyproject.toml` / `requirements-lock.txt` / `requirements-channels-lock.txt` 任何一处（本轮 `pip install futu-api` 装的是 10.10.7008 + PyCryptodome）⇒ 本机可用，但 **Docker 镜像与 `--require-hashes` 的 CI 拿不到它**，富途会静默降级回公开源（loader 与网关都是延迟 import，不会 ImportError）。② 既有 `MARKET_DATA_ORDER_*` 覆盖值是**旧链的排列**，校验是**多重集相等**（只许重排不许增删）⇒ 旧值现在变成非法排列，registry 会**告警并保留默认链**（不是静默失效），Settings 页的 source-priority 卡片需重新保存一次。用户机上实测：`~/.vibe-trading/.env` 只有 `MARKET_DATA_ORDER_HK_EQUITY`，且其内容恰好等于新默认链 ⇒ 合法、无告警。
  - 富途侧的硬约束（决定了不能指望它当唯一源）：历史K线额度按「**每 7 天 N 个标的**」计（本机显示 0/1000）⇒ 已在 `agent/.env` 打开 `VIBE_TRADING_DATA_CACHE=true` 省额度；快照 60 次/30 秒；A 股权限按 **OpenD 登录 IP** 判境内/国际（IP 在境外时 A 股会整体不可用，本机实测可用）；`get_market_snapshot` 返回 **2 元组**而 `request_history_kline` 返回 **3 元组**。

- 本地自定义㉓说明：**代码输入联想（type-ahead）**（用户需求原话「每次输入代码很麻烦，我想和其他软件一键输入号码或者字母前几位就可以跳出股票代码选择」）。能力：在任何标的输入框里打 `600` / `600519.SH` / `aapl` / `茅台` 就出候选（端到端 32-40 ms），四个接入点（`/pro-chart` 顶部输入框、侧边自选「添加」、`/tv-chart` 输入框、`/alerts` 规则表单的 symbol），空框先显示用户自己的列表，且**代码库里没有的输入一个都不吞**（`BTC-USDT`、`NASDAQ:AAPL` 原样提交）。
  - **为什么必须本地名册（活体实测 2026-09-06，走的就是 `src/tools/symbol_search_tool` 暴露给 LLM 的那条链）**：`茅台` → **0 候选**（唯一会说中文的东财 suggest 在本机住宅 IP 下回非 JSON，a-stock-data 项目 FAQ 对 `search-api-web` 记的是同一现象；Yahoo 直接拒非 ASCII）；`00700` → 一支台湾 ETF，**没有腾讯**（Yahoo 按文本相关度不按代码前缀排）；`600` → iShares Euro.600 系列 + 5000 只里混进 1 只 A 股；**单键 380-2300 ms 且抖动**。type-ahead 的三个硬条件（前缀语义、中文名称、<100 ms）远程 suggest 一个都满足不了 ⇒ 自建索引，而不是给远程搜索包一层。
  - **名册来源**：本机 `OpenQuoteContext.get_stock_basicinfo(market, stock_type)`——它是**上市目录不是行情**，不建订阅、不占用「每 7 天 N 个标的」的历史K线额度（㉒ 那条硬约束的直接受益方）。实测 SH STOCK 2380 / SZ STOCK 2967 / HK STOCK 3777 / US STOCK 13046 加各市场 ETF/IDX，**一次全量重建 18.3 s** 得 **32273 行**：`SH 4400 / SZ 4784 / HK 3472 / BJ 341 / US 19276`，按类型 `equity 21733 / etf 9819 / index 721`，中文名齐（`SH.600519 -> 贵州茅台`、`HK.00700 -> 腾讯控股`、`US.AAPL -> 苹果`）。富途**没有北交所**（与 ㉒ 排除 `.BJ` 同源），BJ 走 `akshare.stock_info_bj_name_code()`：341 只，与 `stock_info_a_code_name()` 里按 `_a_share_suffix` 判为 BJ 的 341 只**互证且零丢弃**；网关整体不在 → 降级为 akshare 的 A 股名册；两边都失败 → 空索引 + UI 退回纯文本（**不是报错**）。落盘 `~/.vibe-trading/symbol_roster.json`（运行时目录，仓库零足迹）+ 进程内 `_MEM`，`DEFAULT_MAX_AGE_S = 7*24*3600`（名册是目录，新股一周最多增一次），且**过期照答 + 后台重扫**（stale-but-usable 好过什么都没有）。
  - **匹配档位 `_score`：exact > 代码前缀 > 名称前缀 > 名称中缀**，三条活体回归各 2 条用例钉住：① `600` 曾把 `00600.HK` 与补零的 `000600.SZ` 顶在上海 600xxx 前面 ⇒ **HK 补零等价只算 CODE_PREFIX 不算 EXACT，且只对 HK 成立**；② **下拉框显示什么就能打回去什么**：exact 原先只比首段裸码，于是真机上 `HPE.PRC` 返回 **0 行**、`BRK.B` 返回两支杠杆 ETF 而不是伯克希尔-B ⇒ exact 同时比 `rsplit(".",1)[0]` 与原 `to_bare_code`（**只留前者的话 `BRK` 会丢掉 `BRK.A.US` 的 exact 命中**，两条必须并存）；③ **平手键 `_tiebreak` 加 `symbol.count(".")`**：`HPE.PRC.US` 与 `HPE.US` 裸码相同、其余键全等 ⇒ 原本退化成缓存字典序，真机把 7.625% 优先股排在慧与科技上面；该键只对 US 生效，对沪深港京是 no-op。单次扫描 32273 行 **23 ms**。
  - **进名册的门槛 `_is_chartable`（四类非标的行，全在活体名册里数过）**：沪深 7 开头 6 位（配股/申购/配售，例 `700057.SH 象屿配股`；不丢的话用户打 `00700` 会被递上一支买不到的东西）；HK **六位** `81xxxx`（750 条，`lot_size` 748/750 为 0、listing_date 常为 1970-01-01、名字一律带 `(临时代码)` ⇒ 尚未开始正常交易的临时柜台，不是股票）；US ISIK 标识（`2578256D.US`）与 when-issued 星号（`BHVN*`）；无市场的裸码（`BTCUSD`）。两个陷阱：`00700.HK` 在网关侧的 `code` 是 **`HK.00700`** ⇒ 宽度判断必须用 `len(code.split(".")[1])`，用 `len(code)` 会把整个 HK 市场判成越界（本轮探针就在此得出过错误结论）；`_is_noise_code` 只在 SH/SZ 里查 leading 7，因为港股 `700.HK` 是腾讯（**不能全局把 7 开头当噪声**）。过滤在**构建与读缓存两处**都做（老版本写的缓存正是这类行会藏身的地方）。
  - **后端足迹：`market_routes.py` 净 +37/-0，`api_server.py` 零改动**（⑧ 的 `register_market_routes` 已把整组 /market 接上 ⇒ 第 31 条的 400 行预算完全不受影响）。`GET /market/symbols`（`q`≤32 / `limit` 1-50 / `refresh`）：**绝不在请求线程里建索引**（冷构建 17-20 s 会把输入框冻住），空索引立即回 `{status:"warming", ready:false, building:true}`，过期则照答并后台 `start_warmup`（单飞 + daemon 线程，`refresh=true` 强制重扫）；`asyncio.to_thread` 包裹每个同步调用，`search(..., load=False)` 是“永远不同步构建”的结构保证。
  - **前端契约（`SymbolCombobox.tsx` 319 行 / `symbolSearch.ts` 162 行，零新增依赖、零新 locale key）**：键盘与 ARIA 形状照抄仓内既有 `ModelPicker`（本前端没有 Radix/cmdk/AntD 可据）；“空框也要有东西看”与「名称在上、`代码 · 市场 · 类型` 在下」的行式借 OpenStock 的 `SearchCommand`（**只借交互契约不拷代码**，它的 ⌘K 模态被丢弃——这里替代的是用户眼前的一个输入框，不是新开一层）。220 ms 防抖 + `createSearchSequence()` 丢弃被取代的响应（否则「600」的宽结果会画在「600519」的精确结果上面）；下拉尾部永远留一行「使用 <裸输入>」，且**框里已经是一个完整符号时自动收起**（否则它自己建议自己）。
  - **本轮只有浏览器实跑才能发现、单测全部放过的两个缺陷**（都已钉住）：① **Enter 的语义**：无高亮时原先一律“提交屏幕上的文字”，于是 `00700` 被原样提交——它只能靠**整条 loader fallback 链**才解析得出（实测 `_provenance.source = backtest:loader_fallback_chain`），而且 session / 自选 / 报警规则存下的就是这个非规范形态。现按 `isRoutableSymbol()`（后端 `_is_chartable` 的浏览器镜像，加 `-`/`_` 的币对/外汇例外）分三档：**不可路由且有候选 → 取首条；已可路由（`BTC-USDT`、`600519.SH`）或候选为 0 → 原样提交；用户显式高亮「使用 X」行 → 永远尊重**（`{ArrowDown}×3{Enter}` 仍可强推）；② `emptyHint` 只看“框里有字”，于是**列出两条候选的同时写着「无匹配，可直接输入完整代码」** ⇒ 补 `visible.length === 0`。另一个不在本轮范围但必须记下的既有设计：`ProChart.tsx` 的 `canMinute = /\.(SH|SZ)$/` ⇒ 选了 `.HK` 之后分钟线按钮置灰是**预期行为**（不是 ㉓ 引入的回归）；上一轮曾把它当成缺陷写进了两处注释，已按实测改回准确表述。
  - **刻意不接**：`/correlation`（上游页，输入框是 `000001.SZ,600519.SH,000858.SZ` 逗号串）与筛选器 `ScreenerTab`（`粘贴代码，逗号或换行分隔`）——两者都是**多标的批量粘贴**路径，换成 combobox 会把“粘一串”这个常用动作变难，而它们是上游文件（白改一行就多一个同步冲突面）。名册**不含拼音索引**（输入 `mt` 不会想到贵州茅台），也不把 32k 行下发到浏览器缓存。
  - 实测证据：后端 `pytest tests/test_symbol_roster.py tests/test_market_symbols_route.py` **87 passed**（76 + 11，全 mock，CI 无 OpenD 也绿）；后端全量 **54 failed / 12738 passed / 103 skipped / 9 errors**，与第四节第 30 条 Windows 基线**集合一致**，其中 roster/market_symbols 相关失败 **0 条**；前端 `vitest run` **83 文件 1074 用例**全绿（+2 文件：`SymbolCombobox.test.tsx` 16 个 `it`、`symbolSearch.test.ts` 9 个，含 `it.each` 展开共 39 条）、`tsc --noEmit` EXIT=0；`tools/ci_grep_gates.sh` 六道门中 **gate f 当场两次拦住本次自己写的测试**（先是一处真错——patch 了共享 `os`；改对之后又是它的**静态 grep 匹到了 docstring 里引用坏写法的那串字**，见第 42 条），剥开 ANSI 重读才能判定：现在 a/c/d/e/f 全绿，只剩 gate b 命中 `.qoder/repowiki/**.md`（被 `.git/info/exclude` 本地忽略，与 ㉑ 同一存量）；经 vite 代理的 `/market/symbols` 实测 **32-40 ms 全 200**；两轮内嵌浏览器复验（`/pro-chart`、`/tv-chart`、`/alerts`）确认候选展示/点选/键盘环/无候选直输/自选列表恢复，pro-chart 页 console **0 报错**；`vite build` EXIT=0（`SymbolCombobox-D4nbQQLC.js` 5.21 kB / gzip 2.48 kB 独立 chunk，`ProChart-CmDOjXVu.js` 422.57 kB）。
  - **同步上游 56 提交后的复验（`git rebase origin/main` 零冲突，上游本轮未碰 `api_server.py`/`market_routes.py`/四个页面/`frontend/src/lib`，用 `git diff --name-only HEAD...origin/main -- <这些路径>` 先确认）**：目标五文件共跑（roster + route + futu gateway + futu loader）**155 passed**；前端全量 **83 文件 1074 用例**全绿、`tsc --noEmit` EXIT=0；后端全量按新基线（第 30 条 2026-09-06 那份）**18 failed / 12955 passed / 9 errors，零新增回归**。
  - **上游冲突面**：新增 7 个文件（后端 `src/symbol_roster.py` + 两个测试；前端 `lib/symbolSearch.ts` + `components/common/SymbolCombobox.tsx` + 两个测试）；改上游文件 5 个，合计 **+92/-26**：`market_routes.py`(+37/-0)、`WatchList.tsx`、`ProChart.tsx`、`TVChart.tsx`、`Alerts.tsx`（后四者只是把 `<input>` 换成 `<SymbolCombobox>`，受控签名 `value/onChange/onPick` 与原 `onChange` 一一对应）。**不改 `api_server.py`、不改 `pyproject.toml`/lock、不改 8 个语言包、不加依赖**；`import futu` 仍是函数内延迟 import（未装 SDK 的环境连 `src.symbol_roster` 都能正常 import）。

- 本地自定义㉔说明：**应用内使用说明**（用户需求原话「写一份完整的使用说明书，并且要考虑下怎么使用比较方便，想好方案后和我确认」）。四个岔路口经确认选定：**使用者视角**（讲「打开浏览器之后该点哪里、输什么、会看到什么」，开发与同步细节留在本文件）/ **应用内 `/help` 页** / **单文件 + 目录锚点** / **先不配图**。真源 `frontend/public/usage-guide.md`（584 行 / 34520 字节 / 1 个 H1 + 21 个 H2 + 28 个 H3 = 49 条目录 / 66 行表格语法），页面 `frontend/src/pages/Help.tsx`（276 行），侧栏第 12 项「使用说明」。
  - **为什么真源放 `frontend/public/` 而不是 `?raw` 导入**：① `public/` 下的文件 vite 原样拷进 `dist/`，dev(:5899) 与 prod(:8000) 都能直接 `fetch` ⇒ **改文档不需要重新构建**（`:8000` 只是第一次要 build 把它带进 dist，之后静态目录逐请求读盘，连后端都不用重启）；② `?raw` 这条路是结构性地走不通——`frontend/vite.config.ts` 的 root 是 `frontend/`，导入仓库根的 md 得动 `server.fs.allow`（改上游文件），而 `tsconfig.json` 写了 `"types": ["vitest/globals"]` ⇒ **屏蔽了 `vite/client` 的 `*?raw` 环境声明**，`tsc -b` 会 TS2307，还得新增 `src/vite-env.d.ts`；③ CSP 是 `default-src 'self'; script-src 'self'; connect-src 'self'`（`agent/src/api/security.py`），同源 fetch 合法、注入脚本非法（与第 9 条同源）；④ 静态页**无鉴权**（`api_server.py` 把 `dist` 挂在 `/`，`require_auth` 是**逐路由 dependency 不是全局中间件**），所以设了 `API_AUTH_KEY` 的机器上说明书照样读得到——这是想要的行为，手册不该需要登录才能看懂怎么登录。
  - **锚点是这页的地基，也是最容易静默塌掉的地方**：react-markdown 默认**不给标题加 id**，仓库里也没有 `remark-slug` ⇒ 目录（从**原始 md 文本**解析）与渲染出的标题（`components={{h1..h4}}` 覆写）必须共用同一个 `slugifyHeading`，分开算就会「链接照样点、页面不动」。规则照 GitHub：折叠空白 → 小写 → 剥除「非字母/数字/`_`/`.`/`-`/**空格**」→ 再把剩余空格转 `-`。**顺序就是要紧**：先剥标点会把词间空格一起吃掉，`15 专业图表（/pro-chart）` 粘成 `15专业图表pro-chart`（本轮真实产出的缺陷，靠 vitest 实跑抓到，人眼读代码看不出来）；`6.1 目标机制` 的点号是保留字符 ⇒ `6.1-目标机制`。`extractToc` 必须跟踪 ``` 围栏，否则文档里公式与提示词样例中的 `#` 行会凭空长出目录项。
  - **一条只在真浏览器里才暴露的白屏路径**（见第四节第 43 条）：深链 effect 里 `decodeURIComponent(location.hash)` 遇到非法百分号转义（用户手打的 `/help#%zz`）抛 `URIError`，而 **passive effect 抛异常会让 React 卸载整棵树** ⇒ 一份说明书因为书签编码不对就整页空白。已把整个查锚+滚动包进 try/catch（`decodeURIComponent('%zz')` 实测确实抛 URIError，守卫不是摆设），并补了 `still renders the manual when the deep link cannot be resolved` 钉住。
  - **窄屏没有目录等于没有目录**：sticky 目录是 `hidden lg:block`，视口 <1024px 时整块消失（本机实测视口 757 px 就是这样）。补了一个 `lg:hidden` 的 `<details>目录（49 节）`，且必须放在那个 `flex` 行**外面**（做成 flex 子项会与正文并排而不是上下堆叠，第一版就踩了）；点选某节后自动收起（49 行摊开着会盖住刚跳过去的章节），但点列表留白不收起（`closest("a")` 判定）。jsdom 不跑媒体查询，所以测试只能钉住「这份列表存在、完整、能解析、点完收起」，宽屏形态只能靠真浏览器看。
  - 文档内容全部按代码事实写，含三条**如实告知而不是粉饰**：① `/alerts` 页与侧栏 Alerts 条目**目前渲染的是英文默认值**（`zh-CN.json` 里根本没有 `alerts` 命名空间，是第 32 条「不新增 locale key」取舍的直接后果）；② `:8000` 看到的是上一次 build 的产物、`:5899` 才是最新代码；③ 分钟线只对 `.SH/.SZ` 开放（`canMinute`），选 `.HK` 后置灰是预期行为。
  - 实测证据：前端 `vitest run` **84 文件 1090 用例**全绿（+1 文件：`Help.test.tsx` 16 个 `it`，其中一条读**真源文档**做防漂移断言——每个目录锚点非空且唯一，用 `path.resolve(__dirname, ...)` 读盘，因为 **jsdom 下 `import.meta.url` 是 http 协议、`fileURLToPath` 会 `TypeError: The URL must be of scheme file`**，仓库内 `src/__tests__/viteProxy.test.ts` 已是这个惯用法）；`tsc -b` EXIT=0（顺带证明 IDE 报的 `找不到 node:fs`/`toBeInTheDocument 不存在`/`找不到 @/pages/Help` 全是第 41 条那族假错）；`vite build` EXIT=0（`Help-I6iAli3H.js` 4889 字节独立 chunk、`dist/usage-guide.md` 34520 字节）；`tools/ci_grep_gates.sh` 剥 ANSI 后 a/c/d/e/f 全绿，只剩 gate b 命中 `.qoder/repowiki/**.md` 同一存量。浏览器实跑两模式：dev(:5899) 50 个标题全带 id、0 重复、49 条目录链接全部命中、8 张表、console 0 报错、点目录滚到 scrollTop 1742；prod(:8000) 深链 `#9-…` 滚到 6259、`#%zz` 不再白屏（21 章照渲染）；窄屏 757 px 下目录 `display:block` 而 sticky 轨 `display:none`、padding 点击不收起、点章节收起并跳到位。
  - **上游冲突面**：新增 3 个文件（`public/usage-guide.md`、`pages/Help.tsx`、`pages/__tests__/Help.test.tsx`）；改上游文件 2 个共 **+8/-1**：`router.tsx`(+3/-0：lazy 声明 + 注释 + 一条路由)、`components/layout/Layout.tsx`(+5/-1：`BookOpen` import + NAV 末尾一条)。**不加依赖、不改 8 个语言包**（侧栏条目用 `t('layout.help', { defaultValue: '使用说明' })` 的 defaultValue 直渲染中文，与 ⑨-㉓ 同一做法）、**不改 `vite.config.ts`/`tsconfig.json`/`security.py`/`src/tests/setup.ts`**（后者是上游的 jsdom 垫片集中地，为了 `scrollIntoView` 去改它就是给未来的同步冲突添一块砖）。

- 本地自定义㉕说明：**分钟 K 线全市场优先 FutuOpenD**（用户需求原话「做成全市场优先富途」；起因是上一轮诊断「港股/美股为什么没有分钟 K 线」）。根因既不是权限也不是额度，而是 `market_routes._kline_sync` 的分钟分支**压根没接富途**——写死只走新浪 A 股接口，而 `FutuLoader._INTERVAL_MAP` 早就支持 `1m/5m/15m/30m/1H`、`_SERVED_PREFIXES` 早就覆盖 `HK./SH./SZ./US.`：**能力一直在，路由没接**。用户选定的范围连 A 股也优先富途，新浪降为兜底。
  - **三个不显然的实现约束**（都不是「加个 if」能带过去的）：① **`60m` 不是富途的合法拼写**，loader 表里 60 分钟档叫 `1H` ⇒ 直传会 `KeyError`、被外层 `except` 吞成「网关没数据」，于是**富途最擅长的那一档恰好变成谁也拿不到的一档**；已加 `_FUTU_MINUTE_INTERVAL` 映射表，并让测试直接断言传给 loader 的 `interval == "1H"`。② **口径守卫**：`FutuLoader` 钉死 `autype="qfq"`，`adjust=none/hfq` 若照走富途就是**静默换成另一种价格口径、而线上没有任何迹象**——正是 `PRICE_CALIBER_BY_SOURCE` 注册表存在要禁止的那类事，所以 `_futu_minute_bars()` 对非 qfq 直接返回 `None` 落回公开路径。③ **窗口反推**：免费档历史只有约 7 天，`count` 要按 `_MINUTE_BARS_PER_DAY` 折算成交易日、乘 2 补自然日缺口，再用 `_MINUTE_WINDOW_CAP_DAYS = 12` 封顶。
  - **`end + 1 天` 是缓存新鲜度的承重墙，不是笔误**：`loader_cache_range_is_final()` 只在 `end_date < today` 时才落盘（`VIBE_TRADING_DATA_CACHE=true` 时），而路由传的 end 是「今天 +1」⇒ 落在未来 ⇒ **实时请求永不命中缓存**，同时翻回历史的已结算窗口照常命中。去掉那个看着多余的 `+1`，会让**每 5 分钟轮询一次的报警一整天读到同一份缓存**。已用两条测试锁死这个隐式依赖。
  - **顺带修掉一个存量缺陷：分钟轴整体偏 8 小时**（不在原任务里，是活体探针撞出来的，也是本轮最有价值的一条）。`_bars_from_frame` 用 `pd.Timestamp(naive).timestamp()`，而 **pandas 把 naive 当 UTC**（`datetime.timestamp()` 当本地，两者差 8 小时，探针实测 `1788514500` vs `1788485700`）；所有行情源的分钟 `time_key` 都是**交易所自己的本地钟点 naive**（铁证：茅台分钟序列**独缺 12:00 档**＝午休，只有「本地钟点」解释得通）。后果：**A 股 14:35 那根 K 线一直渲染在 22:35**，新浪源病了很久。日线之所以从没穿帮，是因为**日期标签经得起 8 小时挪动，钟点经不起**。修法：`_MINUTE_WALL_CLOCK_ZONE`（`SH/SZ/HK → Asia/Shanghai`、`US → America/New_York` 带 DST）+ 给 `_bars_from_frame` 加 `wall_clock_zone=None` 参数，**只喂给分钟线两条路径**；日线必须继续传 `None`（它的 timestamp 是「交易日标签」不是时刻，把美股午夜空点本地化会把标签挪到前一天），且**已 aware 的索引不再二次 localize**（会平移两次）。`Asia/Shanghai` 无夏令时，同时充当 HKT；`ambiguous=True, nonexistent="shift_forward"` 处理 DST 折叠。改完实测：腾讯到 16:00、AAPL 到 04:00（＝16:00 ET）、A 股富途与新浪**两条源完全对齐 `14:55..15:00`**（拨网关开关不跳轴）；HTTP 层同一验证里 **HK 与 AAPL 的末根 epoch 不再相同**——修复前两者都是 `1788537600000`，「两个时区收盘差 12 小时却同一 epoch」这个怪象本身就是该 bug 留在数据上的指纹。
  - **前端不是只放宽一个正则**：这条规则的调用点**实际有四处**（按钮 disable 态、`pickInterval` 点击处理、`applySymbol` 切标的时的周期修复、`readSession` 分享/刷新恢复），与第 43 条「锚点必须同一个函数算两次」同源。第二处镜像是历次改动遗漏的，后果是「**别人分享的 `700.HK` 5 分钟图能正常打开，一刷新掉回日线**」。上一轮做法是抽出 `canMinuteBars()` 单一谓词（带 `i` flag，因路由会先 upper-case 再回落），tooltip 分两态（启用态说明「优先富途、A 股落新浪」，置灰态说明「需 FutuOpenD，支持 .SH/.SZ/.HK/.US」）——**但当时只接了两处，剩下的两处酿成一次当场回归，见下面 2026-09-07 补记。**
  - **错误文案要说真因，而探针反过来证明了我的文案是错的**：非 A 股拿不到分钟线有三种完全不同的原因，旧代码共用一句。第一版我写的兜底文案让 `BTC-USDT` 用户「去检查 OpenD 是否在运行」，而 **BTC-USDT 从来没被发给富途**——正是我上一轮批评别人的那类误导，活体探针第 7 个用例当场抓到。现 `_minute_no_source_reason()` 三分支（口径不符 / 市场不覆盖 / 网关未应答），并把测试从「断言会 raise」**升级为「断言措辞」**：只断言 raise 时，文案写错测试照样全绿。
  - 实测证据：新后端测试 `agent/tests/test_market_kline_minute_routing.py`（404 行 / 32 个 `def test_`）**34 passed**；`market` + `futu` 相关 6 文件 **118 passed**；后端全量 **18 failed / 12989 passed / 113 skipped / 9 errors**，与存量基线精确对账（**12955 + 34 = 12989**，零回归），18 条失败按文件为 packaging×7 / swarm_replay×3 / positions_sectors×3 / swarm_retry×1 / state_migration×1 / api_infrastructure×1 / local_connections×1 / harness_eval×1，**不含任何 market/futu/kline 文件**，且单独复跑 `harness_eval` 那条确认是 `OSError: [WinError 1314] 客户端没有所需的特权`（Windows 建符号链接权限，与那批 symlink 失败同族）；前端 `vitest run` **85 文件 1108 用例**全绿（基线 84/1090，+1 文件 +18 例），`tsc -b` EXIT=0，`vite build` EXIT=0；`tools/ci_grep_gates.sh` a/c/d/e/f 全绿、gate e 的 4 条 WARN 是存量 `providers/llm.py`，唯一红的 gate b 仍只命中 `.qoder/repowiki/**.md`（被 `.git/info/exclude` 忽略，不入库不进 CI），并用 `git grep WorldQuant` 在本次 4 个改动文件上 **0 命中**证明 exit=1 与本次改动无关。**真 OpenD 活体 7 例 + `TestClient` 端到端 HTTP 4 例全通**（200/200/400/400，`source=futu:opend`，两条 400 各自报出真因）。
  - **如实告知的代价（不是「白拿富途」）**：① 富途历史 K 线额度按「每 7 天 N 个标的」计，**日线与分钟线同算**，A 股分钟线改富途优先后也开始烧这份额度，本轮验证累计发出约 14 次富途历史 K 线请求；② `/market/kline` 是**工作台筛选器（`ScreenerTab.screenPool`）逐标的调用**的，分钟周期扫 200 个标的＝200 次富途请求，所以那处文案改成明确提示「批量请用日线」，而不是让用户默默多花钱；③ 报警轮询（默认 300 000 ms/次）现在也走富途。缓释只有磁盘缓存一条，而**它恰好不能缓存实时窗口**（见上）。
  - **部署提醒**：`:8000` 起的是 `python api_server.py`（`api_server.py` 内**无 `reload=`**）⇒ **正在运行的后端仍是改动前的代码，必须重启才生效**；本轮没有替用户 kill 该进程（破坏性操作，超出自动执行边界）。文档已同步 `usage-guide.md` 三处（§15.2 分钟线四子弹、§19 FAQ 整条替换、§18.2 额度表两行），其中 FAQ 直接引用了 `_minute_no_source_reason()` 的真实输出串 `FutuOpenD covers HK/SH/SZ/US`。
  - **上游冲突面**：**0 个官方文件**——`git ls-tree --name-only upstream/main`（提交时 fetch 到 `c09deeaa` / tag `v0.1.14`）对 `market_routes.py`、`ProChart.tsx`、`ScreenerTab.tsx`、`usage-guide.md` 四路径**全部无输出**，即全是本地文件（依次为 ⑧、⑧、⑩ 期工作台、㉔ 的产物）。改动合计 **+215/-29**（market_routes 487→649 行、+174/-12；ProChart +32/-10；ScreenerTab +1/-1；usage-guide +8/-6），**新增 2 个文件**（后端路由测试 + 前端谓词测试）。**不加依赖、不改 8 个语言包、不改 `api_server.py`/`vite.config.ts`/`pyproject.toml`/lock**；`import futu` 仍是函数内延迟 import（未装 SDK 的环境照常落回新浪）。
  - **同步预演（提交前干跑，值得记下来）**：`agent/backtest/loaders/futu.py` 现在**同时存在于 upstream**（`c09deeaa`，213 行）且与 merge-base（`ff6695912`，213 行）**逐字节相同** ⇒ upstream 没碰过它，本地那份（325 行）是唯一的修改侧；upstream 那份也含 `"1H": "K_60M"`，所以 `_FUTU_MINUTE_INTERVAL` 映射的目标 key **两边都有**，不会因为同步而失指。`git merge-tree --write-tree HEAD upstream/main` **退出 0、零 CONFLICT**，且合完的树相对本地只有 7 个文件不同（6 个 `README*.md` + `agent/tests/test_readme_counts.py`），**不含本轮任何一个改动文件**。（另：本轮全量重跑后基线修正为 **12955** 存量 passed + 34 = **12989**，与第 30 条那份基线一致。）
  - **㉕ 交付当天就暴的回归（2026-09-07 补记；用户原话「图表中只有K线，没有分时」）**：上一轮抽 `canMinuteBars()` 时只让**两处**用上了新谓词（render 里的 `canMinute` 常量→按钮 disable + tooltip；`readSession`→分享/刷新恢复），而这条规则**实际有四处**：另两处 `pickInterval` 与 `applySymbol` 各自保留着内联的旧 `/\.(SH|SZ)$/`（可从 `git diff` 直接看到这三行旧代码）。后果有两条，用户看到的是第一条：**HK/US 的分钟按钮亮着、点了完全没反应、图永远停在日线**（旧代码那句注释 `// guarded, button also disabled` 在按钮不再 disable 之后当场变成假话）；第二条是**从别的标的切到 HK/US 时会被硬修回日线**（即使会话恢复那条路径已经放开了）。这就是用户说的「没有分时」——不是缺功能，是上一轮自己做的回归。两条可执行的教训而不是「下次细心」：① **先枚举再改**——放宽一个规则的任何一处前，必须 `grep` 出它在页面里的全部调用点（我自己写下的第 46 条自己没执行）；② **纯函数测试钉不住接线**——`canMinuteBars` 自身的 case 全绿而功能是碎的。本轮做法：规则收敛到唯一入口 `intervalAllowed(symbol, interval)`（四处均调它，`repairInterval` 也从它派生，页面里不再有任何内联的周期/后缀判断），测试升级为**真的 `render(<ProChart />)` + `fireEvent.click`「5分」+ 断言假 chart 收到 `{type:"minute",span:5}`**。
  - **本轮做了三次反向验证（mutation probe），逐个站点验**：把 `pickInterval` 改回旧正则 ⇒ **恰好 1 例转红**；改回 `applySymbol` 那一行 ⇒ **恰好 1 例**（不同的那一例）；把 `disabled` 写死成 `false` ⇒ **恰好 1 例**；三次都改回后 **29 passed**、`grep MUTATION PROBE` **零残留**。这才能说三条新增行为测试各自坐在一个站点上，而不是堆了个看起来更多的测试数。
  - 本轮验证数字：`ProChartMinuteBars.test.tsx` 18 例 → **29 例**（新增「点击生效」「切标的保留分钟周期」「切到 BTC 修回日线」「BTC 分钟按钮仍置灰」四例挂载测试）；前端全量 **85 文件 / 1119 用例全绿**（上轮 1108，+11）；`tsc -b` EXIT=0、`vite build` EXIT=0（**`dist/` 已随 build 更新，本轮未改后端⇒浏览器刷新即可见到修复，不必重启 `:8000`**）；后端同族 6 文件复跑 **118 passed** 复核；门禁仍仅 gate b 存量红（命中被 `.git/info/exclude` 忽略的 `.qoder/repowiki`）。改动 `ProChart.tsx` +28/-15、测试 +124/-9。**取证失误两条（同族于第 47 条）**：直接 `bash tools/ci_grep_gates.sh` 在本机命中 **WSL 且无发行版**，输出全是一句「未安装 Linux 子系统」且 `EXIT=1`——**门禁根本没跑，而退出码看起来像是「跑了并且红了」**，必须用 **`& "C:\Program Files\Git\bin\bash.exe"`**（第 47 条② 的新变体：坑不在语法，而在 `bash` 实际解析到哪个程序）；以及又凭记忆写了一个不存在的测试文件名（`tests/test_market_kline_routes.py`），`pytest` 返 **EXIT=4**（用法错）而不是测试失败——EXIT=4 与 EXIT=1 必须分清，后者才是真失败。
  - **「分时」一词的歧义已取证，待用户定性**：券商语境的「分时」指**当日折线图（分时走势 + 均价线）**，与分钟蜡烛图是两种视图。`frontend/src` 下 grep `分时|timeShare|timeline`：**「分时」字面零命中**（`timeline` 只出现在 Alerts 的通知历史，INTERVALS 表只有「1分…60分 + 日线」六档蜡烛），即本项目**从来没有折线分时视图**。klinecharts v10 具备做它的原生能力（`CandleType` 含 `"area"`，`CandleStyle.area` 配 `lineSize`/颜色，均价线可挂 MA 或 VWAP）。若用户要的是这个，那是一个**新功能，不在「全市场优先富途」范围内**，需先对齐再动手。**（已定性：用户选定「新增折线分时视图」，交付见下面 ㉖。）**

- 本地自定义 ㉖说明：**分时折线视图**（券商那种「当日分钟折线 + 均价线 + 昨收涨跌幅」）。起因是用户原话「图表中只有K线，没有分时」——取证先坐实那是 ㉕ 补记的回归（已由 `f1e5364b` 修好），再用 AskUserQuestion 定性，用户选定「**新增折线分时视图**」，即上面那条歧义的答复。
  - **先定性的架构选择：分时是一种「视图」而不是「第七个周期」**。状态收成单个 `ChartView {interval, timeShare}`，`viewPeriod(view)` 是**唯一**决定画哪个周期的函数，而 `interval` 全程不被分时覆盖 ⇒ 关掉分时回到用户**原来那一档**（不是掉到 1 分钟蜡烛）、15 分开过关再开回来还是 15 分、切标时分时状态跟着 `viewForSymbol()` 走（BTC 不支持就丢弃，HK/US 保留）。若把 `interval` 直接改成 `"1m"`，这三条全丢。
  - **切片必须在后端**（`session=latest` → `_split_latest_session(bars, zone)` 返回 `(bars, session_date, prev_close)`）：两个独立理由——① 前端要做就得复制 `_MINUTE_WALL_CLOCK_ZONE`（那是本项目第 46 条教训的第五处镜像）；② 按访问者自己的钟日分组会把纽约 09:30–16:10（= 北京 21:30–04:10）**切成两半**。`prev_close` 拿不到时**必须返回 `None` 而不是当天第一根**（后者会把每一个 session 都画成「平盘开盘」），前端相应地写 `昨收未知` 而**不印 `+0.00%`**；非分钟周期 + `session=latest` 直接拒（日线本来就是一天一根），未知 session 值不默默忽略。
  - **本轮最值钱的一条：探针拓出一个「等价变异体」，而它不是漏测**。把切片时区写死成 `ZoneInfo("UTC")` ⇒ **44 例全绿**。原因不是测试写得少：四个市场的分钟时段**全部落在同一个 UTC 日内**（HK/A 09:30–15:00 → 01:30–07:00 UTC；US 09:30–16:10 ET → 13:30–20:10 UTC），**拿现网形状的数据永远分辨不出两种日历**。修法是补两条**不同性质**的钉而不是造假数据：① 直接单测 `_split_latest_session`，喂两根纽约 20:00 / 21:00 ET（UTC 已是次日）的 bar ⇒ 能杀「写死 UTC」；② 接线测：monkeypatch `_split_latest_session` 记录 zone 实参，按四个后缀参数化断言它取的确实是表里那一条 ⇒ 能杀「路由传错时区」。改完 B1a 杀 1 例 / B1b（北京时间钟日）杀 2 例 / B1c 杀 4 例。**可复用的判据：探针零转红时必须先问「这个变异可观测吗」，而不是立刻补测试凑数——第 47 条那族是「探针自身坏了」，本条是「fixture 与变异体在数据上等价」，两者处置完全不同。**
  - **`chartStyles(dark, timeShare)` 是 `candle.type` 的唯一写者**，且主题 effect 的依赖必须是 `[dark, timeShare]`：少一个 `timeShare`，用户切一次主题就把折线打回蜡烛（本轮最大的坑）。两次独立变异（删 style 里的 timeShare / 删 effect 依赖）**命中完全相同的 4 例**，反而证明了两处是同一件事的两个写法。`candle.area` 配 `lineSize/lineColor/value:"close"/smooth:false/backgroundColor:[GradientColor…]`，线色 `#2b7de9` 故意不用红/绿（那两色已被蜡烛与 badge 占用）。
  - **实读 klinecharts 10.0.3 的 dist 推翻了「需要手动 resetData」的假设**：`StoreImp.setPeriod`(dist 13421) **无条件**调 `resetData()` 且**不比较新旧 period** ⇒ 「1 分钟蜡烛 ↔ 1 分钟分时」这种同周期切换照样重取（已钉一条专门测试），本需求根本不需要 `resetData()`。另：`figures[]` 缺 `type` 会被静默丢弃（既有教训），所以 `AVG_PRICE` 的 `figures` 写死 `type: "line" as const`。
  - **均价是 VWAP 近似不是交易所口径**：bar 只有 OHLCV、无成交额，所以用 `Σ(close×volume)/Σ(volume)`；**volume 全为 0 时退化为累计均值**（否则均价钉 0 会拉塌 y 轴）。分时**禁止 forward 翻页**（一次进入 = 一次 800 根请求，约两个交易日，得多要一点才顺手够到昨收），`more` 直接回 `{forward:false, backward:false}` 而不发 HTTP。**有意的简化**：画线桶仍是 `TICKER|1m`，分时与 1 分钟蜡烛共用同一桶（分时不是一种新周期，不该另起一份存量），§15.2 已明写。
  - 会话持久化形状变了（`{symbol, interval, timeShare}`）；`readSession` 用 `timeShare === true` 严格判定，损坏/手改的桶不会自作主张把图画成折线；周期按钮高亮改用 `periodActive = !timeShare && interval === key`（分时亮着时六个周期按钮都不亮——它们说的是蜡烛的周期，不是折线的）。
  - 实测证据：后端 `agent/tests/test_market_kline_minute_routing.py` 404 → **584 行 / 44 个 `def test_` / 49 passed**（上阶段 44 + 本轮 5）；`market` + `futu` 同族 12 文件 **241 passed**。变异 **22 次零空转**：后端 6 站点（B1a/B1b/B1c/B2 prev_close 退回当日首根/B3 日线默默忽略/B4 未知 session 默默接受）转红 1/2/4/1/1/1；`lib/timeShare.ts` 5 站点（M1-M5）2/4/1/1/1；`ProChart.tsx` 11 站点（S1-S11）4/4/7/1/1/1/1/1/8/1/1；探针后逐字节还原（`restored byte-identical: True` / `same=True`）。前端 `vitest run` **87 文件 / 1159 用例**全绿（上轮 85/1119；+`timeShare.test.ts` 18 + `ProChartTimeShare.test.tsx` 15 + `ProChartMinuteBars.test.tsx` 29→36）、`tsc -b --force` EXIT=0、`vite build` EXIT=0（`ProChart-HWJNs6OB.js` 426.04 kB / gzip 122.56 kB）。门禁 a/c/d/e/f 全绿，gate b 仍只命中被 `.git/info/exclude` 忽略的 `.qoder/repowiki/**.md`，并排除 `.qoder` 后复 grep：全仓只剩本文件那两条**自指门规则**的散文。
  - **真 FutuOpenD 活体**（新开一台临时后端 `:8010` 跑新代码，不去动用户的 `:8000`）：`_kline_sync(…, session="latest")` 直调三市场 **HK 331 根 / US 390 / A 241 根且 `distinct_days=1`**（按各自交易所钟点判定），`prev_close` 433.0 / 328.21 / 1298.88 ⇒ 涨跌幅 +2.26% / −2.51% / +2.40%；HTTP 层 `GET /market/kline?…&session=latest` **200 + `source=futu:opend` + 331/433.0**。取证当时是周一 07:31 未开盘，`session_date` 正确回落到上周五 `2026-09-04`——「最近一个 session」而不是「今天」，正是想要的语义。
  - **诚实告知的未验项**：折线的**观感没有在真浏览器里看过**（本轮会话未挂浏览器工具），只有 15 例挂载测试钉住「点亮分时 → chart 收到 `{type:"minute",span:1}` + area 样式 + 恰好一条 `session=latest` 请求 + badge 文案」；视觉验收需用户实地确认。`f1e5364b` 之后 **`:8000` 仍是旧代码，必须重启后端**（`python api_server.py` 无 `--reload`，本轮未替用户 kill——交付后 15 分钟用户就因此撞到一个假故障，见下面两条）；取证期间另起过同端口的临时 vite，交付前用户原本的 `:5899` dev server 已不在监听，已重启回原位。
  - **上游冲突面**：**0 个官方文件**。`git ls-tree --name-only upstream/main`（按上次 fetch 的 `c09deeaa`）对本次 6 个已跟踪路径（`market_routes.py`、后端测试、`marketApi.ts`、`ProChart.tsx`、`usage-guide.md`）**全部无输出**，即全是本地文件；`git merge-tree --write-tree HEAD upstream/main` **EXIT=0 / 零 CONFLICT**。新增 4 个文件（`lib/timeShare.ts` + 其测试、`pages/__tests__/ProChartTimeShare.test.tsx`，均为本地自定义引入）；改上游文件 **0 个**，改本地文件 5 个合计 **+561/-41**（market_routes +67/-3、后端测试 +180/-0、ProChart +262/-35、marketApi +14/-0、MinuteBars 测试 +51/-1、usage-guide +21/-3）。**不加依赖、不改 8 个语言包（分时 badge / 均价指标名均直写中文）、不改 `api_server.py`/`vite.config.ts`/`pyproject.toml`/lock、不放宽 CSP。**
- **交付后 15 分钟就撞上了那个未验项**：用户报「港股的分钟图无法显示」，附图为红字 `minute bars are only supported for .SH/.SZ A-share symbols`——**这句话在当前代码里根本不存在**（全仓 grep 0 命中；`git log -S` 显示它由 `7306d511` 引入、由 `46c68bea` 删除）。运行中的 :8000 进程启动于 09-06 21:58，而 `46c68bea` 落盘于 09-07 02:03 ⇒ **新界面 + 旧后端**：`vite build` 把 :8000 的静态产物就地换成了新版（逐请求读盘），而 `api_server.py` 无热重载。即：用户看到的不是本轮功能坏了，而是旧进程在回答新界面的请求。
- 处置：杀掉旧进程并重开（`Start-Process -FilePath python -ArgumentList api_server.py -WorkingDirectory …\agent`，等价于 `start.bat` 那行），复验 **HK 5m 120 根 / HK 1m `session=latest` 331 根 `prev_close=433.0` / A 股 241 根 `prev_close=1298.88`，均 `source=futu:opend`**；:5899 也已回位。
- 可复用的判据：**报错文案是一句在代码/说明书里查不到的句子 ⇒ 先怀疑进程年代，而不是改代码**。已把这条判据写回 §19（新增一条 FAQ）。另一个环境坑：`Start-Process cmd /k “cd /d … && python …”` 这种带嵌套引号的启动方式在本沙箱下拉不起服务（窗口会留在报错处），用 `-FilePath python -ArgumentList … -WorkingDirectory …` 才稳。

- 本地自定义 ㉗说明：**主图 MA 图例重复十几行**（2026-09-09 用户原话「主图显示有问题，显示Ma5,Ma10,Ma30,Ma60重复显示」+ 截图约 20 行）。是 ㉖ 引入的回归：`syncPriceOverlay` 被放进**每次周期点击都会跑**的 `setView`，而它只删「对面那个视图」的 overlay（分时→删 MA、蜡烛→删均价），**不删自己要上的那个** ⇒ 每点一次「5分/15分/…」就往 candle_pane 追加一份 MA。
  - **klinecharts 10.0.3 的 `createIndicator(value, true)` 不是幂等的**（本轮最重要的库事实）：`StoreImp.addIndicator`(dist 14150) 确实做了去重 `getIndicatorsByFilter(create)`，但 `ChartImp.createIndicator`(dist 15270) 刚刚给 `create.id` 铸造了一个**全新 id**，而带 id 的 filter **只按 id 匹配**(dist 14179) ⇒ 去重永远命中 0 条，`isStack: true` 一律追加。图例是按 pane 里的实例数逐行画的(dist 7485)，所以「一次点击一行」。修法是在页面侧守住「价格面板恰好一个 overlay」这条不变量：`getIndicators({name})` 为空才 create，并把多出来的副本 `removeIndicator({id})` 掉（从任意旧状态自愈）。
  - **为什么 1159 例全绿却漏了它**：四个 ProChart 测试替身都写着 `getIndicators: () => []`——一个**对被测副作用不建模的假 chart**。㉖ 的挂载测试断的是「发出去过哪些 create/remove 调用」，而缺陷恰恰是「调用之后 pane 上剩几份」。**可复用的判据：接线测试的断言对象必须是「外部可观测的状态」而不是「调用日志」；替身对某个 API 返回常量（`[]`/`true`/`undefined`）时，任何依赖该返回值的逻辑等于没测。** 本轮把 `ProChartTimeShare` 的替身改成**忠实复刻库语义**的状态化 store（create 必追加、filter 按 id 优先否则按 name），新增 3 例断 mounted 数量。
  - **活体取证（本项目第一次真开浏览器）**：browser-use MCP + 真 :8000 + 真 FutuOpenD，用 React fiber 走到 `chartRef.current` 直接读 `chart.getIndicators()`。修复前：从 1 份起点「1分/5分/15分/30分/60分/日线」**每点一次 +1，6 次后 7 份**（用户截图即此状态）；修复后同一序列 **MA 恒为 1、`getIndicators().length` 恒为 3（MA+VOL+MACD）**，再跑 分时↔日线 与切标的（茅台/腾讯）共 12 次交互仍为 1，截图确认图例只剩一行。
  - 变异 4 次全杀：M1 恢复「每次都 create」⇒ 2 例红；M2 删副本清理 ⇒ 1 例红；M3 wanted/other 写反 ⇒ 3 例红；M4 不再移除对面 overlay ⇒ 3 例红；每轮 `restored byte-identical: True`。**探针自己也坏过一次**：`--reporter=basic` 在 vitest 4 已不存在 ⇒ CLI 直接报错、无 summary，被正则当成「0 failed」——已改成「**没有 summary 就是 INCONCLUSIVE**」，不能等于绿（第 47 条同族）。
  - 实测数字：`ProChartTimeShare.test.tsx` 15 → **18 例**；前端全量 **87 文件 / 1162 用例**全绿（上轮 1159，+3）、`tsc -b --force` EXIT=0、`vite build` EXIT=0（`ProChart-DxDdX-A6.js` 426.09 kB / gzip 122.60 kB）；门禁 a/c/d/e/f 全绿、gate b 仍是 `.qoder/repowiki` 存量红。**本轮未改后端**（0 次 pytest）。环境补记：`:8000`/`:5899` 需重新拉起时用 `Start-Process -FilePath python -ArgumentList api_server.py -WorkingDirectory …\agent` 与 `-FilePath npm.cmd -ArgumentList run,dev -WorkingDirectory …\frontend`；browser-use 的 `evaluate_script` **15 秒硬超时**，长交互序列要切成 ≤3 步的小批。
  - **上游冲突面：0 个官方文件**（`ProChart.tsx` 与其分时测试均为本地文件，㉖ 已核）；改 2 个本地文件合计 **+121/-10**（ProChart +21/-7、测试 +100/-3）。不加依赖、不改语言包、不改说明书（缺陷修复，用户可见行为不变）。

- 本地自定义 ㉘说明：**分时这张图整体不可用**（2026-09-10 用户原话「分时显示不正常，功能实现存在问题，重新分析这个功能，可以看看github上有没有现成的功能实现」+ 追加授权「所有操作自动进行，包括使用网络功能，到github上查找」）。不是新需求，是 ㉖ 的验收缺陷：㉖ 交付了「取哪一天、画成线、badge 文案」，**没有交付「这张图看起来对不对」**。重新分析后列出 7 条模型层缺陷 D1–D7，本轮修 D1/D2/D3 + 部分 D4 + 两个附带洞，D5/D6/D7 有意不做（见下）。
  - **GitHub 调研（用户明确要求，结论是「没有现成的可直接用」）**：`klinecharts/KLineChart` issue **#790**（open，2026-04-01）「能否添加个分时图的例子」正文逐字命中本轮 D1——按文档设 `candle:{type:'area'}` 后「**样式不正确，一天内的数据没有在一个屏幕内展示，而是像日k图那样还是要左右滑动**」，**官方无示例、无人回复**；#187 同类（closed）；#341「分时图逻辑补全」要的是双坐标轴 + 以昨收为 0 轴上下居中。`jones2000/HQChart`（3.4k★）只当**分时功能清单**参照，属换库，不在「不加依赖」的授权范围内 ⇒ 这三条只能自己补。
  - **D1 铺满（致命）的库事实**：`candle.type:"area"` 只改画法**不改窗口**，zoom 仍是 `DEFAULT_BAR_SPACE = 10`(dist 13056)，`_adjustVisibleRange`(dist 13533) 按 `_totalBarSpace / _barSpace` 出可视根数，**库不会自动拟合**。`barSpaceLimit {min:1,max:50}`(dist 13249)，而 `StoreImp.setBarSpace`(dist 13666) 越界或等值是**静默 return**（不 clamp）⇒ 夹取必须自己做（`fitBarSpace`）。有效配方就三句：`setOffsetRightDistance(0)` + `setBarSpace(contentWidth/barCount)` + `scrollToRealTime(0)`。
    - 活体前后对比：**修前** 376px 面板 / 331 根 ⇒ 只见最后约 40 根，x 轴末格是 `16:06`（16:00 收盘之后不存在的一分钟，即 D2）；**修后** `barSpace 1.12`、`visibleRange {from:0,to:331}`、x 轴首格 **09:30**、末格 14:22 ⇒ 整天一屏、右缘无留白。
    - **取整方向是向下**（保留 2 位小数）：200/176 ⇒ floor 1.13（1.13×176=198.88 在框内）而 round 1.14（1.14×176=200.64 溢出）。M2 变异（floor→round）在 ㉖ 留下的旧用例 `400/120=3.33` 上**不转红**（两者同值）⇒ 换的是用例不是断言；宁可挤也绝不把线画到画面外。
  - **D3 React StrictMode 下面板预算被整块跳过**：`layoutSigRef` 原本只存字符串，而这个 ref 在 dev 的两次挂载之间**是被保留的** ⇒ 第二次挂载拿到的新 chart 实例面板回到库默认 100px，守卫却命中了。修法是把 **chart 身份**一起放进 ref（`{chart, sig}`）。实测：修前 `sub:VOL=100 / sub:MACD=100 / 主图 130px`，修后 `74 / 74 / 主图 182px`。**prod 单挂载不受影响 ⇒ 这是一类只在 dev 复现的缺陷**（第 10 条通用病的又一实例）。
    - 顺带钉一个替身事实：`planPaneHeights` 的 `assignments` **只含副图**（`paneLayout.ts:156`），主图靠库自己吸收余量 ⇒ 测试替身若只报 candle_pane 就永远不会调 `setPaneOptions`（本例第一版正是这样假失败，不是代码坏）。
  - **D4 缺昨收基准：本轮只做「灰虚线 + 图例」，对称轴/百分比轴经实测否决**。机制上选**注册指标**而不是自绘：`YAxisImp.createRangeImp`(dist 984 → 1078) 会把 pane 内 indicator 的 figure 值纳入纵轴范围 ⇒ 整天都在昨收下方时，那条线和它量的高度都不会被挤出画面（实测 `PREV_CLOSE@candle_pane` 331 行、末行 `{prev:435.4}`）。`figures[].styles` 允许是回调（d.ts 738-745），虚线样式由此给出。
    - **顺序坑（值得单独记住）**：昨收值只能放模块态（指标 `calc` 只拿得到 bar 列表），所以 `setChangeBase` 必须在 `callback()` **之前**——`calc` 就在那次投递里跑，而分时既无翻页也无推送，写晚了这条线要等到一个永不发生的下一个数据事件（M4 变异专门杀它）。
    - **否决理由（实测，不是猜测）**：`ChartImp.overrideYAxis`(dist 15561) → `DrawPane.createOrOverrideYAxis`(dist 10308) → `YAxisImp.override` 的 `merge` 确实挂得上自定义 `createRange`，但 `createRangeImp` 消费的是 **`realFrom/realTo/realRange`**——只返回 `{from,to}` 会把轴打成 NaN、ticks 空；补齐 9 个字段后**生效的仍是数据自然范围**。扩展点在本页的 dev 双挂载/后台标签页路径下不可靠 ⇒ 不把可读性建在它上面。另注：`getYAxes(filter)` 的 filter 是**必填**（dist 15540 直接解构），与 `getIndicators`（有 `?? {}`）不同。
  - **附带洞 1：OpenD 首笔空响让整块图变白 ⇒ 分时自动重试一次（600ms，只此一次）**。活体证据：页面报 `minute bars … FutuOpenD … did not answer`，而同进程隔一秒并发改 4 次**全部 331 根** ⇒ 定性为空闲后首笔抖动，不是代码坏了。蜡烛有 fallback 链，分时只有一个源，一次空响就是一张白图。
  - **附带洞 2：缩放借还 + 兜底（活体才测得出来的那一个）**。把 barSpace 压到 1.1px 是**分时视图的属性**，不是数据的 ⇒ 进分时存 `candleSpaceRef`、退出还回。**但页面直接以分时打开（session 恢复）时 `candleSpaceRef` 是空的**，退出分时就把 176 根日线挤成 1.12px 一条——同一个报障搬到开关另一侧。加 `DEFAULT_CANDLE_BAR_SPACE = 10`（库自己的默认值）兜底 + 一条 jsdom 用例，浏览器复验 1.12 → 10。**判据：这类「进入/退出」对称缺陷，代码只读得出「借了要还」，读不出「从未借过」的那条路径。**
  - **有意未做（诚实登记）**：D5 午休被 index 轴抹平——与通达信一致（午休不占宽度，11:30/13:00 就是相邻 bar），加分隔线要用 overlay，会污染 `serializeDrawings` 的画线持久化桶 ⇒ 不做，说明书也不承诺；D6 分时里 MACD 无意义——裁剪副图会与用户自定义副图打架，改由 D3 保证高度合理；D7 分时 tooltip 未定制。
  - **取证判据新增一条：截图不可信**。本轮一张写着「1290.88 / 1 个交易日 / 331 根 / 1 分线」的截图，其字样全仓 `grep -rn '分线'` **0 命中** ⇒ 截图与 JS 上下文不是同一份渲染（后台标签页 rAF 停摆同族）。分时这类画布功能**一律用模型层数值断言**（`barSpace` / `visibleRange` / `getIndicators()` / `getPaneOptions()`），不用截图当证据。
  - **测试替身纪律**（㉗ 那条判据执行到底）：`barSpace`、`offsetRight`、`scrollToRealTime`、pane 高度、`getDom` 宽度全部建成**状态**而非调用日志；`init()` 每次调用新建**该实例私有**的 `panes` 列表（共享列表会让第二次挂载「看起来已完成」）；`callback` 内捕获 `basesAtDelivery`。新增 3 个 describe（铺满 / 昨收喂入顺序 / StrictMode 预算）。变异 4 站点全杀：M1 删 loader 里的 fit 调用 ⇒ 3 例红，M2 floor→round ⇒ 1 例红（1.13≠1.14），M3 守卫只比字符串 ⇒ StrictMode 例红，M4 `setChangeBase` 挪到 `callback` 后 ⇒ base-at-delivery 例红；每轮逐字节还原。
  - 实测数字：`timeShare.test.ts` 18 → **27 例**、`ProChartTimeShare.test.tsx` 18 → **27 例**；前端全量 **87 文件 / 1180 用例**全绿（上轮 1162，+18）、`tsc -b --force` EXIT=0、`vite build` EXIT=0（`ProChart-_C9sCRxf.js` 427.36 kB / gzip 123.04 kB）。门禁 a/c/d/e/f 全绿，gate b 仍只命中 `.qoder/repowiki/**`（`git ls-files` 证实**未被 git 跟踪**的 IDE 生成缓存），排除 `.qoder` 后全仓只剩门禁脚本与本文件的自指散文。**本轮未改后端**（0 次 pytest）。
  - **上游冲突面：0 个官方文件**（5 个改动路径 `git ls-tree --name-only upstream/main` 全部无输出，均为本地文件）；`git merge-tree --write-tree HEAD upstream/main` **EXIT=0 / 零 CONFLICT**。改 5 个本地文件合计 **+611/-32**（`timeShare.ts` +136/-0、`ProChart.tsx` +113/-21、`timeShare.test.ts` +107/-0、`ProChartTimeShare.test.tsx` +248/-7、`usage-guide.md` +7/-4）。不加依赖、不改 8 个语言包（昨收线 `shortName`/图例直写中文）、不改 lock/CSP。§15.2 分时条目同步重写（一屏放得下整天 / 昨收虚线是零线 / 自动重试一次 / 缩放借还 / 昨收未知则线也没有），并顺手修掉一处「分画」错字。
  - **推送时发现 origin/main 已领先 26 个提交**（包含一次 `upstream/main` 合并与 0.1.15 发布）：`git diff --name-only` 比对两边改动的 56 / 6 个路径，**交集为空** ⇒ `git rebase origin/main` 一次干净通过（本地那 1 个提交未被推过，不构成改写已发布历史）。rebase 后按纪律**在新基线上重跑全量**：87 文件 / 1180 用例全绿、`tsc -b --force` EXIT=0、`vite build` EXIT=0（chunk 名变为 `ProChart-B-W_JbFN.js`，427.36 kB / gzip 123.04 kB 不变——上游只改了 `package.json` 的 version 字段，无新依赖）。推论：**上游合并进来的 `fix(loaders)` 等后端改动正在要求重启 `:8000` 才会生效**（㉖ 交付后那三条同族：旧进程回答新界面），本轮分时不依赖它们，但跑回测的人需要重拉后端。

- 本地自定义 ㉙说明：**分时「显示还是有问题」——盘中左半边空白**（2026-09-10 用户原话「分时显示还是有问题，显示不正常」+ 截图：09988.HK 盘中 09:33，badge / 图例 / x 轴 09:30→09:42 都正常，但折线从画面中间才开始、左侧约 1/3 完全空白；标题处是「09988.HK · 1」）。**不是 ㉘ 修坏了，是 ㉘ 的取证时机看不见这个形状**：㉘ 全部证据取自收盘后 331 根，那时 `fitBarSpace` 不夹取、残差恒 0 ⇒ 补「盘中 + 收盘各取证一次」的纪律（§四 第 50 条）。
  - **根因是一条算术，不是一个玄学现象**：`fitBarSpace` 把间距夹在 `BAR_SPACE_LIMIT=[1,50]`。盘中 09:33 只有 13 根，922px 的面板需要 71px/根 ⇒ 只给得到 50 ⇒ 内容 650px；而 ㉘ 的 `setOffsetRightDistance(0)` 把**最后一根钉在右边缘** ⇒ 用不掉的 272px 全部堆到**左边**。
  - **修法**：新增 `fitOffsetRight(contentWidth, barSpace, barCount) = max(0, contentWidth - barSpace*barCount)`，把残差交给**右侧**（今天还没走到的分钟），折线从 09:30 那侧开始画。这个值是从 `StoreImp.dataIndexToCoordinate`(dist 13885) 反解的：`x(i) = totalBarSpace - (dataCount + offset/barSpace - i - 0.5)*barSpace` ⇒ `x(0) = barSpace/2` 当且仅当 `offset = totalBarSpace - n*barSpace`。不夹顶时残差 ≤ 0.01×N px（`fitBarSpace` floor 的余量）⇒ **收盘后形状逐像素不变**（活体：81 根 / `barSpace 4.61` / `offsetRight 0.59px`）。
  - **本轮最硬的库事实：这两个调用的顺序是 load-bearing**。`StoreImp.setOffsetRightDistance`(dist 13692-13694) 用**当时的 barSpace** 把像素换算成 `_lastBarRightSideDiffBarCount`，而 `StoreImp.setBarSpace`(dist 13666-13681) **从不重算**那个 bar 数 ⇒ 先设偏移后设缩放，残差会被两次缩放之比悄悄改掉。`ChartImp.setOffsetRightDistance`(dist 15174) 内部传 `isUpdate=true`，会重排可视区。
  - 顺带核掉的四个担心：① **正偏移不会被夹**——`_scrollLimitRole='bar_count'`(dist 13170) 让 `Math.min(_maxOffsetDistance.right=50, d)` 那条分支不生效；`maxRightOffsetBarCount = visibleBarCount - min(2, total)`(dist 13549)，而我们的 `diff = visibleBarCount - total` 恒 ≤ 它。② `scrollToRealTime`(dist 15629-15634) 在此**可证明是 no-op**（它减去的 `getInitialOffsetRightDistance()`(dist 13707) 就是刚被写进去的同一个字段），保留只为表达意图。③ `realFrom=-1`(dist 13566) 安全——`CandleAreaView.drawImp`(dist 6406-6408) 有 `kLineData?.[value]` + `isNumber` 守卫跳过空列。④ x 轴确实会向未来外推刻度（`XAxisImp.createTicksImp` dist 10916 → `dataIndexToTimestamp` dist 13784），但盘中那些是**还没到的真实分钟**，与券商分时一致 ⇒ 接受，并写进说明书免得被当成数据缺失。
  - **否决的诱惑：把 50px 天花板抬了不就完了？**不行——`barSpaceLimit` 只在 dist 13249 定义、13667 消费，`_layoutOptions` 仅在 ChartStore 构造时从 `options.layout` merge(dist 13273-13276)，全 dist 搜 `applyOptions`/`setOptions` **0 命中** ⇒ 只能 `init()` 传，那会连带放宽蜡烛图的缩放（第 49 条同族：扩展点先实测再往上建功能）。另一条否决：造一张「当日应有几分钟」的 capacity 表——后端没这表，新造等于在本地复制交易所日历（第 46 条会第一时间点名它）。
  - **第二个缺陷（标题「· 1」）机制独立**：tooltip 标题 = `span + i18n(type)`(dist 7431) 套 `'{ticker} · {period}'`(dist 11512)，而库两张语言表都写 `minute: ''`(dist 6944/6975)，`i18n`(dist 7006-7009) 只在 `null/undefined` 才回退成 key ⇒ **空串直接命中，永不回退**。`registerLocale`(dist 7000-7002) 是 **merge** 语义，所以 `lib/klineLocale.ts` 只补 `minute` 一个字段、遍历 `getSupportedLocales()` 打一遍，不复制整张表、不碰 8 个官方语言包（硬约束）。TS 侧 `Locales` 有 15 个必填 key：让函数返回 `Pick<Locales,"minute">`、只在调用处 `as Locales` 局部放宽，而不是把整表抄一遍。
  - **交付前自查发现的对称缺口：分时借走的不只是缩放，还有右边距**。`StoreImp.resetData`(dist 13652-13657) 不重置 `_lastBarRightSideDiffBarCount` ⇒ 退出分时只还原 barSpace，就会把分时留下的**bar 数**按蜡烛间距重新放大：1920px 面板 / 13 根 ⇒ 残差 1270px = 25.4 根 ⇒ 回蜡烛后凭空多出 **254px** 右边空白。修法是 `candleOffsetRightRef` 与缩放同借同还，且**先 `setBarSpace` 再 `setOffsetRightDistance`**（同一条顺序约束）；没借过（页面直接开在分时）则还库自己的 `DEFAULT_OFFSET_RIGHT_DISTANCE = 80`(dist 13057)。
  - **变异 4 站点全杀，但探针自己暴露两个盲点（比修复更值得记）**：
    - M1（交换 `setBarSpace`/`setOffsetRightDistance` 顺序）**第一次只杀掉旧用例、没杀掉新的 09:33 例**：分时恢复路径会 fit **两次**，第二次在已稳定的 zoom 下把第一次的 skew 洗掉了 ⇒ 把偏移建成「状态」还不够，要建成**带当时 zoom 的有序日志**（`offsetCalls: {distance, atSpace}[]`）并断言「每一次 offset 都落在最终 zoom 上」。补后 M1 红 `expected 8 to be 50`。
    - 重跑 M1 时出现过一次「29 例全绿」的**假象**：`Select-String` 回显证明磁盘上确实是变异态 ⇒ 上一轮 vitest 读的是**尚未落盘**的旧文件（此前 `SearchReplace` 报过 save failed 但延迟写入）。判据：**跑变异要在同一条命令里先回显磁盘内容再跑测试**（§四 第 52 条）。
    - M2（`fitOffsetRight` 恒 0）⇒ 5 例红（含 `expected 297 to be close to 25`）；M3（删 `ensurePeriodUnitLabels()` 接线）⇒ `expected [] to deeply equal ['en-US','zh-CN']`，靠一个**故意不被 `beforeEach` 清空**的 `localePatches` 累加器绕开 once 守卫造成的用例顺序依赖；M4（退出分时不还右边距）⇒ 2 例红，红值 **203.2** 正是推导的 25.4×8。另：新增一个全局副作用（`registerLocale`）要**横扫所有挂载型测试替身**——`ProChartMinuteBars/Drawings/Paging` 三个替身不补 `registerLocale` 就会 TypeError。
  - **活体（真盘中 :5899，走真实代码路径）**：正常路径 81 根 `barSpace 4.61 / offsetRight 0.59px`（不夹顶 ⇒ 与 ㉘ 逐像素一致）；用 monkey-patch `window.fetch` 把 `session=latest` 截到 5 根，制造**真实的** capped fit：`barSpace 50 / offsetRight 114px`；对照旧行为（手动 `setOffsetRightDistance(0)+scrollToRealTime(0)`）首根中心 x = **25 vs 299** ⇒ 272~274px 空白落在左侧，与用户截图同形，A/B 决定性。借还闭环：日线 `10/80` → 分时截断（残差 114px = 2.28 根）→ 15 分 ⇒ `10/**80**`（不修会是 22.8px）。截图另确认标题已渲染成 `09988.HK · 1分钟`。现场已复原：撤销 fetch patch、切回分时、重载，会话桶写回 `{09988.HK, 1m, timeShare:true}`。
    - **量出来但决定不修的一条**：fit 之后 y 轴标注会变宽、把主图 canvas 反向挤窄（本机主图 372 + y 轴 55 = 427 恒定），分时读宽度的时机早 2px ⇒ 首根中心可能到 -1.8px。这是 ㉘ 就存在的既有误差、与本轮 300px 无关；真要修得给主图 widget 单独挂 ResizeObserver 并配 `applyPaneLayout` 那种 `<2px 就 continue` 的容差守卫，否则会和自己的 fit 打乒乓 ⇒ 记为 **D8（已知，量级 2px，动它需要先有容差守卫）**。D5/D6/D7 依旧有意不做（㉘ 已记），但 **D6 在本轮多一层代价**：盘中 MACD 副图整块空白会叠加「图没加载完」的观感，若用户继续报「不正常」需优先重评这一条。
  - 实测数字：`timeShare.test.ts` 27 → **32**、`klineLocale.test.ts` 2 → **4**、`ProChartTimeShare.test.tsx` 27 → **30**；前端全量 **87 文件 / 1190 用例**全绿（㉘ 是 1180）、`tsc -b --force` EXIT=0、`vite build` EXIT=0（`ProChart-CCFtI3FG.js` 427.82 kB / gzip 123.19 kB）。门禁 a/c/d/e/f 全绿、gate b 仍只命中未被跟踪的 `.qoder/repowiki/**`。**本轮未改后端**（0 次 pytest），且 `:8000` 的静态产物是逐请求读盘 ⇒ 用户**刷新即可**看到，不需要重启后端。
  - **上游冲突面：0 个官方文件**（本轮 `git fetch upstream` 后 upstream/main = `a44ed6e8`；11 个改动路径 `git ls-tree --name-only upstream/main -- …` **全部无输出** = 均为本地自定义文件；`git merge-tree --write-tree HEAD upstream/main` **EXIT=0 / 零 CONFLICT**）。明细：3 个源文件（`lib/timeShare.ts` +51、`lib/klineLocale.ts` +49、`pages/ProChart.tsx` +49/-6）、6 个测试（`timeShare.test.ts` +56、`klineLocale.test.ts` +61/-1、`ProChartTimeShare.test.tsx` +139/-12，另三个替身各 +1）、说明书 +4/-4、本文件，合计 **+415/-21**。不加依赖、不改 8 个语言包（`minute` 单位走 `registerLocale` 的 merge）、不改 lock/CSP。说明书 §15.2 那条「右边也不留空白（不会有 `16:06` 那种刻度）」**已被本轮推翻并改写**（现在如实写：一根最宽 50px、盘中残差留在右侧、右侧出现未来刻度是正常样子），借还那条同步补上「右边空白也是借走再还的」，FAQ 增一句「线从画面中间开始、左边空着 ⇒ 旧版本，刷新就好」。

- 本地自定义 ㉚说明：**「启动报错」= vite 代理在 `:8000` bind 之前抢跑，不是代码坏了**（2026-09-10 用户截图：vite 窗口 11:09:25-26 刷一屏 `http proxy error … connect ECONNREFUSED 127.0.0.1:8000`，路径是 `/sessions`、`/market/quote`、`/market/kline`）。
  - **判据是时间戳比对，不是读日志**：`Get-CimInstance Win32_Process` 显示 `python api_server.py` 的 `CreationDate = 11:09:16`，报错发生在 **11:09:25** ⇒ 进程活着、只是还没监听。`api_server.py:160` 在 lifespan 里 `await _run_startup_preflight()`，而 Uvicorn 是 **startup 跑完才 bind socket** ⇒ 窗口期实测 ≥10 秒（11:09:16 起进程，11:09:26 仍在 refused）。复查时截图里那三条端点全部 200（`/market/kline…session=latest` 10206 字节），浏览器刷新后 103 根 / `barSpace 3.61` / `offsetRight 0.17px` / 无断连横幅 ⇒ 自愈，无需改业务代码。
  - **真正该修的是 `start.bat`**：它写的是「起后端 → `timeout /t 3 /nobreak` → 起前端」，3 秒必然小于窗口期 ⇒ 每次一键启动都会刷这片红。改成**就绪闸门**：轮询到后端真在回答再放行前端；超时只打 WARN 并**照样起前端**，不卡死。（第一版写的是「最多 90 次」，而屏幕写着 `up to 90s` —— 那句是假的，见下一条。）`start.bat` 未被 upstream 跟踪（本地自定义①，`git ls-tree upstream/main -- start.bat` 无输出）⇒ 零冲突面。
  - **本轮最值钱的一条工具事实（差点交付一个坏掉的启动脚本）**：`cmd` 读 UTF-8 `.bat` 是按**字节**定位的，**一条足够长的中文 `::` 注释行会让读取指针错位，行尾被当成命令执行**——干跑实测报 `'—那是启动时序，不是故障。这里改成轮询 ' is not recognized as an internal or external command`，并连带吃掉下一行 `echo`。所以 `.bat` 里的**注释一律 ASCII**（中文 `echo` 行是安全的，原文件已有先例）。判据：**改 `.bat` 必须干跑一次**（把 `start "…" cmd /k …` 两行替换成 `echo WOULD-START …`，否则要么真起两个窗口、要么根本不跑），并把 cmd 的 stdout 用 `> file 2>&1` 重定向后按 UTF-8 读回——直接管道给 PowerShell 会因多字节字符跨读取块边界而显示乱码，那是**取证的假象不是文件的毛病**（本轮差点据此误判）。
  - 双向验证：端口 8000（在跑）→ `backend ready.` 用时 1.3s；把端口改成没人听的 8123 → `RESULT=NOT_READY` / `[WARN] … starting frontend anyway.` 且第三步照样执行，两个 case 的 `not recognized` 计数 **0**。另：编辑工具落盘瞬间 `start.bat` 出现过 6 个裸 LF（第 52 条同族的延迟写入），交付前统一按 CRLF + UTF-8 无 BOM 重写并复验（37/37 CRLF、首字节 `40 65 63 68`、U+FFFD = 0）。本轮**未碰前端源码与后端**（vitest/tsc/build/pytest 0 次，无需重跑：改动面只有一个 `.bat`）。
  - **提交环节自己也被假绿了一次（第 52 条搬到 git 上）**：本沙箱里 `git.exe` 的启动会**间歇性**报 `程序 git.exe 无法运行: 拒绝访问`（不固定是哪一个，同一命令内可能前一个被拒、后几个正常）。后果比失败更糟：`git push` 根本没跑，但 `echo "PUSH_EXIT=$LASTEXITCODE"` 在管道后仍给出 **0** —— 本会话连踩三次。做法见 §四 第 54 条（重试循环 + 只看 `ls-remote` 的真 ref）。
  - **复查自己第一版时发现的一句假话：屏幕上 `up to 90s` 其实是 `up to ~272s`**。轮询预算我按「90 次」写、按「90 秒」说，而**本机一次「连接被拒」就要 ~2 秒**：对关闭端口 8123 连打三次 `Invoke-WebRequest -TimeoutSec 2` = 2.16 / 2.00 / 2.01 秒，`TcpClient.Connect` 也是 2.05 秒，`netstat` 里能看见 `SYN_SENT` 停在那儿 ⇒ **是 connect 层慢不是 HTTP 层慢，改 `-TimeoutSec` 救不了**。一轮 = ~2s 超时 + 1s sleep ⇒ 90 轮 ≈ **272s**。最终写法：`[Diagnostics.Stopwatch]` 按**墙钟**封顶 90s，每 500ms 查一次 `IPGlobalProperties.GetActiveTcpListeners()`（纯 .NET、无模块依赖，实测 183ms 命中 / 28ms 未命中，**零连接成本**），只在端口真 bind 上之后才付一次 `/api/health` 的语义确认。实测三个数：正例 **1 轮 0.7s**；把上限调成 8s 打关闭端口 → **8.2s 精确退出**（证明墙钟是真的）；bind 之后 **0.59s** 放行（旧写法最差要 3s）。同轮两次量到后端冷启动 → bind = **11.7s / 12.0s**，给「窗口期 ≥10 秒」补了第二个样本。
  - **同一轮里我自己造的两条新假绿（都不是 git 的锅，第 54 条的亲戚）**：① 用 `Start-Process $env:ComSpec -ArgumentList "/c `"$bat`" > `"$out`" 2>&1"` 跑干跑批次，cmd 的引号规则把重定向串咬掉 ⇒ **批次静默不跑**、输出文件是空的，于是「12 秒内没出现 `backend ready.`」以 `held-gate = True` 通过 —— **负向断言被「什么都没发生」满足，等于没断言**。改用进程级 `-RedirectStandardOutput/-RedirectStandardError`，并且**先断言批次真跑了**（转录里必须有 `[1/3]` 与结尾的 `[3/3]`、stderr 必须为空），后面的负向结论才算数。② 脚本开头写了 `$ErrorActionPreference='SilentlyContinue'`，真跑那一次 `Start-Process` 抛的异常被吞掉、下一行照样打印 `launcher spawned`，150 秒后留下一句 `8000 BOUND at +-63924638463.7s`（`$Tb` 是 `$null`，减法算出个天文数字）⇒ **凡是「我做了 X」的日志都必须跟着 X 的产物断言**；差值/比率出现量级荒谬的数字，先怀疑操作对象为空而不是机器坏了。最后用 `Start-Process -FilePath …\start.bat`（不加任何重定向，等价于用户双击）才真开出两个窗口，用户截图里那四条端点走 vite 代理全部 **200**（`/market/kline…session=latest` 14874 字节）。

- 本地自定义 ㉛说明：**「分时数据可以直接从 futu openD 直接获取，查下现在获取的逻辑是否正确，分时副图显示 vol，并且要有添加或者更换指标功能」**（2026-09-10 用户原话，一句话里三件事：① 核验取数 ② 分时副图 = 成交量 ③ 副图可加可换；第三件顺带拍掉了 ㉘ 挂起的 **D6**）。
  - **① 核验结论：取数链路是对的，本轮 0 改后端**。分时 = `1m` + `session=latest`，分钟线先问 `_futu_minute_bars`（`agent/src/api/market_routes.py:263`），只有三种情形返回 `None` 落回新浪：`adjust != "qfq"`（`FutuLoader` 钉死 `autype="qfq"`，给错口径不如不给）、标的不在这四类后缀、网关没应答；**A 股以外没有第二条路**，直接按因由报错而不是「你去看看 OpenD 在不在跑」。活体：`/market/kline?symbol=09988.HK&interval=1m&count=800&adjust=qfq&session=latest` → `source=futu:opend`、**331 根**、`session=2026-09-10`、`prev_close=109.8`、**`volume==0` 的根数 = 0**（均价线不会整段贴 0），端口 11111 在听。「点亮一次分时只发一个请求」也被读实：800 根 ≈ 两个交易日，`_split_latest_session`（`market_routes.py:466`）从同一批里切出当天 + 上一交易日末根当昨收，**不为昨收再发一笔**。
  - **②③ 副图从「两行硬编码」变成状态**：新增 `lib/subIndicators.ts`（19 项目录 + 每视图持久化 + `withAdded/withReplaced/withRemoved/withDefaults` 纯函数）与 `components/charts/SubIndicatorPicker.tsx`（工具条那一行管理条），`ProChart.tsx` 的 `syncSubPanes(chart, wanted)` 取代旧的「挂载时 `createIndicator(VOL)` 再 `createIndicator(MACD)`」。默认 `{kline:[VOL,MACD], timeShare:[VOL]}`（**K线逐字不变，只有分时换默认**），存储 key `pro-chart.subIndicators.v1`，上限 `MAX_SUB_PANES = 6`。
    - **核心语义（下一轮改这块前必读）**：**每视图那条有序列表是权威**，`syncSubPanes` 在 init effect、`setView`、picker 三处各重铺一次 ⇒ 「面板被关了但列表还要它」这个状态**本应用用户造不出来**（从 picker 关就同时改集合）。`ProChartDrawings.test.tsx` 那条既存的「副图关了就不上图」正是死在这个新不变量上，**改的是测试**（`closePane` 现在先走 picker 关、再手工 splice 面板）而不是把产品改回说谎；详细判据见 §四 第 56 条。
    - **19 项 = 库自带注册表减去 `series:"price"`**（`AVP/BOLL/BBI/EMA/MA/SMA/SAR` 是主图叠加，`MA` 还归 `syncPriceOverlay` 管，放它进副图清单 = 两个主人抢同一块面板）。清单**读自 dist 的 `name:`/`series:` 对**而不是文档页；`subIndicators.test.ts` 再拿 `getSupportedIndicators()` 逐个反查——**目录里一个库没注册的名字，就是一个点了什么也不会发生的按钮**。
    - **「换」不能原地替换，这是设计不是偷懒**：面板地址由指标名派生（`paneLayout.subPaneIdOf` ⇒ `sub:MACD`，⑲ 靠它让画线跨 reload 认路），把 RSI 挂到 `sub:MACD` 上会让那条面板上所有画线的「归属副图」标注说谎，而 klinecharts 10.0.3 **没有面板重排 API**。⇒ 换 = 关旧 + 追加到末尾，代价（位置变了）写在按钮 title 里，不叫用户事后自己发现。
    - **`syncSubPanes` 只碰 `BUILTIN_SUB_NAMES`**：用户脚本（`UCI_*`）、`MA`/均价/昨收都不在它权限内——「同步副图清单」顺手删掉别人的脚本是本轮最容易写出来的事故（P2 变异专杀它）。另：`addSub` 里那句「副图最多 6 个 — 先关掉一个再加」实际**点不到**（picker 已把按钮 `disabled`），留着只当不变量守卫 ⇒ 说明书没照抄它（§四 第 59 条）。
  - **两次假绿，都是自己的替身在撒谎（比修复更值得记）**：① 假图的 `h.mounted` 挂在 **helper 全局**上，而 `init()` 第二次调用（StrictMode 双挂载）做的是**新 chart** ⇒ 上一世代的 VOL/MACD「已在图上」，一条都不创建，**既存 ㉘ 那条预算用例当场被打破**；② 同处 `getPaneOptions()` **忽略 `id` 形参**、`getIndicators: () => []` 永远说「什么都没挂」。修法：`mounted`/`panes` 每实例一份并镜像到 `h`（镜像必须是同一个数组对象 ⇒ `removeIndicator` 由 filter 重赋值改成**原地 splice**），并把「是否已挂载」的判定从 `getIndicators({name})` 换成 `getPaneOptions(paneId) != null`（与 ⑲ `paneLookup` 同一个探针）。⇒ §四 第 57 条。
  - **实测数字**：`ProChartTimeShare.test.tsx` 30 → **42**、新建 `lib/__tests__/subIndicators.test.ts` **24**、`ProChartDrawings.test.tsx` 59（改契约不加数）；前端全量 **88 文件 / 1226 用例**全绿（㉙ 是 87/1190）、`tsc -b --force` EXIT=0、`vite build` EXIT=0（`ProChart-Bdo5AywN.js` **435.66 kB / gzip 125.48 kB**，㉙ 是 427.82/123.19）。变异 5 站点全 killed：P1 分时默认加回 MACD ⇒ 7 红；P2 去掉 `isBuiltinSubIndicator` 守卫 ⇒ 1 红（脚本用例抓住）；P3 `withReplaced` 改回原地替换 ⇒ 2 红；P4 `normalizeSubList` 放行未知名 ⇒ 2 红；P5 picker `pick()` 忽略 `target` ⇒ 1 红。另：探针脚本里那条 `Tests \d+ failed` 正则被 ANSI 码挡住而误标「SURVIVED-ish」——**结论要逐段读输出，不能只读自己写的正则**（§四 第 47 条）。
  - **活体（真浏览器逐节点验，DOM 数值取证不是截图）**：K线默认面板高 `[182,1,74,1,74,26]`、工具条「副图指标 · 2」、`localStorage` 初始 `null`；加 RSI → `· 3` + 存储 `{"kline":["VOL","MACD","RSI"],"timeShare":["VOL"]}` + `[182,1,49,1,49,1,49,26]`（**主图 182 不动**）；把 VOL 换成 KDJ → `["MACD","RSI","KDJ"]`（追加到末尾）；点分时 → `· 1` + `[211,1,120,26]` + badge「分时 2026-09-10 · 昨收 109.80 · -2.64%」；回 K线三套完整复原；「恢复默认」只把 K线打回 `["VOL","MACD"]`，分时的选择没被碰；控制台 **0 error / 0 warn**。顺带量到的两条：`.min-h-[360px]` 宿主与面板之间还有一层 wrapper（量的是 `host.firstElementChild.children`）；图例文字画在 canvas 上，DOM 里**本来就**搜不到。
  - **待用户拍板、本轮不擅自扩大范围的一条**：分时均价仍是 `Σ(close×volume)/Σ(volume)` 的**估算**。富途 `request_history_kline` 的返回列里**本来就有 `turnover`**（SDK `quote_query.py:1469-1477` 构造 kline 记录时就写进去，本项目 quote 路径也已经在用），是 `agent/backtest/loaders/futu.py:120` 的 `_normalize_frame` 裁到 `_OHLCV_COLUMNS` 时丢的。⇒ 说明书旧那句「该数据源不供成交额」**只对「本路由」成立，本轮已就地改成如实表述**（§15.2 与 §19 那条 FAQ）。要改用真成交额算 VWAP 得同时动 loader 列契约、`_bars_from_frame` 与前端 `averagePriceSeries`——**那是回测链共用的 schema，属另一轮**。
  - **上游冲突面：0 个官方文件**（`git fetch upstream` 后 upstream/main = `f9cb061b`；本轮 7 个改动路径 `git ls-tree --name-only upstream/main -- …` **全部无输出**；`git merge-tree --write-tree HEAD upstream/main` **EXIT=0 / 零 CONFLICT**）。明细：3 个新文件（`subIndicators.ts` 239 行、`SubIndicatorPicker.tsx` 227 行、`subIndicators.test.ts` 234 行）+ `ProChart.tsx` +225/-17、`ProChartTimeShare.test.tsx` +317/-10、`ProChartDrawings.test.tsx` +17/-12、`public/usage-guide.md` +22/-4。不加依赖、不改 8 个语言包（目录名直写中文，面板图例仍是库自己的）、不改 lock/CSP。门禁 a/c/d/e/f 全绿，gate b 仍只命中未被跟踪的 `.qoder/repowiki/**`。

- 本地自定义 ㉜说明：**「发现没有周K线，月K线，请补充」**（2026-09-10 用户原话。`/pro-chart` 的周期按钮原本六档 `1分/5分/15分/30分/60分/日线`，缺周月两档）。
  - **核心决策：周月不向数据源要，由后端把日线折出来**（`_fetch_calendar_bars`）。三条理由写进函数 docstring，各自都在否决一条「看起来更直接」的路：① **覆盖面**——凡日线能画的标的周月都能画，加密/外汇/指数根本没有源生周线（akshare 说 `weekly`、富途说 `K_WEEK`、Yahoo 链上无周线会一路掉进另一个市场的 loader）；② **复权口径唯一**——源生周线自带一套复权基准，工具栏那个「前复权」就会在两个按钮上意味着两件事；③ **翻页游标干净**——合并 bar 打**组内首日**的时间戳 ⇒ `before` 恒为组边界，日线那套契约不需要粗周期变体。状态行带 `+1W` / `+1M` 后缀，明说「这些线是折出来的」而不是「从哪台机器取来的」。
  - 后端三件套（`market_routes.py` +135/-3）：`_calendar_group_key`（周用 **ISO (isoyear, isoweek)**，跨年那一周 Mon 29–Fri 2 是一根且不与明年 week 1 撞号；日线时间戳本就是「交易日标签的午夜 UTC」⇒ 不需要时区表）、`_merge_to_calendar`（开=组内首日、收=最后一个交易日、高低=组内极值、量=求和、时间戳=首日）、`_fetch_calendar_bars`（按尺取日线 → 合并 → 丢左边界残组 → `[-count:]`）。分派与**路由白名单都从这几张表推导**（`market_routes.py:755` `interval not in _MINUTE_PERIODS and interval not in _AGG_DAILY_PER_BAR and interval != "1D"`），错误文案 `unsupported interval '1Y' (known: 1m/5m/15m/30m/60m/1D/1W/1M)` 也是拼出来的 ⇒ 没有第五份周期表可漂（§四 46 那一族）。`session=latest` 对粗周期**明确报错**而不是静默忽略：分时是从分钟线里切一个交易日，日线本来就一天一根。
  - **本轮唯一的真 bug 是「定尺的单位」，而它的症状是另一个缺陷**：`_AGG_DAILY_PER_BAR` 初版按**交易日**估（`1W→6`、`1M→24`），加密货币 7×24 就是每天一根 ⇒ 页偏短 ⇒ `more.forward=false` ⇒ 表现得和「历史翻到头了」一模一样，连取证脚本都会先接受这个解释。活体读数（2026-09-10）：`BTC-USDT interval=1W count=200` 只回 **176 根**、`interval=1M count=40` 只回 **32 根**。改成**日历上界 +1**（`{"1W": 8, "1M": 32}`）后 sizing 从经验值变成可证明的（哪个市场的日历都塞不满这个上界）；`_AGG_DAILY_CAP` 15000 → **16500**（正好在图表自身最深一页 `500×32+30=16030` 之上 ⇒ 只兜手搓请求）。⇒ §四 60。
  - **一处可达性自我否决**：按日历上界定尺后「满窗 ⇒ 合并出的组数 > count」恒成立 ⇒ `merged = merged[1:]` 那句守卫**当前是 no-op**（尾部 `[-count:]` 已经丢掉残组）。它真正的用处在「页短到仍含残组」时（不丢就会把那一组的早期日子永久落在两页之间）。处置：代码注释如实写明「当前常量下不触发」+ 新增 `test_the_guard_still_fires_when_the_page_itself_is_short` 用 `monkeypatch.setitem(_AGG_DAILY_PER_BAR, "1W", 6)` 把那条路径**主动抵达**。⇒ §四 61（B1 变异第一轮就是因为这条没写而 SURVIVED）。
  - 活体取证两层：① HTTP **30 条断言全绿**（`BTC-USDT 1W` 翻页 **200+200**、接缝 `2022-11-07 → 2022-11-14`；`600519.SH 1W` **500+500**、接缝 `2016-11-28 → 2016-12-05`；`09988.HK 1M` 60+23 合 83；最新一周从它第一个有交易的交易日开（`2026-09-07` 那周 = 4 根日线）；`1Y` 被拒并带回那份 known 清单；`count=8` 被 `ge=10` 拦住；`before=0` 与 1D **行为一致**（falsy → 最新页，既有契约不改）。② 真浏览器 :8000（先 build 出 dist）：状态行 `futu:opend+1W` / `backtest:loader_fallback_chain+1W`、500→1000 根、`before=1480896000000` **恰等于最旧一根时间戳**、1000 根 **0 重复**、第三页 24 根后 `forward=false`、月线 242 根且 `more` 两侧 false、刷新恢复 `{symbol:"600519.SH",interval:"1W"}`、刻度 `2026-05-04` / `2024-07` 用的是库内置 locale ⇒ **没改 8 个语言包**。
  - **首轮 11 条「红」全部是校验器自己的错期望或既有行为，不是产品**：周间隔 15/11/14 天 = 春节与国庆**整周休市**（真不变量是「一个 ISO 周一根 + 严格升序」，不是「间隔 ≤9 天」）；`BTC/USDT` 404 是**标的拼错**（同一个 URL 换 `1D` 也一样 404 ⇒ 既有行为，页面 ticker 用 `BTC-USDT`）；`09988.HK 1M` 第二页 0 根 = 公开日线链只到 **2019-11**（83 个月）的**源深度**；错误体字段是 `error` 不是 `detail`。判据照旧：**先问「这条红描述的是产品还是我的探针」**（§四 39 同族）。
  - 前端：`marketApi.ts` +43/-5（`IntervalKey` 增两档、`INTERVALS` 增两按钮、**新增 `CALENDAR_PERIODS` 单表** + `isCalendarInterval`/`intervalToPeriod`，`periodToInterval` 改成读同一张表；非分钟（`second/hour/year`）**降级 1D**，而不是从年周期上读 `span:1` 去要 1 分钟线）、`ProChart.tsx` +14/-11（删本地 `periodFor`，两处 `setPeriod` 改走 `intervalToPeriod`；`intervalAllowed` = `isCalendarInterval(interval) || canMinuteBars(symbol)`；tooltip 第三条「日线链：周线/月线把日线按自然周/自然月合并，复权口径与日线一致」）。**`PAGE` 仍是 500**：一页 500 根周线 ≈ 10 年、月线 ≈ 40 年，客户端**不乘倍数**（乘数只会把上游的日历问题搬到自己身上），`ProChartTimeShare.test.tsx` 钉住「AAPL 点周线仍发 `count:500`」。`ALERT_INTERVALS`（`lib/alertsApi.ts:19`）**有意不动**：本轮只补图表缺的两档，不擅自扩大报警面。
  - **三个兄弟测试文件里那份手抄的周期表一并干掉**（`ProChartDrawings` / `ProChartPaging` / `ProChartMinuteBars`）：原本 `vi.mock("@/lib/marketApi", () => ({ INTERVALS: [{ key: "1D", … }], periodToInterval: () => "1D", fetchKline: 假的 }))` —— 工具栏真长出两档而替身只递上一条假 `1D`，这些页面级测试**看不见新增档位也不会变红**。改成 `async (importOriginal) => ({ ...await importOriginal(), fetchKline: 假的 })`，只有传输层是假的。⇒ §四 63（同族：`ProChartTimeShare` 第一轮那条 `expected 1 to be +0` 也是替身自己发的请求——挂载时 `setSymbol` 会带默认 day 周期跑一趟 loader，所以上限只能断言「**没有新增** 1D」）。
  - 数字：后端新建 `tests/test_market_kline_coarse.py` **25 例**（分组键/OHLC/接缝/升序/定尺/残组/session 拒绝/source 后缀/查询校验）；前端新建 `marketApiPeriods.test.ts` **14 例**、`ProChartTimeShare.test.tsx` 42 → **46**（请求形状那三条）、`ProChartMinuteBars.test.tsx` +50/-0；前端全量 **89 文件 / 1250 用例**全绿（㉛ 88/1226）、`tsc -b` + `vite build` EXIT=0（`ProChart-BSTbYYY_.js` 435.97 kB / gzip 125.62 kB，㉛ 435.66/125.48）。后端全量 `pytest tests -q` 两次分别 **19 / 18 failed**、13256 passed、113 skipped、11 errors、426 s —— 18 那一跑与 §四 30 的基线集合**逐条同形**（9 symlink + 8 gbk 解码 + 1 thin-assembler），多出的第 19 条是 `tests/test_rate_limiter_memory.py` 的时钟 flake（隔离跑 **9 passed**）。门禁 **a/c/d/e/f ok**，gate b 仍只命中未被跟踪的 `.qoder/repowiki/**`；跑门禁仍须 `& "C:\Program Files\Git\bin\bash.exe"`（PATH 上的 `bash` 是 WSL 占位程序，§四 42 已在）。计时：A股 1W/1M ≈0.9 s、AAPL ≈1 s、BTC ≈5 s（它 1D 自身 3.34 s）。
  - 说明书：§15.2 六档 → **八档** + 合并语义三条理由 + `+1W`/`+1M` + 「整周休市就没有那一周的线」+ 实测深度（A股约 20 年 / 茅台到 2006-08、港股到 2019-11、加密 = 它日线的全部历史）+ 一页时间跨度；分时段落里「六个周期按钮」改「八个」，并补「分时**不多占一档周期**」；§19 新增 FAQ「周线怎么少了几周 / 周月为什么翻不到上市第一天」。
  - **变异 8 站点全 killed（含三条前端）**：B1 残组不丢 ⇒ 1 红；B2 `high` 取最后一日 ⇒ 1 红；B3 改回按交易日定尺 ⇒ 3 红（且复现活体那两个错数字 176/32）；B4 组内末日打戳 ⇒ 4 红；B5 不再拒绝 `session=latest` ⇒ 1 红；F1 `1M` 画成 day ⇒ **5 红**；F2 粗周期不放行 ⇒ **7 红**；F3 非分钟掉进 1m ⇒ 1 红。探针驱动自己那四个坑（比这 8 条结论更值得记）见 §四 62。
  - **交付环节：推送循环六轮全被拒，而它第一次真拦住了人**。`git push` 回 `! [rejected] main -> main (fetch first)`，本地 `origin/main` 跟踪 ref 停在 `a5195ba0`，所以 `git rev-list --left-right --count origin/main...HEAD` 报的是 `0 / 2`（“落后 0、领先 2”），而 `git ls-remote` 里远端已经是 `efad78b1` —— **每日 sync 工作流在北京时间 20:00 把上游合进来了**（那批内容：README 六语 + `stooq_loader` + `strategy_store/adaptation` + 四个后端测试文件）。带收敛判据的循环六轮都报 `CONVERGED=False` 就停手，**没把“远端还在动”误当成 git.exe 被拒而反复重试**（§四 54 那条判据第一次真正起作用）。处置：`git fetch origin` → 按 §四 34 `git rebase origin/main`（**零冲突**，两个提交线性叠上）→ **在新树上重跑**：后端全量 **18 failed / 13276 passed / 113 skipped / 11 errors / 388 s**（passed 比 rebase 前多 20 = 上游自己新增的测试，失败集合仍是 §四 30 那 18 条同形）、门禁 a/c/d/e/f ok / gate b 存量。前端：`git diff --name-only a5195ba0 origin/main -- frontend` = **0 个文件** ⇒ 上面那 89 文件/1250 例是在**字节相同的树**上跑的，另在新树上复跑本轮那 5 个页面级文件 **164 例全绿**。（取本地与远端 ref 时又踩一次 §四 54 的 `git rev-parse --short A B` ⇒ `Needed a single revision`，要逐个取。）
  - 上游冲突面：**0 个官方文件**（`git fetch upstream` 后 rebase 前 upstream/main = `f9cb061b`，与 ㉛ 同一个 ⇒ 本轮无新上游代码合入，只有每日 sync 把同一批合进了 `origin/main`；11 个改动路径 `git ls-tree --name-only upstream/main -- …` **全部无输出**；`git merge-tree --write-tree HEAD upstream/main` **EXIT=0 / 零 CONFLICT**，该数取于 rebase 前）。明细：2 个新文件（`agent/tests/test_market_kline_coarse.py` 386 行、`frontend/src/lib/__tests__/marketApiPeriods.test.ts` 79 行）+ `market_routes.py` +135/-3、`marketApi.ts` +43/-5、`ProChart.tsx` +14/-11、四个测试 +169/-31、`public/usage-guide.md` +10/-5、本文件，合计约 **+836/-55**。不加依赖、不改 8 个语言包（周月刻度与单位字都是 klinecharts 自带的 `week:'周' month:'月'` 与 `YYYY-MM` / `YYYY-MM` 格式）、不改 lock/CSP、不动 `api_server.py`（第 31 条那条 400 行边界）。


- 本地自定义 ㉝说明：**「按照你的建议做，不建议做的不做，设计好后自我操作，自我验证，不需要经过我同意」**（2026-09-12。用户给的是**授权令而不是需求**，所以本轮的验收标准变成「上一轮我建议了什么、有没有偷做不建议做的」；iFinD MCP 从「能接」推进到「在架构里站得住」。）
  - **落地面**：`agent/backtest/macro_series.py`（773 行，EDB 回包解码 + 单表解析 + 中文单位归一 + 落盘缓存）、`agent/backtest/macro_regime.py`（751 行，因果滞回状态机）、`agent/src/tools/macro_indicator_tool.py`（596 行，`read_macro_indicators` / `fetch_macro_indicator` 两个工具）；新建测试 60 + 39 + 42 = **141 例**（`test_macro_series.py` 556 / `test_macro_regime.py` 597 / `test_macro_indicator_tool.py` 723 行）；fixture 3 份（`tests/fixtures/ifind/*.json`，3743B / 870B / 1759B）**逐字节取自真实回包** —— 手写 fixture 测的是我对形状的记忆，不是形状本身。
  - **「不建议做的」一条没做**：K 线主链、实时行情替换、全市场因子面板三项全部不碰；`runner.py` / `backtest/regime.py` / `engines/` / `api_server.py` **零改动**（那条 400 行边界没被顶，§四 31 仍红在原地）。
  - **额度即架构**（iFinD 免费档 2000 次**总量**、2 QPS）：① 任何自动链路（回测、REST、regime 计算、swarm worker）**永不**联网打 iFinD，取数只能由 Agent 显式调 `fetch_macro_indicator` 并落盘；② 工具层 cache-first —— 新鲜缓存命中时代码路径**根本不构造 adapter**（测试断言 `stub_adapter["built"] == []`，而不是「返回了缓存」）；③ 每个回包都带 `quota_spent` 明说这次有没有花钱，`_error()` 默认 `False`、只有真发过请求的分支覆盖成 `True`（写反的方向会让 agent 以为白赚一次而重试）。
  - **因果性怎么被钉住的**：availability date = 报告期末 + 发布滞后（M=18d / Q=45d / D=1d），trailing 滑窗 + 双阈值 Schmitt 滞回（enter 0.34 / exit 0.12）。两条反直觉的取证常识：pandas **偶数窗** `center=True` 与 trailing 等价 ⇒ 要暴露泄漏必须用奇数窗；**单调数据**区分不了 trailing/centered ⇒ 测试序列必须含转折点。
  - **两次设计转折（本轮真正的产出，两个结论都比第一版方案更对）**：
    ① **env 开关写完又整份删掉**。原设计 `VIBE_TRADING_IFIND_MACRO_ENABLED` 默认关。删它的理由不是审美：`test_preset_honesty.py::test_every_granted_tool_resolves` 与 `test_swarm_presets_packaging.py::test_every_preset_tool_exists_in_local_registry` 都用**不带 `agent_config` 的注册表**解析 bundled preset 的白名单，于是「默认关、靠 env 打开」与「进任何 bundled preset」**逻辑上不可共存**。改成与 `fred_macro` 同范式：免费工具无条件注册，计费工具的 `check_available()` = 「存在一个 `enabled_tools` 允许 `get_edb_data` 的已配置 MCP server」。`env_schema.py`（那 11 行）与 `test_readme_counts.py::_CREDENTIAL_GATES` 全量回滚 ⇒ `git status` 里两者零 diff。
    ② **不做 SKILL.md、不新增 preset**。一个 bundled skill 会牵动 6 份 README 的 badge + 7 处 prose + 类别表 + 树行（§四 67），而 `macro_strategy_forum` / `macro_rates_fx_desk` **早就在仓库里** ⇒ 再加第 31 个同类团队是重复品。正解是给既有节点补它缺的那件工具：`domestic_economist`（框架写着 CPI/PPI/PMI/GDP/M1/M2/社融，手上却只有 `read_url` 和「Tushare patterns」可以凑）、`chief_strategist`、`rates_analyst`（同一份 PBOC LPR/MLF/RRR 清单）三个节点 grant `read_macro_indicators`，presets 计数 **30 → 30 不动**。
  - **preset 文案被门禁反向塑形**（下一轮写 prompt 前必读）：`test_every_tool_named_in_a_prompt_is_granted_to_that_node` 取 `build_registry().tool_names` 里所有含下划线的名字，对 `system_prompt` 做 `(?<![\w-])name(?![\w-])` 扫描 ⇒ 计费工具 `fetch_macro_indicator` **既不能 grant（无密钥世界解析不出）也不能在 prompt 里提一句（提了就要求 grant）**。三个节点的文案因此只能写「reader 免费但永不取数；没缓存就报没缓存，编一个数字比留一个空白更糟」，把「取数是运维方的显式动作」这条知识留在工具描述里而不是 preset 里。
  - **真数据活体（合计只花 1 次请求，`.qoder/tmp/ifind_live_check.py`）**：`fetch_macro_indicator("中国 社会消费品零售总额 当月同比 2025-01至2026-06 月度")` → `ok` / `quota_spent=true` / `cached=true`、`index_id M001657195`、`unit %`、`freq M`、**14 点**、`data_source 国家统计局`、`publication_lag_days 18`；同一条 query 第二次跑 → **`quota_spent=false` 且 adapter 一次都没建**（cache-first 在真数据上成立，不是只在桩里成立）；`read_macro_indicators(as_of="2026-03-31")` 给出 `current.date = 2026-01-18`（= 2025-12 期末 + 18 天），**2026-06 那些读数被正确挡在门外** ⇒ 因果性在真数据上成立；`warnings` 如实写着 `breadth rests on 1 voting indicator(s); below 3 the gauge is a coin flip, not a consensus`。单指标演示是刻意的：花 3 次请求去刷一个好看的 breadth 数字，不如让工具**自己承认**浅。
  - **一个实现真 bug（不是测试 bug）**：`breadth_from_votes` 把 `pd.Timestamp(now)` 直接和 naive 时间线比较 ⇒ 调用方传 aware datetime（`datetime.now(timezone.utc)`，也就是任何生产调用方）当场 `TypeError`，而**既有测试全部只喂 naive** 所以带着它通过。修法在实现层：新增 `_naive_utc()`（aware 先 `tz_convert("UTC")` 再 `tz_localize(None)`），`regime_on_date` 对 aware 输入先转 UTC 再取日历日（否则 `str()` 打印的是本地日，JST 08:00 会被读成当天）。补 3 条 tz 回归 + 3 条变异。⇒ §四 65。
  - **变异检验三层全零存活**：series 6/6、regime 16/16、工具 18/18（探针 `.qoder/tmp/ifind_mutation_probe*.py`；anchor 不唯一 / 未命中单独报，绝不当「killed」算）。工具探针里那条「read 不再复查闸道」随 env 开关一起作废，换成「`enabled_tools` allowlist 不再过滤」——**同一条测试意图（不能对服务不了 EDB 的 server 假装能服务）换了个还活着的锚点**，而不是删掉让计数好看。
  - 全量 `pytest tests -q`：**18 failed / 13417 passed / 113 skipped / 11 errors / 406 s**，失败集合与 §四 30 那份 27 个 id 的基线**逐条同形**（symlink 10 条报 `WinError 1314 客户端没有创建符号链接的权限`、gbk 解码 8 条含 `test_keyring_is_not_a_core_install_dependency`、`test_api_server_is_thin_assembler` 1 条；errors 11 条 = `tests/factors/test_registry.py` 的 `OSError`），**读的全是我从未触碰的文件**，按第 30 条的规矩不逐条重查。passed 比 ㉜ 的 13256 多 161 = 本轮 141 + 上游新增。门禁：gate e（AST 版 `python tools/ci_env_var_gate.py`）**rc=0**（两条 WARN 是 `src/providers/llm.py` 存量）；gate a / f 静态扫描 0 命中；本轮新文件**不含任何 `os.getenv` / `os.environ` 读取**（缓存根经 `src.config.paths.get_runtime_root()`）。`C:\WINDOWS\system32\bash.exe` 仍是 WSL 占位、在本仓库路径上跑不动 ⇒ 六道门逐条等价执行（第 42/47 条老规矩仍然有效）。
  - 文档连带面：**只有一处** —— keyless 注册表 107 → **108**（`read_macro_indicators` 常注册），六语 `│ │ ├── tools/ # N` 树行同步。同步脚本 `.qoder/tmp/sync_readme_tool_count.py` 的锚点**与门禁正则同源**（`(│\s+│\s+├── tools/\s+# )\d+\b`），且读写两侧都 `newline=""`（`Path.read_text` 会把 CRLF 归一成 LF ⇒ 一个数字改成整文件 diff）。MCP 那份 74 条工具清单**不动**：`mcp_server.py` 是显式清单，本地新工具不会自动出现在里面（跑 `test_readme_counts.py` 亦证实）。整仓 checkout 是 LF（未改动的 preset 也 0 CRLF），git 那句 `LF will be replaced by CRLF` 只是 `core.autocrlf=true` 的通用提示，非本轮引入。
  - 密钥纪律与扫描：真 token 只存在于 `.qoder/tmp/ifind_key.txt`（被 `.git/info/exclude` 忽略）与进程环境变量；提交前对**全部待提交内容**做三类扫描（真 key 子串 / `Bearer\s+[A-Za-z0-9\-_.]{20,}` / `api-mcp\.51ifind\.com` 域名），唯一命中是测试里那枚「断言不得被回显」的探针 token（**假值**，但长得像真凭据、对人和对扫描器都是噪音）⇒ 改写成 `Bearer INVALID-TEST-VALUE-NOT-A-CREDENTIAL` 后 42 例复绿。活体脚本自带 `assert KEY not in text`，任何一次回显即失败退出。
  - 上游冲突面：**本轮 0 新增**。两个被改的 preset 与 `upstream/main`（本轮从 `f9cb061b` 前进到 `f84b2977`）**逐字节相同** ⇒ 结构上不可能冲突；6 份 README 的 `5/3` 差异取的是 `git diff --numstat HEAD upstream/main`（HEAD **未含**本轮改动）⇒ 存量。`git merge-tree --write-tree HEAD upstream/main` 的 2 处 CONFLICT 是 `agent/backtest/loaders/registry.py` + `agent/tests/test_registry.py`（㉒ 的存量，本轮未触碰）。`origin/main = HEAD = ac0dd24f`、`git rev-list --left-right --count origin/main...HEAD` = `0⇥0` ⇒ 提交基线干净，无需 rebase。

## 四、已修复的问题（勿重复排查）

1. `api_server.py`：`python api_server.py` 直接运行时模块名为 `__main__`，需 `sys.modules.setdefault("api_server", ...)` 才能注册路由
2. `vite.config.ts`：前端代理默认 8899，后端实际 8000，已改默认值
3. pip 安装需用清华镜像（网络原因）
4. Windows PowerShell 下用 `npm.cmd` 而非 `npm`
5. Git 浅克隆（--depth=1）会导致 force push 失败，需先 `git fetch --unshallow`
6. 同步工作流曾连续失败（2026-08-15 修复）：官方提交含 .github/workflows/ 变更时推送被拒。验证过无效的方案：声明 workflows: write（校验器不认）、仓库默认权限改读写（GITHUB_TOKEN 被硬性限制）。最终解法：仓库密钥 SYNC_TOKEN（经典 PAT，需勾 repo + workflow 两个范围），工作流 checkout 用 `token: ${{ secrets.SYNC_TOKEN }}` 注入推送凭证
7. py_mini_racer（akshare 新浪接口依赖）在 Windows 子线程首次使用时会 V8 FATAL 崩溃整个 Python 进程（现象：后端忽好忽坏后直接退出）；任何新浪/mini_racer 调用必须收敛到单一专用线程串行执行（见 market_routes.py 的 `_run_sina`，2026-09-04）
8. 东财 push2 系被本机代理拦截 + tencent 日线接口高频限流（HTTP 501）：A股日线首选新浪 `stock_zh_a_daily`（qfq），分钟线首选 `stock_zh_a_minute`（均已在 /market 路由落地并压测验证）
9. 生产模式（:8000 服务 dist）CSP 为 `script-src 'self'`（`agent/src/api/security.py`，有单测锁定），**任何 `new Function`/`eval` 都会 EvalError**；dev（vite）无此限制，故“dev 能跑 prod 不行”先查 CSP 再查代码（⑩ 公式引擎因此必须用解释器）
10. React StrictMode 双挂载是本页面类功能的通病（⑩：boolean 守卫被首个已 dispose 的 chart 消费掉，指标不再恢复；⑪：`?s=` 深链接静默失效）：一次性状态不能放 boolean，要么用实例键（WeakSet）要么提到模块作用域并共享 in-flight Promise；dev 与 prod 行为不一致时两边都要实测
11. KLineChart 自定义指标**每个 figure 必须带 `type`**（line/bar/circle/text/area）：`prepareIndicatorFigures` 会丢掉没有 type 的 figure，线、图例、右侧末值标记三处同时消失且**不报错**——“数据算得出、图上画不出”先查这个（⑫）
12. 标准差一律用**总体口径 ÷n**（Pine `ta.stdev` 的 `biased` 默认 true、TA-Lib BBANDS、KLineChart 内置 BOLL 同口径）；两套公式引擎（Pine / 向量）必须同口径，否则同一个布林带在主图上是两套带（⑫）
13. KLineChart v10 DataLoader：**`forward` = 要更早的 bar（前插，不动视图），`backward` = 要更新的 bar（追加，且按条数平移视图）**，与英文字面直觉相反；把 backward 当「往更早翻页」回答会交付重复块 → 视图被挪 + `more.backward` 恒真 → 「拖一下弹回原位、停不下来」。答复必须只含图表没有的 bar（⑬，见 `klinePaging.ts`）
14. KLineChart v10 **面板高度**：除主图外每个 pane 取固定 `height`（默认 100），主图是 flexible pane **只拿余数**（dist 14787-14830）⇒ 360px 的图 + 3 个副图指标，主图实测只剩 **29px**；而 overlay 落点 `value` 由**接收鼠标事件的那个 pane 的 y 轴**算出（dist 8798-8811）⇒ 「价格线」存成成交量量级、线画在看不见的地方。任何「画线没反应 / 画出的线找不到」先量 `getDom('candle_pane','main')` 的高度（⑭，见 `paneLayout.ts` + `chartDrawings.ts`）
15. KLineChart v10 两个 API 陷阱：`removeIndicator(filter)` 传**字符串**会被当成空过滤对象 ⇒ **一次删光图上全部指标**（必须传 `{id}` 或 `{paneId, name}`）；`createIndicator(value, isStack?)` **没有** v9 的第三个 paneOptions 参数，副图高度只能事后 `setPaneOptions({id, height})`，且该调用内部是 `height = max(minHeight, height)`（minHeight 默认 30，15422-15423）（⑭）
16. KLineChart v10 **画线的事后编辑不回传**：拖动改位（`pressedMouseMoveEvent` 原地写 `overlay.points`，dist 8632-8654）和右键删除（`_figureMouseRightClickEvent` → `removeOverlay`，dist 8774-8790）都只经 `onPressedMoveEnd` / `onRemoved` 通知，且**回调必须在 `createOverlay` 时就传进去**——`restoreDrawings` 漏传就会得到「恢复出来的线改不动、删不掉，刷新全回档」。右键删线是库内置手势，UI 文案要主动告诉用户（⑮）
17. `StoreImp.removeOverlay` **先 `onRemoved`（dist 14467）后 splice（14475-14478）**：回调里读 `getOverlays()` 仍包含被删那条 ⇒ 快照与计数必须排除回调传回的那个 id（否则「删掉的线刷新复活」+「已画 N 条」说谎）；同理切标的时的 `removeOverlay()` 全清要先 mute，否则逐条回调会把**旧标的**的画线桶写成空。`destroy()` 用 `_overlays.clear()` 不触发回调，卸载期天然安全（⑮）
18. KLineChart v10 **图元样式优先级**：`{ ...defaultStyles[type], ...overlay.styles?.[type], ...figure.styles }`（dist 8955）⇒ overlay 层改色会被**图元自带 styles 盖掉**，导入外部指标/模板前必须先看它锁没锁 color；改一条线的颜色要 **line + point + text 三处一起**（默认价签白字蓝底，11744-11761），只改 line 得到「红线蓝签」像坏了一样；自定义样式片段必须带 `[key: string]: unknown` 索引签名才能通过 `DeepPartial<OverlayStyle>`（d.ts 486-495 / 1122），窄接口会被 `createOverlay` 以「缺少 string 索引签名」拒绝（⑯）
19. `overrideOverlay` 与选中态两个坑（⑯）：① 它走 `getOverlaysByFilter`（dist 14285-14298），`isValid` 只挡 null/undefined ⇒ **不传 id 就命中图上全部 overlay**（一次改色全图），传空串则一条都不命中（静默无效）；② 「用户点了哪条线」只能从 `onSelected`/`onDeselected`（8687-8702）拿，且 `setClickOverlayInfo`（14538-14557）是**先 deselect 旧、后 select 新** ⇒ 清选中必须按 id 守卫，无条件置 null 会吃掉用户刚选的那条。另：`shouldUpdate()` 用对象身份比 styles（8314-8318），所以 override 一定会重绘，「改了颜色没反应」不是这里
20. KLineChart v10 **`lock` 与 `visible` 完全不对称**（⑰）：`lock` 只在 `_figureMouseDownEvent` 开头被检查（dist 8731-8734）⇒ 锁定的线**仍可点选、仍可右键删**，它只防误拖；更阴的是 `shouldUpdate()`（8314-8318）**不比较 lock** ⇒ 纯锁定改动 `overrideOverlay` **已经生效却 return false**（14430-14458 只有 sort/draw 才 updatePane），凡按返回值判断成败/是否落盘必丢状态，要读回实例校验。`visible:false` 才是重绘开关（`OverlayView.drawImp` 8895-8899），且因为图元事件在绘制阶段注册（8612-8670），**隐藏会连带让线不可点** —— 想「藏起来但还能点」得自己实现（例如仍然绘制、只把颜色画透明）
21. 内嵌浏览器 harness 的两条硬限制与破解（⑰，影响一切 prod 取证）：① hidden 页 rAF 不跑 ⇒ klinecharts 画布既不缩放也不 `_listener()` 绘制 ⇒ **图元事件不注册** ⇒ 「点线选中/按住拖动」假失败；破解=手工执行 dist 9083-9107 的绘制体（重置 `_requestAnimationId`、按 dpr 设 `_pixelWidth/Height` 与 canvas 尺寸、`clearRect`+`scale`、调 `cn._listener()`）。② hidden 页 `setTimeout` 被钳到 ≥1s，既让 React 18 提交延迟（MessageChannel 让出不保险，动作后要用真 `setTimeout(300)` 再断言），也让 klinecharts 的 `Delay.ResetClick=500` 复位永不触发 ⇒ 第二击被当成潜在双击吞掉 ⇒ 多点画线工具画不完；破解=patch `window.setTimeout` 捕获那个 500ms 的 `bound` 回调并在每击前后手动调用
22. **导入外部文件 = 把别人的 bug 引进来的入口**（⑱，`drawingExchange.ts`；paneId 规则已被 ⑲ 收窄）：三条不可降级——① 落点必须带数值 `timestamp`（只有裸 `dataIndex` 的点直接丢，前插历史后会指到别的 bar，⑭）；② 文件里的 `paneId` **只认本项目自己发过的地址**（⑲：`isRestorablePaneId` = `candle_pane` 或带 `sub:` 前缀的稳定面板地址），其余一律改回 `candle_pane`——别人机器上的随机 `indicator_pane_<时间戳>` 落到本机必然不存在，交给库猜就是 ⑭ 那个隐形线 bug（⑰ 的 `drawingKey` 也因此**把 paneId 算进去**：同一组数字在主图与在 MACD 上是两条线）；③ 条目逐键重建而不是 `JSON.parse` 后直用（挡 `__proto__` 原型污染与多余键）。另：去重签名 `drawingKey` **不能含样式**，否则用户改过色后再导一次同一份文件就翻倍；坏条目要当 `skipped`（带「第 N 条：原因」）而非整体报错，一份手改的文件里丢一两条不该导致导不进其余的
23. 分享链接的两个使用端（⑱）：① `copyText`/`downloadText`（`workbench/types.ts`）**失败返回 false 不抛**——剪贴板在非安全上下文/内嵌 webview 里基本不可用，下载也可能被拦，UI 必须摊出一个只读链接框兜底，不能只弹个「复制失败」了事；② codec（`g`/`j` 前缀 + base64url + gzip）与**错误文案必须共享**（`ShareCodeError{stage}` + `shareCodeMessage()`），两边各写一份一定会漂（本轮重构就当场被旧断言抓住）；③ `extractPayload` 要能收「聊天工具截断后的裸码」：仍带 codec 首字母且不含 URL 标点的输入应交给解码器报出坏在哪一步，而不是谎称「链接里没有分享码」
24. KLineChart v10 **副图地址是随机的，且库会用「改写面板」掩盖问题**（⑲）：`createIndicator` 不传 paneId 时走 `indicator.paneId ?? createId('indicator_pane_')`（dist 15271）⇒ 每次挂载面板地址都变，存进画线桶的副图 id 下次刷新必然失效；而 `createOverlay` 遇到不存在的面板 id **不报错，静默改写成 `candle_pane` 并保留原 value**（dist 15364-15367）⇒ 表现为「线画成功了，刷新后跑到主图底部看不见」（⑭ 的隐形线从存储回流）。三件事必须一起成立：副图创建时传稳定地址（`sub:<指标名>`，见 `paneLayout.ts`）、上图前用 `getPaneOptions(id) != null` 问面板在不在（dist 15587-15594）、不在就 parked 暂存**绝不交给库去猜**
25. 库**删指标不会删住在它面板上的 overlay**（⑲，prod 才看得见）：工作台取消勾选关掉副图后，那条线仍留在 `getOverlays()` 里 ⇒ 隐形、仍被计数、连 parked 都没进，「清单 · N」当场说谎。凡「图上可见行数」是对外承诺的语义，就必须在面板集变化的入口主动 `parkOrphans`（移出画布 + 并入暂存）；单测里所有副图用例都走 `reload()`（会经 `restoreDrawings` 被 park），所以这类缺陷**只有不刷新路径能暴露**
26. 变异探针会**集体失明**并把「零执行」伪装成「全部 killed」（⑲，Windows 专属）：① `spawnSync("npx.cmd", …, {shell:true})` 跑 vitest 会崩在 jsdom 环境-setup；② 改用 `process.execPath` 直调 `node_modules/vitest/vitest.mjs` 后，若脚本里的项目路径写了**小写盘符 `e:/`**（Node 解析为 `E:/`），`@vitest/runner` 会被加载两份 ⇒ `describe()` 抛 `Cannot read properties of undefined (reading 'config')` ⇒ `numTotalTests:0`/`assertionResults:[]`。防线只有一条：**变异前先跑基线自检**（钉住文件未变异必须全绿且 total>0），并把 `total===0` / collection-error 判为独立三态 `HARNESS-BLIND`，否则一个断言都没跑也能汇报「N 段全部 killed」
27. 解码本项目分享码取证的两个必踩点（⑱/⑲）：① `?d=`/`?s=` 值的**首字符是 codec 前缀**（`g`=gzip / `j`=裸 JSON），必须 `slice(1)` 再 base64url 解（漏剥会在 atob 报 `InvalidCharacterError`，长度 mod 4 === 1 是关键线索）；② 取证脚本里任何 `String(x).slice(0, N)` 截断都会让后续解压报 `Z_DATA_ERROR incorrect header check`——那是**取证坏了**不是产品坏了；页面内解码用 `DecompressionStream('gzip')` 最省事
28. KLineChart v10 **最新 K 线右侧的空白区可以点，且越界时间戳被外推成“存在”的时间**（⑳）：`timestampToDataIndex` 遇到 `timestamp > lastTimestamp` 不报错而是外推（dist 13829-13843），绘制 x 就是 `dataIndexToCoordinate(timestampToDataIndex(ts))`（8905-8936）⇒ 画线能停在最后一根右边、价签写着数据里从未有过的日期（实测 `dataIndexToTimestamp(502)=2026-09-07`，甚至能落在周末），下一根 bar 进来还会左移。凡「落点必须在真实 K 线上」，就要在**绘制完成 / 拖动松手 / 载入 / 导入四个入口**各收一遍；只改 `timestamp`，**`value` 永远不动**；吸附后所有落点重合为一个点的线已无容身之处 ⇒ 删除并说明（判定必须要求「每个点都带 timestamp」，否则误杀只带 `dataIndex` 的点）。修法用 `overrideOverlay({id, points})` **原地改**（`OverlayImp.override` 的 points 分支 8277-8307 + `shouldUpdate` 比 points JSON 8314-8318 + `overrideOverlay` 刷 X_AXIS 14430-14458），删了重建会丢 ⑯ 的选中态与 ⑰ 的清单 id
29. 在 prod 里**手工模拟画线手势必须照抄库自己的顺序**（⑳）：真实完成路径是 `stepDrawingModeEventMoveForDrawing → nextStep → chartStore.progressOverlayComplete() → onDrawEnd`（dist 8502-8520），拖拽是 `startPressedMove → eventPressedPointMove/eventPressedOtherMove → onPressedMoveEnd`（8737/8638/8605）。少了 `progressOverlayComplete()`，那条线会一直挂在 `_progressOverlayInfo` 上，而 `getOverlays()` 会把它**追加进返回值**（14308-14311）⇒ 「removeOverlay 之后它还在」「它还把落点写进了存储」全是脚本造的假缺陷。另：`overlay.points` 里残留的 `dataIndex` 不影响绘制（`_drawOverlay` 里 `timestamp` 优先级高于 `dataIndex`，8920 早于 8924），整条拖拽时还会被 `eventPressedOtherMove` 按 timestamp 重算（8420-8426）⇒ 修 x 只需改 timestamp
30. **本机（Windows）`agent` 全量 pytest 有一批与仓库改动无关的固有失败**（㉑ 用纯净副本对照实证）：同步上游前基线 = 50 failed + 9 errors / 12423 passed；2026-09-05 同步上游后 = 54 failed + 9 errors / 12604 passed（**失败数会随上游给自己的测试文件加用例而涨，不是本地引入的**）。失败类目：symlink（创建符号链接需特权，报 `OSError`）、`0o600`/父目录 fsync 断言（NTFS 无 POSIX mode）、governance 账本、telegram fence、`tests/factors/test_registry.py` 的 `OSError`。以后任何增量不要逐条重新排查，做法：把失败 id 列表抽出来，在纯净基线树里跑同一批，**比对集合而不是数量**（㉑ 实测同步前 `working=59 baseline=59`、同步后 `working=63 baseline=63`，回归均为 0 条）
   - **2026-09-06 重新基线（上游 56 提交合入后，本机全量实测两次一致）**：`18 failed / 12955 passed / 113 skipped / 9 errors`（364 s / 357 s）。旧的 54 条里 **governance 账本与 telegram fence 两类已被上游自己修好**（PR #1261 `fix(windows-cross-platform)`），剩下的 18+9 全部仍与本仓改动无关，类目只有三个：① **symlink 特权（10 条）**——`tests/api/test_positions_sectors.py`×3、`tests/harness_eval/test_schema_and_artifacts.py`×1、`test_state_migration.py`×1、`test_swarm_replay.py`×3、`test_swarm_retry_artifact_isolation.py`×1；② **上游测试自己 `read_text()` 不传 `encoding=`，GBK 默认码下直接 `UnicodeDecodeError`（8 条）**——`test_packaging_dependencies.py`×7 + `test_local_connections_hardening.py::test_keyring_is_not_a_core_install_dependency`（本质是上游 bug，不是本机环境问题，但也不是我们能推给下游的）；③ **`test_api_server_is_thin_assembler`（1 条）**，即第 31 条那条 400 行边界。`tests/factors/test_registry.py` 的 9 个 `OSError` 仍在。下一次比对就用这份 27 个 id。
31. `tests/test_api_infrastructure.py::test_api_server_is_thin_assembler` 要求 `api_server.py` **< 400 行**（上游有 `cb56dd2d fix: keep api_server.py under the thin-assembler line budget`，是真实契约），而上游 main 是 **397 行**（门禁绿）；本地 ⑧ 给 market 路由加了 3 行 ⇒ HEAD 恰好 **400 行 = 断言 `< 400` 的边界，门禁从 ⑧ 起就已为红**（不是环境差异，已用 `git show upstream/main:agent/api_server.py` 与纯净 HEAD 副本分别数过；2026-09-05 同步上游后该文件 blob 仍是 `520a7249…`，与同步前**逐字节相同** ⇒ 依旧 400 行依旧红）。㉑ 已把自有足迹压到净 +7 行（416→407）并删了 `AlertRuleRequest/AlertRuleResponse/get_alert_service` 的 F401 再导出（grep 证明无测试依赖），**没有为凑数删上游任何一行**。后续要再往 `api_server.py` 接线路由，只能挤自己的存量，不能再加行
32. 本项目新增页面的两条硬规则（㉑）：① **不新增 locale key**（八套语言包是上游文件，改一行就一个同步冲突面），一律 `t("alerts.xxx" as never, { defaultValue: "..." })`，而 defaultValue 里的插值**必须是 `{{var}}`** —— i18next 对单花括号原样渲染且不报错；页面级用例留一道总闸 `expect(document.body.textContent).not.toContain("{{")`。② testing-library 的 `getNodeText` **只拼接元素的直接文本子节点**，React 里 `{a}{b && c}` 会拆成多个文本节点 ⇒ `getByText("精确串")` 假红，要么用正则要么用 `toHaveTextContent`；另 `vitest.config.ts` 开了 `restoreMocks`，每个 `beforeEach` 必须重设默认 `mockResolvedValue`
33. 拉一份纯净基线树用 **`git worktree add --detach .qoder/tmp/wt-base origin/main`**，不要用 `git archive HEAD | tar -x`（㉑ 两轮都试过，worktree 完胜）：archive 走 PowerShell 管道会把 tar 当文本切碎（必须 `-o` 落文件），bsdtar 又解不动中文名的 skill 文档；worktree 是字节精确的完整检出。两个必踩的坑：① 基线树里 `src` 会被 editable install 指回工作树，必须显式 `$env:PYTHONPATH="<worktree>/agent"`（先用 `import src; print(src.__file__)` 验证解析到了 worktree）；② 对照要**按文件跑而不是按 node id 跑** —— 上游可能已把某个用例改名，而 pytest 遇到任意一个 not-found 的 node id 会 `exit 4` 整体退出、一条都不跑（表现为 `no tests ran`，极易误读成「全绿」）；把 id 列表写成文件时用 `Set-Content -Encoding utf8` 会带 BOM，读侧必须 `utf-8-sig`，否则第一条 id 就被 BOM 污染
34. 本地提交还没推、远端 main 已被每日 sync 工作流合入上游时：**`git pull`（默认 merge）会造出一个 merge 提交，而本地 ①~㉑ 一直是线性堆叠**，所以把未推送的那个提交 `git rebase origin/main` 叠上去更贴合仓库现状（rebase 未推送提交不改写任何已公开历史）。㉑ 实测 rebase 零冲突（上游那轮没动 `api_server.py`/`router.tsx`/`Layout.tsx`/`项目档案.md`，用 `git diff --name-only <旧基线> origin/main -- <这四个路径>` 先确认），但 **rebase 后必须在合并后的新树上重跑全部验证**，旧结论不迁移
35. **改「数据源优先级」有三个地方，只改 `registry.FALLBACK_CHAINS` 会看似改完实则无效**（㉒）：① `src/market_data.py::fetch_market_data` 的 `_fetch_via_chain` 里 `candidates = [src, *chain]` —— `src = detect_source(code)` 是**静态硬编码首选**，会被顶到第 1 位，新注册的链头只能落在第 2；所以真正的「优先」要改 `_preferred_source(code)`（新增）而不是只改链。② `src/api/market_routes.py::_fetch_daily` 对 `.SH/.SZ` 有一段**在进链之前就 return** 的 Sina 硬短路，不在此处插队则 A 股永远不走富途。③ 想用**配置**把一个新源加进 A股/美股链是**做不到**的：`MARKET_DATA_ORDER_*` 的 `is_valid_source_order` 是**多重集相等**校验（只许重排、不许增删），非法值会告警并保留默认。另：`resolve_loader`（回测 runner）只取 first-available，而 `correlation._fetch_price_series` 会**一路走到真的返回数据为止**，两条降级语义不同，不要互相推断
36. **富途 SDK 的实测契约（v10.10.7008，全部跑真机验过，文档与直觉都会骗人）**：`request_history_kline(..., autype='qfq', max_count=1000, ...)` 的 **`autype` 默认就是 `'qfq'`（前复权）**——不要因为「代码里没传复权参数」就断定拿到的是不复权价（本轮上一轮就犯过这个错，真缺的是 `PRICE_CALIBER_BY_SOURCE` 的登记）；枚举真名是 **`AuType` 不是 `RehabType`**（`futu.RehabType` 直接 AttributeError），合法值域只有 `{qfq, hfq, 'None'}`（注意 NONE 的值是**字符串 `'None'`**），传 `'front'/'none'/''` 一律 `ret=-1`；`get_market_snapshot` 返回 **2 元组** `(ret, df)`，而 `request_history_kline` 返回 **3 元组** `(ret, df, page_req_key)`——按 kline 的习惯解快照会 `ValueError: too many values to unpack`；`max_count=10000` 被接受；`futu-api` 不在仓库任何 lock 文件里，所以所有 `import futu` 必须延迟到函数内，否则没装 SDK 的环境连 import 都会炸
37. **熔断/降级层只能把「传输层故障」当作关于网关的证据**（㉒）：只 `OSError` 才 `note_failure`；被拒绝的那一批（坏代码/无权限）和本地处理异常（解包错、字段名拼错）都**不得熔断**——否则一个自己的笔误会把一个健康的 OpenD 挤出所有降级链 90 秒，比它掩盖的 bug 破坏性更大（用 `test_local_bug_does_not_latch_the_gateway` + `test_batch_refusal_does_not_latch` 两条钉住这个区分）。配套的两条硬规则：① 「网关不在」必须是**正常状态而不是异常**，所有入口（`fetch_snapshots`/`realtime_quotes`/`_futu_live_quote`）在故障时返回空值让调用方回落，不能让「OpenD 没开」变成一个失败的需求或一条死掉的报警规则；② **测试世界必须显式钉住「无网关」**（`tests/conftest.py` 把 `FUTU_HOST/FUTU_PORT` 设成未配置），因为 `FUTU_HOST` 默认 `127.0.0.1`、`FUTU_PORT` 默认 `11111`，开发机开着 OpenD 就会把探测答成 True——与第 30 条的 `MARKET_DATA_ORDER_*` 冲刷是同一类「开发者本机状态改变测试结论」的漏
38. **代码输入联想（type-ahead）不能用远程 suggest 接口，也不要再试**（㉓，2026-09-06 实测）：本项目已有的 `src/tools/symbol_search_tool`（东财 search + Yahoo + 交易所官网）对 LLM 查一个标的够用，但满不了 type-ahead 的三个硬条件——`茅台` 回 **0 候选**（东财 suggest 在本机住宅 IP 下回非 JSON，上游项 a-stock-data 的 FAQ 对 `search-api-web` 记同一现象；Yahoo 直接拒非 ASCII）、`00700` 回一支台湾 ETF **没有腾讯**（Yahoo 按文本相关度不按代码前缀）、**单键 380-2300 ms**。正解是 `src/symbol_roster.py`：本机富途上市目录（**不耗历史K线额度**）+ akshare 北交所，落盘 `~/.vibe-trading/symbol_roster.json`，内存扫一次 32273 行 23 ms。另：`_normalise` 只在**后面紧跟数字时**才剔掉 `SH|SZ|BJ|HK|US` 前缀，否则 `HP`（HP Inc.）会被改写成 `P`、`SH`（Soni 的一支 ticker）会被剪空；`SZSE:000001` / `NASDAQ:AAPL` 这类交易所前缀式输入**不命中任何候选**，因此不会被换掉（TVChart 的透传分支得以存活）
39. **活体探针的“差值”必须先证明是探针自己的口径，才能当成数据回归**（㉓，本轮两个“未决问题”最后都是假的）：① `EXPECT=3777` 是**网关原始行数**而 `kept=3027` 是**过滤后行数**，750 的差全落在本次未改动的 HK 六位码规则上（那 750 条实测全为 `81xxxx` 的「(临时代码)」占位）；② “旧缓存 BJ=1002” 是把 round-trip mismatch 计数误读成了行数，实测拆开 = 341 条 BJ（富途无北交所，反向推 `f"{market}.{code}"` 必失败）+ 661 条带第二个点的 US（同一个反推在 `US.HPE.PRC` 形状上被拒）。两条同一个形：反推一个不可逆的映射（行 → 网关 code）算出的“不一致”，描述的是探针而不是数据。配套：**富途 `code` 带市场前缀**（`HK.00700`），所以任何“宽度”判断必须用 `code.split(".")[1]`，用 `len(code)` 会恒真（本轮就是这样把整个 HK 判成越界的）。③ 同一类事在测试上也发生了一次：全量 `pytest` 与 `vite build` **并发**跑多出 1 条假红（54→55），串行重跑回到 54/12738——所以下结论前先查“这一轮旁边有没有另一个写盘的任务”
40. **`monkeypatch.setitem(sys.modules, "pkg.sub", 假模块)` 对 `from pkg import sub` 无效**（㉓，现象是“单跑绿、全跑红”）：一旦真实子模块已被别的测试文件（本轮是 `test_futu_gateway.py`）导入过，`from pkg import sub` 会**优先读父包属性**，注入的那份被静默无视，被测代码拿到的是真货（本轮真的去连网关 → `RosterUnavailable: futu gateway is not configured`，而 conftest 按第 37 条把 `FUTU_HOST/FUTU_PORT` 钉成了未配置）。子模块必须 `monkeypatch.setattr(real_module, "fn", ...)`（先 `from pkg import sub` 再改属性）；顶层模块（`futu`、`akshare`）走 `import x` 时 `sys.modules` 注入仍然有效，所以同一个文件里两种桩法共存不矛盾。定位手段：`pytest tests/test_futu_gateway.py tests/test_symbol_roster.py` 这种**最小共跑**而不是全量跑
41. **输入框类组件有两个缺陷只在浏览器里看得见**（㉓，单测全绿同时实跑出错）：① **Enter 的语义**——测试只会验“按 Enter 提交屏幕上那个字符串”，而真机里“屏幕上的字符串”可能是 `00700`（非规范裸码，只能走完整 loader fallback 链解析，且会被 session/自选/报警当规范码存下来）；规则是“不可路由 + 有候选 → 取首条，可路由或无候选 → 原样提交，显式高亮「使用 X」→ 永远尊重”，路由判定用后端 `_is_chartable` 的浏览器镜像 `isRoutableSymbol()`；② **辅助文案的成立条件**——`emptyHint`（无匹配提示）只判“框里有字”，于是在列出两条候选的同时写着「无匹配」；任何“空态文案”必须同时判**列表为空**。另：`frontend/tsconfig.json` 的 `exclude` 掉了测试文件 ⇒ IDE 用 inferred project 分析会假报 **`toBeInTheDocument` 不存在**（本仓库已有 11 个上游测试文件同报，共 29 条）；不要为此改测试写法，用一份临时 tsconfig（`include: ["src"]`）跑 `tsc --noEmit` 证伪后删掉（真错只有 `.at(-1)` 需要 lib es2022 一类，改索引写法即可）
42. **新增测试只要 patch `os.*` 就必须走 `tests/module_os_helpers.patch_module_os`，CI 有专属门禁（gate f）**（㉓）：`monkeypatch.setattr(sr.os, "replace", boom)` 里的 `sr.os` **不是每模块一份的副本，就是全局那个 `os`**，所以替换对整个进程生效；而 pytest 自己的 teardown 会调 `os.scandir`/`os.replace`，且 `pytest_runtest_*` hook **跑在 fixture finalizer 之前** ⇒ 一个会抛的替换能把 teardown 链带走，之后的 undo 永不执行（#1123：一个这样的测试造出 ~3000 条级联 error）。正确写法：`patch_module_os(monkeypatch, 被测模块, replace=...)`（它改的是**模块对 os 的引用**）。两条附注：① 门禁只算**静态 grep**（`tools/ci_grep_gates.sh` 的 gate f，排除 `module_os_helpers.py` 自身），所以本地 `pytest` 全绿不等于过门——**提交前必须跑一次 `bash tools/ci_grep_gates.sh`**（Windows 上用 `& "C:\Program Files\Git\bin\bash.exe"`，`C:\WINDOWS\system32\bash.exe` 是 WSL，在本仓库路径上跑不出来），而且**看它的输出必须先剥 ANSI**（`$_ -replace "\x1b\[[0-9;]*m",""`）——`ok`/`FAIL` 都被色码前缀顶着，用 `^FAIL` 筛选会**把两道红门读成绿**（本轮就因此误判过一次 gate f 已修好）；② 它是**静态 grep，不分代码还是注释**：把 `setattr(sr.os, ...)` 写在 docstring 里解释「不要这么写」，同样会被 gate f 拦下（本轮就是这么二次红的）——引用坏写法时用反引号包裹但**不要带括号与逗号**，或直接换成文字描述；③ 脚本自 ㉑ 记录的“五道门”已长到**六道**（新增的正是 gate f），gate b 唯一命中的 `.qoder/repowiki/**.md` 被 `.git/info/exclude` 本地忽略，与 ㉑ 记下的是同一存量，不是本次引入的。
43. **前端“一份文档一个页”的四个静默失效面**（㉔，都是单测/读代码看不出来、只靠实跑或真浏览器才暴露的）：① **标题锚点必须同一个函数算两次**——react-markdown 默认不给标题加 id、仓库无 `remark-slug`，目录与渲染各自算就会「链接照样点、页面不动」；而且 GitHub 规则的**剥除顺序**是地基：剥除集里必须**保留空格**，转完才把空格换 `-`，先剥标点会把词间空格一起吃掉（`15 专业图表` → `15专业图表`）。钉法：一条“每条目录 href 都能查到已渲染 id”的断言，而不是只测 slug 函数本身。② **passive effect 里抛异常 = React 卸载整棵树**，不是“那一块没渲染”——`decodeURIComponent(location.hash)` 遇非法百分号转义（`/help#%zz`，用户手打得出来的 URL）直接抛 `URIError`，于是一份说明书因书签编码不对就整页白屏。凡“读用户可控输入再去做一件可选的事”（滚动/聚焦/测量）都要包起来，失败退化成 no-op。③ **jsdom 的 `window.location` 在同一文件的用例之间不复位**：上一个测试 `link.click()` 留下的 hash，会让下一个测试挂载时去滚一个陈旧的锚点，撞上 ② 就把不相邻的测试带红（本轮表现为“另一个测试断言 null”）——`beforeEach` 里 `window.location.hash = ""`。另：**jsdom 不实现 `scrollIntoView`**，而上游 `src/tests/setup.ts` 是垫片集中地但属于上游文件（改它 = 新增一个同步冲突面），所以要么在本文件内解决，要么像 ② 那样让生产代码容错。④ **测试里读仓库文件用 `path.resolve(__dirname, ...)`，不要用 `import.meta.url`**：jsdom 下 `import.meta.url` 是 http 协议，`fileURLToPath` 会 `TypeError: The URL must be of scheme file`（仓内 `src/__tests__/viteProxy.test.ts` 已是前者这个惯用法）。
44. **本机 vite dev 只绑 IPv6（`[::1]:5899`）**（㉔取证时踩到）：用 `Invoke-WebRequest http://127.0.0.1:5899/...` 探测会得到“无法连接到进程”，而服务其实好好的——`netstat` 里能看到它只在 `[::1]` 监听。两个后果：① **探测 dev 服务必须用 `localhost:5899`**（`:8000` 的后端绑 IPv4，`127.0.0.1` 可用，不要拿同一招验两个端口）；② 看到“端口被占用”而 HTTP 又连不上时，不要直接断定“有个挂死的残留进程”并去 kill，先 `Get-NetTCPConnection -LocalPort <p> -State Listen` 拿到 PID **再 `Get-Process -Id` 确认它是什么**（沙箱里 `netstat.exe` 有时直接“拒访”失败，产出的空结果不是“端口没人听”的证据）。
45. **`pd.Timestamp(naive).timestamp()` 把 naive 当 UTC，而 `datetime.timestamp()` 当本地——行情源的 naive 时间戳全部是交易所自己的钟点**（㉕）：两者差 8 小时（实测 `1788514500` vs `1788485700`），于是 A 股 14:35 的分钟线一直渲染在 22:35，**且这个错从 ⑧ 就存在**。三个可复用的推论：① **日线看不出这类错**——日期标签能活过 8 小时挪动，所以这类错在日线上 latent、上了分钟线才穿帮，“日线好了”不是“时区逻辑对了”的证据；② 修的时候必须**区分两种 timestamp 语义**：“时刻”要本地化，“交易日标签”不能——给 `_bars_from_frame` 加的 `wall_clock_zone` 默认 `None` 就是为了让日线逐字节不变（把美股午夜空点本地化会让日期掉到前一天），以及**已 aware 的索引不能二次 `tz_localize`**（会平移两次）；③ **探针要打印钟点而不是只打印 epoch**，本轮就是靠“HK 与 AAPL 末根 epoch 竟相同（12 小时时差）”这个不该出现的相等反推回去的；确认一个 naive 列是哪个钟点，方法是找它的**结构性缺失**（茅台分钟序列独缺 12:00＝午休）。（另：`Asia/Shanghai` 无 DST 可直接充当 HKT；美市用 `America/New_York` + `ambiguous=True, nonexistent="shift_forward"`。）
46. **同一规则写两份就一定会漂移，钉法只能是“跟真源对照”而不是“各自测各自”**（㉕，与第 42/43 条同族但面更大）：本轮抽出 **5 处镜像**。前端两处：`canMinute`（按钮 disable）与 `readSession`（分享/刷新恢复）各持一份 A 股正则，后果是「分享链接能渲染、刷新掉回日线」——**只放宽按钮那一处就能做出一个只在刷新后才出现的 bug**；做法是抽 `canMinuteBars()` 单一谓词供两处共用并 export 给测试。后端三张表（本路由的 `_FUTU_MINUTE_INTERVAL` / 端点周期表 `_MINUTE_PERIODS` / `FutuLoader._INTERVAL_MAP`）+ 一张时区表，全靠 `TestTablesAgree` 钉：三表 key 集合相等、**每个映射值必须存在于真 loader 表里**（而不是写死一份期望值）、zone 表的 key == suffix 集合。附带一条同样重要的：**错误文案也要断言措辞**，只断言 `pytest.raises(ValueError)` 时文案写错测试照样全绿——本轮把测试升级后才发现自己的兜底文案让 BTC-USDT 用户“去检查 OpenD 是否在运行”，而**那个标的一根本没被发给富途**；只有真连网关的活体探针才能把这种“测试全绿的误导”抓出来。**本条自己有个更硬的后续**：㉕ 当时按“两处”抽出谓词并只改了两处，**漏下的两处当天就酿成一次回归**（按钮亮着、点了没反应，因为 `pickInterval` 仍持有旧 guard），被用户一句「只有K线没有分时」抓了回来。所以把本条的可执行部分改写成三句：① **先枚举后动手**——改一个规则的任何一处前，`grep` 出它在页面/路由里的**全部**调用点并逐个打勾，“抽个函数出来”本身不是修完；② **纯函数测试钉不住接线**——谓词自己的 case 全绿而功能是碎的，必须有一个**挂载 + 真实点击 + 断言外部收到什么**的测试（jsdom 下给 chart 假实现并记录 `setPeriod` 参数就够了）；③ **新增测试要反向验证**——临时把代码改回旧行为，确认**恰好预期的那一例转红**，否则你不知道测试是不是空跑。
47. **Windows/PowerShell 取证四坑，每一个都能把“好”读成“坏”**（㉕全部实踩）：① `npx` 命中 `npx.ps1` 被执行策略拦下（`禁止运行脚本`）⇒ 用 **`npx.cmd`**；② `"C:\Program Files\...\bash.exe" script.sh` 直接 PowerShell **ParserError** ⇒ 带引号路径必须加调用运算符 **`& "path" script.sh`**；③ `Out-File -Encoding utf8` 会在文件头写 **BOM**，再用 `print()` 回显到 GBK 控制台就 `UnicodeEncodeError: 'gbk' codec can't encode character '\ufeff'`——**报错在回显语句而不在被读的文件**，很容易误判成“门禁输出坏了”；干脆用 Read 工具或 `[IO.File]::ReadAllText`；④ `cmd | Out-File -NoNewline` 会把整段多行输出**拼成一行**，于是 `Select-String "^FAILED"` 零命中并错判为“没有失败”；改用 `[regex]::Matches($t,"FAILED (\S+)")` 或干脆不加 `-NoNewline`。另两条旧知仍然有效：管道后的 `ExitCode` 会被 `NativeCommandError` 污染，要单独取 `$LASTEXITCODE`；看 `ci_grep_gates.sh` 输出先剥 ANSI（`$_ -replace "\x1b\[[0-9;]*m",""`）。
48. **分时这类画布功能只能用模型层数值取证，截图不算证据**（㉘）：本轮一张写着「1290.88 / 1 个交易日 / 331 根 / 1 分线」的截图，其字样全仓 `grep -rn '分线'` **0 命中** ⇒ 截图与同一时刻的 JS 上下文**不是同一份渲染**（后台标签页 rAF 停摆、或画面是上一代代码留下的）。可执行的部分：① 先把截图里的字样当**指纹去 grep**，命中 0 次就判定证据失效（与 ㉖ 交付后那条「报错文案在代码里查不到 ⇒ 先怀疑进程年代」是同一招的两个方向）；② 断言只建在可读回来的量上——klinecharts 用 React fiber 从容器（挂的是 `__reactContainer$`，不是 `__reactFiber$`）走到 `chartRef.current`，再读 `getBarSpace()` / `getVisibleRange()` / `getIndicators()` / `getPaneOptions()`；中途要 deref `stateNode` / `stateNode.current` / hook 的 `memoizedState(.current)` / `queue.lastRenderedState`；③ 页面被 HMR/reload 重置后挂在 `window` 上的助手会丢，**每个脚本自带完整的 fiber 遍历**而不是复用上一步的引用。
49. **第三方库的扩展点先实测再往上建功能，“能传进去”不等于“会被读”（㉘）**：klinecharts #341 要的「以昨收为 0 轴、上下对称、右侧百分比副轴」需要的 `overrideYAxis({createRange})` 确实**挂得上**（`ChartImp.overrideYAxis` dist 15561 → `DrawPane.createOrOverrideYAxis` dist 10308 → `YAxisImp.override` 的 `merge`），但 `createRangeImp`（dist 1078）读的是 **`realFrom/realTo/realRange`**：只返 `{from,to}` 会把轴打成 NaN、ticks 空；补齐 9 个字段后**生效的仍是数据自然范围**。结论：该扩展点在本页的 dev 双挂载/后台标签页路径下不可靠 ⇒ **换一个能达成同一目标且机制清楚的做法**（昨收用 `registerIndicator({series:"price"})`，因为 `YAxisImp.createRangeImp` 会把 pane 内 indicator 的 figure 值纳入纵轴范围）。同族细节：`StoreImp.setBarSpace`（dist 13666）越界是**静默 return 而不是 clamp**；`ChartImp.getYAxes(filter)` 的 filter 是**必填**（dist 15540 直接解构），与 `getIndicators`（有 `?? {}`）不同。
50. **一天之内会变形状的功能，取证必须覆盖两个时刻**（㉙）：㉘ 在收盘后取证（331 根）全绿，用户第二天盘中（13 根）看到的却是一半空白——因为这类缺陷的定义式是 `内容宽 = min(需要的间距, 库上限) × 根数`，**只有根数少到撞上限时才现形**，而「少」只发生在开盘后头几个小时。可执行部分：① 任何按「数据条数 / 容器宽度」算视口的代码，测试与活体都要**各取一次「数据少到撞边界」和一次「数据多到铺满」**；② 造不出真实数据时，用 monkey-patch `window.fetch` 把响应截断，**走的仍是生产 fit 路径**（比直接改 store 有力得多），用完立即撤销并重载复原现场；③ 「另一半用户不受影响」（收盘后逐像素不变）这类断言，要靠**残差 ≤ floor 余量**的算术证明，不是靠「我看着还是那张图」。
51. **「进入时借、退出时还」要把借用的每一项列全，且替身要建模「调用时的状态」**（㉙，与第 46 条同族但面向副作用）：① ㉘ 修了缩放借还，但同一个 fit 还写了**第二项**（右边距）却没还——库把偏移存成 bar 数且 `resetData` 不重置它，于是一台 1920px 面板的 1270px 残差回到蜡烛图变成 254px 空白。**判据：把 `setXxx` 的每一个调用点都当成「借了东西」，逐个问「退出时谁还」，而不是只看上报症状那一侧。**② 从未借过的那条路径（页面直接开在分时）必须有兜底，且兜底值要用**库自己的常量**（`DEFAULT_OFFSET_RIGHT_DISTANCE = 80`、`DEFAULT_BAR_SPACE = 10`），不要自己挑一个“看起来差不多”的数。③ 替身只把偏移建成「一个数字」会被自修复掩盖（恢复路径 fit 两次，第二次洗掉第一次的顺序错）⇒ 要记成 `{distance, atSpace}` 的**有序日志**；而像素值一律经替身自己的 bar-count 状态推导（`Math.max(0, bars*space)`），测试不读私有字段。
52. **变异探针自己会骗人，两种假绿都要防**（㉙；与第 39 条末尾“并发跑多出假红”是对称的一面）：① **同一路径跑多次**（本例 StrictMode/会话恢复下 fit 两次）会把「顺序错」在第二次修正⇒ 一个变异只杀一例时，先怀疑断言建在了一个被洗过的可观测上，而不是庆祝「测到了」。② **文件未落盘**：一次编辑工具报 `save failed` 但实际延迟写入，vitest 读到的是旧内容，于是「29 例全绿」。判据：**在同一条命令里先 `Select-String` 回显磁盘上的变异态、再跑测试**，把「变异确实生效」变成每次的前置断言。③ 新增一个全局副作用（本例 `registerLocale`）会横扫所有挂载型测试替身（三个兄弟文件一跑就 TypeError），**先补替身再跑全量**，否则红在无关文件上容易被误判成“我的改动有连带问题”。
53. **「启动报错」先比进程创建时间与报错时间，再决定是修代码还是修启动脚本**（㉚）：`Get-CimInstance Win32_Process -Filter "Name like '%python%'"` 显示进程 `CreationDate` 只比截图时间戳早十秒、且现在 `Invoke-WebRequest` 全 200 ⇒ 是**冷启动竞态**不是故障（Uvicorn 在 lifespan 预检跑完才 bind 端口）。可执行部分：① 一键启动脚本里**不要用固定 sleep 等后端**，改成轮询 health 端点；超时也要继续往下走（只打 WARN），否则用户会得到一个「什么都不发生」的黑窗口。② 改 `.bat` 时**注释必须 ASCII**（cmd 按字节读 UTF-8 bat，长中文注释行会错位并把行尾当命令执行），且必须干跑：把 `start` 行换成 `echo` 后跑，用 `> file 2>&1` 重定向再按 UTF-8 读回（直接管道进 PowerShell 的乱码是读取块边界假象）。③ vite 代理的 `ECONNREFUSED` 刷屏**只可能出现在 dev**（:8000 服务 dist 的生产模式没有这层代理），看到它先想代理的上游是谁，不要当成前端回归。
54. **本沙箱里 `git.exe` 间歇性启动失败（`拒绝访问`），而它仍会报 `PUSH_EXIT=0`**（㉚，第 52 条“假绿”搬到交付环节）：同一命令内可能第一个调用被拒、后几个正常，也可能反过来（本会话连踩三次：commit 没执行、push 没执行、连垫场的 `git --version` 也被拒）。**所以“先垫一次廉价 git 调用”不是可靠解**（写过一次，下一轮就被自己的观察证伪）。可靠做法只有一条：把推送写成**带收敛判据的重试循环** —— 每轮先 `git ls-remote origin -h refs/heads/main` 拿远端真 ref，等于本地 `git rev-parse HEAD` 就 break，否则 `git push` + `Start-Sleep 3`，最多 6 轮；循环结束后再独立取一次 `remote / HEAD / git rev-list --left-right --count origin/main...HEAD`（应为 `0\t0`）。**但重试循环自己也会假绿（本会话实踩，比上面的坑更隐蔽）**：git 被拒时输出是空的，于是 `$null -eq $null` 为真，循环当场报 `converged at loop iteration 1` —— 而远端其实还停在上一个提交。所以收敛判据必须是**先断言两侧 ref 都非空**（取不到就重试或干脆报「无法下结论」），非空才允许进比较；本轮最终的可靠形式是把每次 git 调用包成 `function G { $out = & git @args_ 2>$null; if ($LASTEXITCODE -ne 0) { return $null }; … }`，用 `$LASTEXITCODE` 而非输出来判定调用是否真的发生了。三个不能当验收的反例：管道后的 `$LASTEXITCODE` 在 native command 被拒时仍为 0（PowerShell 层根本没跑起来，拿到的是上一个命令的残留值）；`git status` “clean” 只说明工作树，ref 落后看不出来；`git rev-parse --short A B` 多 ref 会报 `Needed a single revision`，要逐个取。**同族再坑（本轮实踩）：把这套重试循环写进 `.ps1` 再从沙箱里 `powershell -File` 嵌套调用，`git.exe` 会完全起不来**——此时 `$head[0]` 是 `$null`、`Substring(0,8)` 抛错被吞，日志里 `local HEAD = ` 是**空的**却照样打出 `CONVERGED at loop 1`。⇒ 守卫不能只判「数组是不是 `$null`」，要判**取到的值本身是不是 40 位十六进制**（`if ($v -notmatch '^[0-9a-f]{40}$') { retry }`）；而这类循环必须跑在输出能被直接观察的 shell 里（嵌一层就把错误藏起来了）。本轮最终就是靠工具直连的 shell 独立复读两个 40 位 ref 相等（`HEAD` 与 `git ls-remote` 都是 `97e791d9…`）+ `rev-list --left-right --count` = `0⇥0` 定案。

55. **等待/轮询类脚本的预算要按墙钟写；负向断言必须先证明被测对象真的在跑**（㉚ 复查自己第一版时踩的）：① 「循环 N 次」≠「等 N 秒」——本机一次 refused connect 就要 ~2s（`TcpClient.Connect` 同速，说明卡在 connect 层，调小 `-TimeoutSec` 没用），于是自称 `up to 90s` 的闸门实际最坏 ~272s。判据：**任何自称秒数的等待，用 `[Diagnostics.Stopwatch]` 把墙钟当循环条件**，并且分别实测「正例多久放行」与「把上限调小后负例多久退出」两个数（本轮 0.7s 放行 / 对 8s 上限 8.2s 精确退出）。② 探测端口就绪不必付连接成本：`[Net.NetworkInformation.IPGlobalProperties]::GetIPGlobalProperties().GetActiveTcpListeners()` 纯 .NET、不需要模块、~30-180ms，比 `Invoke-WebRequest`/`Test-NetConnection` 快一个量级，适合高频轮询；命中之后再打一次真 HTTP 确认语义。③ 用 cmd / `Start-Process` 驱动脚本时，**先断言脚本真跑了**（stdout 里要有已知的开头与结尾标记、stderr 必须为空），否则「没报错」「没放行」这类负向结论全是空的。④ 取证脚本不要开 `$ErrorActionPreference='SilentlyContinue'`：它会把 `Start-Process` 失败吞成「下一行照样报成功」。
56. **把「固定 UI」换成「用户可编辑的状态」时，新不变量会推翻旧用例的假设；改产品之前先证明那个状态用户造不出来**（㉛）：新语义是**每视图那条有序列表是权威**，`syncSubPanes` 在 init effect / `setView` / picker 三处各重铺一次。它的直接后果是一条既存用例变红（假图说「什么都没挂」而列表还要 MACD ⇒ 面板被重建），而**正确的处置是改测试**：① 先查清「面板被关了但列表仍要它」这个状态**能否由用户点出来**——本轮不能（内置副图只能从 picker 关，关就同时改集合），所以旧用例那种「只删面板不改集合」的走法已经不代表任何真实操作，把它重写成走真 UI（先点「副图指标」再点「关闭副图 MACD」，然后手工销毁面板并注上为什么）；② 若那个状态**真能**造出来，那它就是产品 bug（换个标的那条副图自己活过来），要补的是产品守卫而不是改断言。配套的两条：判定「这条是否已上图」用**面板存在性探针** `getPaneOptions(paneId) != null`（dist 15587-15594，与 ⑲ `paneLookup` 同一个）而不是 `getIndicators({name})`；而**跳过不等于不创建**——守卫换成探针后那条用例仍然红，因为面板真的不在了，这一步只能靠①定夺，不要误以为换了探针就修好了
57. **测试替身的两类静默假绿：共享状态与忽略形参（㉛，本轮各中一次，第一次还打破了既存用例）**：① 替身里记录「图上有什么」的列表必须**每次 `init()` 新建一份实例私有的**（挂在 helper 全局上，StrictMode 第二次挂载就会看到上一世代的「已在图上」而一条也不创建，㉘ 写过这条判据但 `mounted` 仍漏在外部）；② 把这个列表**镜像**给外部辅助函数读时，镜像必须是**同一个数组对象** ⇒ 删除要原地 `splice`，`x = x.filter(...)` 重赋值会断链；③ **带形参的查询**（`getPaneOptions(id)`、`getIndicators({paneId})`）在替身里忽略形参 = 「关掉的东西换个标的又活了」这类缺陷永远测不出来。判据：**给替身自己补一条「按 id 问应该得到不同答案」的断言**，替身不通过就别往上建功能
58. **搜索类工具的输出会剥掉命中行的前导空格，据此判断缩进会把好代码改坏**（㉛，本轮自伤）：按 Grep 回显误判 5 处「缩进丢了」，发出的 `SearchReplace` 部分成功后**真的**把两行改成了 4 空格 / 20 空格。判据：**定位用 Grep，缩进与空白一律 `Read` 复核后再改**；而“改缩进”这种看起来无风险的批量修复尤其要先确认基线本来就是好的。同族的两条旧判据仍然有效：门禁/测试输出先剥 ANSI 再匹配，PowerShell 管道后的 `ExitCode` 要单独取 `$LASTEXITCODE`（第 47 条）
59. **写每一句“用户会看到 X”前，先复现一次它的触发路径；复现不出来就把它降级为实现注释**（㉛）：三例本轮全部实际发生过。① `addSub` 里的「副图最多 6 个 — 先关掉一个再加」**点不到**（picker 已把目录按钮 `disabled`，存储加载也截到 6），它只是不变量守卫 ⇒ 说明书只能写「按钮点不动」，不能承诺一句提示；② 工具条 tooltip 原文「（K线的副图各记各的）」在只展示一个视图时是病句 ⇒ 改成跟着当前视图的「（分时与 K线各记各的，现在改的是…）」；③ 一句注释写「`syncSubPanes` runs inside the data loader」是错的（实际在 init effect / `setView` / picker），**错的注释比没有注释更贵**——下一轮人会按它去插桩。同一判据反过来也成立：变异探针脚本里自己写的 `Tests \d+ failed` 正则被 ANSI 码挡住，于是把 5 次全 killed 标成了「SURVIVED-ish」⇒ **自己写的解析式也要拿真输出验一次**（第 47 条）

60. **换算类代码的「单位」选错不会报错，只会伪装成另一种缺陷**（㉜）：`count 根周线 = count × 6 根日线` 是按**交易日**估的，而加密 7×24 每天一根 ⇒ 取回窗口偏短 ⇒ 页偏短 ⇒ `more.forward=false` ⇒ **一个尺寸 bug 长得和「历史翻到头了」一模一样**（活体：`BTC-USDT 1W count=200` 只回 176，而链上还有四年日线）。三条可执行判据：① 凡 `条数 × 每条吃几根` 的定尺，用**日历上界**而不是市场经验（一周最多 7 根日线、一月最多 31 ⇒ 8/32），上界是**可证明**的、经验值是每市场一个 bug；② 必须有一条「**每天一根的 tape 仍然能填满页**」的用例（1400 个日历天 ⇒ 断言 200 / 40）；③ 变回去要能复现**与活体一样的错数字**（旧表下 176/32）——这是「单位错」而非「数据浅」的决定性证据，两者靠计时器与日志分不开。同一轮里“页为什么短”的第一版解释（“源没那么多历史”）就是错的，错在它**能自洽**。适用边界：任何“拉 N 个聚合单元就要先去多少条明细”的路由/分页/缓存定尺。
61. **明知不可达的守卫，要么抵达它要么删掉它，不要静默留着**（㉜）：第 60 条的修法（日历上界定尺）顺带让上一行的 `merged = merged[1:]` 变成 **no-op**（“满窗 ⇒ 合并出的组数 > count”恒成立，尾部 `[-count:]` 已经丢掉残组）。B1 变异（把它改成 `merged[:]`）第一轮 **SURVIVED**——不因为测试差，因为**没人能走到那一行**。处置不是“删了了事”也不是“留着假装测到了”：① 注释写明它在什么条件下才可达（页短到仍含残组，否则那一组的早期日子会永久落在两页之间）；② 用 `monkeypatch.setitem(常量表, "1W", 6)` 把那个条件**在测试里捏出来**（`test_the_guard_still_fires_when_the_page_itself_is_short`，同时断言两页拼起来与真值后缀相等 ⇒ “页间无洞”）。⇒ “兜底分支”与“防手搓请求的 ceiling”这类代码，下一轮只会问“测到了吗”，没人在它改成 no-op 时问。
62. **第 26 条（盘符大小写让 vitest 集体失明）本轮原样再踩，多了两个新形状**（㉜）：① 这次不是脚本而是**驱动器里的 `ROOT = Path(r"e:\…")`**，vitest 报 `Test Files 1 failed / Tests no tests` + `TypeError: Cannot read properties of undefined (reading 'config')`，**根本不印 tally** ⇒ 三条前端探针安静地全是 INCONCLUSIVE，与“测不出问题”长成同一个样子（同一个文件同一个命令，PowerShell 里 rc=0、Python 里 rc=1，差的只有一个字母的大小写）。② **vitest 4 在管道里仍然上色**（继承 `FORCE_COLOR`），`^\s*Tests\s+(\d+) failed` 这类锚点必须先剥 ANSI（`re.sub(r"\x1b\[[0-9;]*m", "", out)`），否则“锚点存在却一条也不匹配”；又因为 vitest 先印 `Test Files` 再印 `Tests`，直接 `search(r"(\d+) failed")` 会把“几个文件红了”当作“几例红了”。③ 探针自己 `print()` 中文 + `›` 到 cp936 控制台会 `UnicodeEncodeError`，而它**在文件已复原之后才抛** ⇒ 整个结论被吞掉；开头 `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`。④ 驱动里 `needle` 的多行串必须按**目标文件自己的换行**拼（仓内 Python 文件是 **CRLF**），否则 0 命中静默 SKIPPED。⇒ 加固后的判据（本轮驱动器 `.qoder/tmp/mutate_coarse.py` 就是这么写的；该目录不入库，**判据以本条为准**）：**变异前先跑一次未变异基线**，不绿就 `exit 3` 拒绝变异（第 26 条那句“基线自检”本轮才真的实现），并把 `no tests`（套件没加载）与 `no tally`（真的没数字）分开报。
63. **测试替身手抄一份产品常量 = 新增档位从页面级测试旁边溜走**（㉜）：`ProChartDrawings/Paging/MinuteBars` 原本各自 `vi.mock("@/lib/marketApi", () => ({ INTERVALS: [{key:"1D"}], periodToInterval: () => "1D", fetchKline: 假的 }))`——工具栏长出周月两档，这三个文件**既不会变红也不会看见**（它们测的是一页只有日线的假应用）。正解是**只假传输层**：`vi.mock(mod, async (importOriginal) => ({ ...await importOriginal<typeof import(mod)>(), fetchKline: 假的 }))`。判据：**mock 一个模块时逐个问“我为什么要抄这份常量”**，除非它会打网络/读时钟，否则就不该抄它。同族且同轮发生的是替身的**行为**也会污染断言：假 `setSymbol` 在挂载时就按默认 day 周期跑了一次 loader，于是“点周线后不应再出现 1D 请求”必须写成“**挂载之后没有新增** 1D”（先取基线计数），否则第一条红是替身自己点的。

64. **bundled preset 能 grant 什么，是由一个「密钥之前就被解析」的隐藏世界决定的**（㉝）：`test_preset_honesty.py::test_every_granted_tool_resolves` 与 `test_swarm_presets_packaging.py::test_every_preset_tool_exists_in_local_registry` 都调 `build_swarm_registry(requested, include_shell_tools=True)` / `build_registry(include_shell_tools=True)` 且**不传 `agent_config`** ⇒ 任何依赖凭据或依赖远端 MCP server 的工具在 bundled preset 里**永远解析不出来**（运行时也是真拿不到，不是测试苛刻）。三条可执行判据：① 想让一个工具进 preset，先问「它在无密钥注册表里存在吗」；② 因此**「默认关、靠 env 打开」与「进 bundled preset」不可共存**，别一边写开关一边写 preset；③ `src/tools/__init__.py::build_swarm_registry` 对白名单里解析不出的名字是 **warn 后静默丢弃**而不是报错，所以「preset 写了」与「worker 真拿得到」之间没有任何默认保证（#1343 就是这条）。附带一条同族的反向塑形：`test_every_tool_named_in_a_prompt_is_granted_to_that_node` 会扫 `system_prompt` 里**所有含下划线的已注册工具名**，所以「在 prompt 里提一下那个计费工具」= 要求 grant 它 = 撞回 ① ⇒ 计费工具的说明只能待在它自己的 `description` 里。
65. **naive 时间线遇到 aware `now=` 是运行时才炸的 TypeError，而它藏在「测试全都喂 naive」里**（㉝）：`pd.Timestamp(aware)` 与 naive 索引比较直接抛错，因果时间线又是刻意建成 **naive UTC 日界**的（period end + 滞后 = 一个日历日，没有时区可谈），于是「接受外部时刻」的每个入口都得显式表态。三条规矩：① `now=` / `as_of=` 这类公开入参必须**同时**接受 naive（按 UTC 面值读）与 aware（先 `tz_convert("UTC")` 再 `tz_localize(None)`），见 `macro_regime._naive_utc`；② 只要一个 aware 值会被降级成日历日，就必须**先转 UTC 再取 `.date()`** —— `str(dt)` 打印的是本地日，JST 08:00 会被读成「当天已经有」而实际是前一天 23:00；③ 测试要**各喂一次 naive 与 aware 并断言两者同意**，否则实现带着 bug 也能全绿（本轮就是这样通过了 39 例才在活体路径上撞出来）。同族旧账：第 45 条那条「`pd.Timestamp(naive).timestamp()` 把 naive 当 UTC 而 `datetime.timestamp()` 当本地」是同一枚硬币的另一面。
66. **编辑工具的回执不能证明磁盘状态，两次都要单独验**（㉝，本轮两次自伤，比第 52/58 条更狠）：① 同一个文件连续两次 `SearchReplace`，第二次可能拿**过期缓冲**写回，于是**第一次的改动被静默冲掉**（本轮丢了一个函数，靠 `ImportError` 才暴露；回执照样是 `success` 且带 diff）；② 一次多 pattern 的批量替换报「第 3 个 pattern 未匹配 → 失败」，但第 1/2/4 **已经落盘** ⇒「全部有效才执行」这个直觉不成立。判据：**同文件连续编辑后一律 `Select-String` 回读磁盘真实状态**，改测试就再跑一遍对应文件（本轮 `_SECRET` 那枚 token 就是这么确认的第二遍 42 例）；跨文件的批量替换失败后必须重新 grep 实际内容再决定补哪几条，不能假设「报失败 = 什么都没动」。
67. **「新增一个东西」的真实成本是它牵动的文档计数面，先数清楚再选形态**（㉝，本轮据此删掉两个原计划的交付物）：`test_readme_counts.py` 把「reader 看得见的每个数字」钉在代码上，于是三种形态的连带面差一个量级 —— 一个 **bundled skill**：6 份 README 的 badge + 7 处 prose（`test_every_skill_count_in_the_prose_is_current` × 六语）+ 类别表（`test_the_skill_category_table_matches_the_frontmatter`）+ 树行；一个 **bundled preset**：6 份 badge + 树行；一个**无条件注册的本地工具**：6 份树行里的一个数字。⇒ 判据：在「新增一份资产」与「给既有资产补一件它缺的能力」之间，先算前者要同步几处文档，再问后者是不是本来就有位置 —— 本轮 `macro_strategy_forum` / `macro_rates_fx_desk` 就是那个「本来就有位置」，两个新交付物因此变成一个都没有新增，功能反而更可达（用户在 `--swarm-presets` 里已经会看到这两个团队）。

68. **`Select-String` 收到管道输入时，把「路径数组」当成「待匹配文本」——于是门禁报 0 命中而它一个文件都没打开**（㉝ 交付后独立复核时抓到，第 54/55 条的第三同族）：`$files | Select-String -Pattern X` 与 `Select-String -Path $files -Pattern X` 只差一个参数，前者对每个**路径字符串本身**做正则（文件名里当然没有 `datetime.now(`）⇒ 全零，后者才真读文件。本轮 gate (d) 先被报成 `RAW=0 FAIL=0`（看着像「扫描过且干净」），改用 `-Path` 后是 `RAW=13 FAIL=0`（真扫了 13 处，全部因带 `timezone.utc` 被豁免）。判据：**任何以「0 命中」为通过的检查，必须同时印出豁免前的总量**，raw 为 0 就说明扫描没发生，此时的 FAIL=0 没有任何含义；取文件一律 `-Path`（路径可能含 `[ ]` 时用 `-LiteralPath`，`-Path` 会按通配解释而静默丢文件）。与 54（`$LASTEXITCODE` 残留值）、55（负向断言被「什么都没跑」满足）合起来是同一句话：**取证脚本自己给出的 0 默认不可信，除非它同时给出一个非零的分母**。

## 五、Repo Wiki 说明

- IDE 需直接打开本文件夹（Git 仓库根）才能识别 Git 信息并启用 Wiki 自动更新
- Wiki 文件导出位置：本目录下 `.qoder/repowiki`
- 官方代码大版本同步后，Wiki 需手动触发更新/重新生成
- 2026-08-14 修复：曾在父目录 `E:\Vibe-Trading-main` 下误生成过 Wiki，已将其 `.qoder/repowiki` 整体移回本项目目录。生成 Wiki 前务必确认 IDE 打开的是本目录（Git 仓库根）
